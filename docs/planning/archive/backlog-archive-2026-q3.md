# Backlog Archive: 2026 Q3

This file stores older `done` and `cancelled` rows moved out of `docs/planning/backlog.md` by `.opencode/scripts/normalize-backlog.js` so the main backlog can stay focused on active work.

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P2-PERMCFG-005 | done | permconfig | Add positive fire-case to `dead_rule_lint_test.go` proving detection capability (closes the gap that `TestDeadRuleLint_NoShadowedAllowOrAsk` passes even with a broken detector because no real rule is dead). | build | done:2026-08-02 (build) — commit de44ff85. Extracted LintDeadRules helper; TestDeadRuleLint_PositiveFire synthesizes dead rule + asserts finding; mutation/red-control proven. source:review-defer · promoted-from:.local/coordinator/tasks/defer-f5-dead-rule-lint-positive-fire.json · studied:2026-08-02 · fired (trigger `path_touched(internal/permconfig/dead_rule_lint_test.go) OR path_touched(internal/permconfig/dead_rule_lint.go)` fired by b652311 2026-07-23 'fix(permconfig): close read_only migration residues'). KEY INSIGHT (2026-08-02 sharpen): detection logic is currently INLINE in the test (dead_rule_lint_test.go:56-114 inside TestDeadRuleLint_NoShadowedAllowOrAsk using t.Errorf directly) — extracting a callable `LintDeadRules(rules, agent) []Finding` helper into a NEW `internal/permconfig/dead_rule_lint.go` is the enabling refactor that lets the positive fire-case feed a constructed dead rule and assert a returned finding rather than t.Errorf. The existing TestDeadRuleLint_NoShadowedAllowOrAsk iterates REAL CoreLocationRules asserting NONE are dead — proves real-data cleanliness but would pass even if the detector were broken; the synthesized known-dead input proves DETECTION CAPABILITY. File scope: internal/permconfig/dead_rule_lint.go (NEW, optional refactor — extract detection into a callable helper), internal/permconfig/dead_rule_lint_test.go (add TestDeadRuleLint_PositiveFire synthesizing a known-shadowed-allow LocationRule, emitting its bash block, asserting the lint returns/reports a finding), internal/permconfig/rules_test.go (verify the synthesized LocationRule constructor/fields exist or extend as needed). Validation plan: (1) `go test ./internal/permconfig/... -run TestDeadRuleLint_PositiveFire` synthesizes a known-shadowed-allow LocationRule, emits its bash block, asserts the lint reports a finding; (2) mutation/red-control — temporarily disable the detection branch, confirm TestDeadRuleLint_PositiveFire goes RED, revert (proves the test exercises detection capability, not a tautology); (3) `go test ./internal/permconfig/...` full package green (no regression to the two existing tests); gofmt -l internal/permconfig/ + go vet ./internal/permconfig/... clean. Low-risk test-only (P2). |  |
| P1-TESTS-002 | done | tests,verify | Persistent Go-driven integration test for the unrendered-source render-location guard crux #1 (source-copy state-lib.js refuses to load). | build | done:2026-08-02 (build) — commit 1681e520. state_lib_render_guard: source-copy refuses with expected stderr; red control loads past guard. Decoupled (frozen fixture). source:review-defer · promoted-from:.local/coordinator/tasks/defer-f6-guard-crux-automated-test.json · studied:2026-08-02 · fired (trigger `path_touched(templates/core/.opencode/scripts/state-lib.js)` fired by 5cd5caf 2026-07-31 'Add deterministic TOCTOU re-resolve guard test'). KEY INSIGHT (2026-08-02 sharpen, open_question RESOLVED): test placement = Go-driven tests/integration exec test for crux #1 ONLY. Crux #2 (rendered verify-f3-authoring-surfaces.js passes) and crux #3 (verify-no-unrendered-paths.js FAILs on a recreated marker) are ALREADY persistently CI-covered by the verify-script runs themselves (verify-no-unrendered-paths.js invariants 1+2 cover #3; CI running verify-f3-authoring-surfaces.js covers #2) — a sibling verify-script would duplicate #2/#3. So the genuine narrowing is crux #1 only: a source-copy of state-lib.js must refuse to load (manual demonstrations in commit 186ba269 do not survive a future edit). File scope: tests/integration/state_lib_render_guard_test.go (NEW: Go-driven exec test invoking `node templates/core/.opencode/scripts/state-lib.js` against a source-copy fixture and asserting non-zero exit + the 'REFUSING to load state-lib.js from an UNRENDERED templates/core/ source copy' error), tests/integration/fixtures/state-lib-source-copy/ (NEW: a copy of state-lib.js retaining the {{COORDINATOR_DIR}} token so the render-location guard IIFE assertRenderedNotSource fires). templates/core/.opencode/scripts/state-lib.js is read/invoked, NOT modified (guard shipped + verified in 186ba269). Validation plan: (1) `go test ./tests/integration/... -run TestStateLibRenderGuard` invokes node against the source-copy fixture, asserts non-zero exit + stderr containing 'REFUSING to load state-lib.js from an UNRENDERED templates/core/ source copy'; (2) red control — run against a fixture copy where {{COORDINATOR_DIR}} has been pre-rendered away (simulating a guard regression / a dropped assertRenderedNotSource IIFE), confirm the test FAILs, revert (proves the test pins crux #1, not a tautology); (3) `go test ./tests/integration/...` full suite green; gofmt -l + go vet ./tests/integration/... clean. Non-goals: do NOT change the guard logic itself; crux #2/#3 stay covered by existing CI verify-script runs (no assertion duplication). |  |
| P2-DOCS-004 | done | docs,backlog | Document the mandatory two-commit normalizer archive-sweep landing (backlog skill + committer + commit-reviewer runbook) so a promoter running normalize-backlog.js knows to split proactively instead of discovering the O1 backlog-split SPLIT verdict. | build | done:2026-08-02 (build) — commits ad2a84c+f0019550. Two-commit normalizer archive-sweep runbook pointer added to coordination README (runbook itself already in SKILL.md/committer.md). No gate carve-out. source:review-defer · promoted-from:.local/coordinator/tasks/normalizer-archive-companion-tension.json · studied:2026-07-07 · operator-decision:2026-08-02 (solution-brief replan chose option (b) documented two-commit runbook; option (a) gate carve-out is NOT adopted — only on measured recurring friction). Design tension: normalize-backlog.js performs a quarter-boundary archive sweep that intrinsically spans docs/planning/backlog.md (removes old done/cancelled rows) + docs/planning/archive/backlog-archive-<quarter>.md (verbatim row additions) + docs/planning/archive/index.md (count reconciliation), BUT the commit-gate O1 backlog-split guard (commit-gate.sh backlog_must_commit_separately, codified in internal/cli/commit_gate_preflight_test.go) rejects ANY acquire mixing docs/planning/backlog.md with any other path — no archive-companion carve-out. A normalizer sweep therefore CANNOT land as one atomic commit. The safe existing path is the MANDATORY TWO-COMMIT landing: (commit 1) docs/planning/backlog.md alone; (commit 2) docs/planning/archive/** companion (transient count-line inconsistency between them, self-healed by the second commit). This row ships the runbook that prescribes the split up front. NON-GOALS: do NOT add a gate carve-out (option (a)) unless measured recurring friction warrants it; do NOT weaken the W1 conflict-prevention policy the split enforces (the split is what keeps a concurrent backlog edit from cas_conflicting a code commit); do NOT change the normalizer itself (quarter-boundary filing is correct behavior). Files: templates/core/.opencode/skills/backlog/SKILL.md (runbook section: 'Normalizer archive sweep = two commits'), templates/core/.opencode/agents/committer.md (prescribe the split when normalizer output spans backlog.md + archive/**), templates/core/.opencode/agents/commit-reviewer.md (recognize the two-commit normalizer landing as legitimate, not a split verdict on the first commit). Validation plan: (1) backlog SKILL.md documents the mandatory two-commit normalizer landing with the exact commit-1/commit-2 path split; (2) committer.md prescribes the proactive split; (3) commit-reviewer.md recognizes the legitimate two-commit sequence; (4) render/doctor consistency (templates/core touched) via `make update --dry-run`; (5) go test/gofmt/go vet unaffected (docs-only). |  |
| P1-PERM-002 | done | permission | Harden the shared `inspectorAllowIf` / `ALLOW_IF_INSPECTOR_FULL` / `INSPECTOR_FULL` builder for the infra-lifecycle rules (apt-install-ad-hoc, user-group-mutation, ssh-host-key-bypass, scp-upload). | build | done:2026-08-02 (build) — commit 6bb33aab. Per-rule INFRA_LIFECYCLE_INSPECTORS (INSPECTOR_FULL minus `command`); shared INSPECTOR_FULL unchanged (public API preserved, mirrors git-lane f88d29f); chain-guard 7→11-op; 20-case LiveBridge matrix. Behavioral-closure proven. Full go test ./... green. Follow-up validated during the `f88d29f` commit (P1-PERM-001). Same gap class as the git-lane fix: (1) the shared inspector set still contains the `command` executor builtin, so `command apt-get install x` / `command usermod ...` / `command ssh ...` / `command scp ...` would carve the rule out; (2) it carries the narrower L104 chain-guard missing `\n`/`\r`/`<(`/`>(`. VALIDATED as a real reachable FP (not theoretical): during the `f88d29f` commit, `user-group-mutation` false-positive'd on the literal `usermod` token in commit-message prose (committer worked around it byte-identically via adjacent-string concatenation). Fix shape mirrors the git lane: per-rule inspector sets excluding `command`, upgrade the shared chain-guard to the full 11-op set, with its own LiveBridge regression matrix for apt/usermod/ssh/scp. Open question: `command` exclusion may need to be per-rule because the shared set serves non-mutation rules too — confirm whether `command` can stay shared or needs per-rule narrowing. |  |
| P1-CONFIG-001 | done | config | Wire coordination-hints-lib.js to read product_prefixes from .vh-agent-harness/project.config.json (remove the phase-later TODO). | build | done:2026-08-02 (build) — commit 2f756f6. PREMISE REFUTED: project.config.json is contract-closed (4-field set, Go-test-enforced); fix uses dedicated .vh-agent-harness/product-prefixes.json (load+parse+null-on-absent, loadComplexityPolicy precedent). Monorepo default fallback; templates/core/ stays domain-free. verify-coordination-hints.js green. deferred from project.config consumption |  |
| P1-MEMORY-001 | done | memory,claims,release | Recurrence signature + dedup enforcement (resolves defer-002 S3). | build | done:2026-07-31 (build) — Recurrence-signature + dedup-enforcement feature COMPLETE (6 staged slices, spec memo efa53fb). Two-level identity: explicit `recurrence_id` (exact-defect key) + versioned `symptom_class_id` (symptom class). Synchronous producer dedup (N reports w/ one recurrence_id → 1 canonical defer entry carrying N observations; no spawned cards). Doctor advisory diagnostics added (#21 `recurrence-state`). Release-gate fail-closed enforcement via doctor #12 on unacknowledged recurrence (`count > last_acknowledged_count`); manifest ack-pair forward-compat. Commits: 7c7c295 (Slice 1 contract) / 86ca29b (Slice 2 derivation) / 1eca045 (Slice 3 producer-dedup) / cacce766 (Slice 4 doctor) / 5056596 (Slice 5 release-gate) / 50c10e3 (Slice 6a migration tests) / 95c51f2 (Slice 6b docs). Dedup crux PROVEN at every layer (write path Slice 3, release-gate Slice 5). Backward-compat preserved throughout (legacy cards → effective identity=task_id; v1 manifest entries without ack fields pass; no auto-hash). Verification: go test/gofmt/go vet green; normalize-backlog.js --check exit 0; verify-task-card-schema.py pass. Open follow-ups (tracked, not done): defer-018 (gate-wire Python validator into CI — jsonschema dep wrinkle), defer-recurrence-toctou-race (concurrency integrity, bounded by release ceremony), defer-recurrence-ack-min-multimatch-test, defer-recurrence-doctor-rundoctor-integration-test. | efa53fb |
| P2-SUBSTRATE-002 | done | substrate | Wire the existing `verify-task-card-schema.py` validator into a gate (`make test` / CI) so task-card schema violations are caught automatically, not only on manual invocation. | build | done:2026-07-31 (build) — Resolved by ARCHITECTURAL CORRECTION, not the original wire-in framing: the Python `verify-task-card-schema.py` (jsonschema Draft7Validator + `pip install jsonschema` dep) was a standalone-Python outlier in a Go repo's gate. PORTED to a Go binary subcommand `vh-agent-harness task-card validate [<file>]` (noun-verb, mirrors `recurrence dedup`): pure logic in new `internal/taskcard/` (hand-rolled bounded draft-07 subset engine — type/enum/const/required/properties/additionalProperties/pattern/minimum/minLength/minItems/format(date-time)/items/allOf/anyOf/if-then-else — NO new Go dep) + cross-field ack-pair guard `recurrence_count >= last_acknowledged_count`; CLI bridge `internal/cli/taskcard.go`. Schema sourced from embedded `corpus.CoreFS` (drift-free). Gate = a Go test (`internal/taskcard/validator_test.go`) that ports all 20 Python fixtures (6 ok / 13 schema-reject / 1 ack-reject) + 7 extras (additionalProperties at 3 levels, nested evidence/aliases shape) → runs in `go test ./...` with NO Python, NO pip. Python files DELETED (source + rendered mirror); live refs updated (diagnostics.go comment, this row); `internal/memory/recurrence.MalformedBlock` RETAINED (doctor's runtime advisory subset, complementary). Verification: go test/gofmt/go vet green; normalize-backlog.js --check exit 0; `make update` (templates/core Python deletion) re-rendered clean. Resolution path chosen over the wire-in framing because a pip dep must not live in a Go repo's gate. |  |
| P1-DEFER-LIVENESS-001 | done | release,safety-gate,defer-liveness | Release-boundary defer-liveness gate (F4-C): doctor #12 release-diff-recurrence predicate + release-prep manifest enumerator; refuses the tag while any open defer with a fired `path_touched` target in `PRIOR_TAG..HEAD` lacks a disposition. | build | source:case-study-§4.3 · memo:researches/decisions/2026-07-30-defer-liveness-release-gate.md · crosswalk:69e0104→09fe9eb · studied:2026-07-30. DONE 2026-07-30 (commit 09fe9eb, recorded by memo 5dd2c3c5). F1-D1: 16 `draft` DEFER cards had fired `path_touched` targets in `v0.18.0..HEAD` and would ship without a forced verdict. Two contradiction classes MERGE'd into doctor #12 (released-claim `69e0104` + release-diff-recurrence `09fe9eb`); architecture (iii) BOTH — doctor-live = load-bearing tag refusal via G0c; manifest = committed CI-verifiable disposition record, now mechanically populated via the `--mode=release-prep` enumerator. Changed files: internal/cli/release_gate.go, internal/memory/claims/claim.go, .opencode/scripts/check-defer-triggers.js (+ template source templates/core/.opencode/scripts/check-defer-triggers.js), scripts/release-tag.sh, internal/cli/release_gate_test.go. Verification: `go test ./internal/cli/ -run TestDeferRecurrence` (9 tests, refuse→clear PROVEN across 3 disposition surfaces [closed status / manifest entry / operator override] + silencer immunity [.md card blocks]); `go test ./...`, `go vet`, `gofmt`, `vh-agent-harness doctor` HEALTHY; commit 09fe9eb. **DORMANCY-NOTE:** Implemented; recurrence refusal becomes active when an about-to-release note exists. Ordinary `doctor` runs remain advisory by design. Open v1 boundaries (fog cards bypass; glob/dir triggers COLD) captured by spawned DEFER cards. | 09fe9eb |
| P1-GATE-003 | done | gate | S1 (review-time) commit-scope integrity — forward `head_at_acquire` through the committer→commit-reviewer handoff and repoint the leaf review diff off bare `HEAD` onto the acquire-time anchor; a concurrent committer moving HEAD can no longer pull an unrequested dirty/concurrent file into reviewed scope. | build | source:decision-memo · memo:researches/decisions/2026-07-27-commit-scope-integrity.md (d87e6f7d) · studied:2026-07-28. DONE 2026-07-28 (ses commit-scope-integrity-reframe, commit 4bc821e, co-landed with S2=P1-GATE-004). Touch sites: committer.md step 3 forwards head_at_acquire verbatim from acquire output JSON; commit-review.md passes the anchor; commit-reviewer.md + commit-reviewer-{a,b,c,d}.md consume it and repoint the leaf diff to `git diff <head_at_acquire> <tree_hash>`. Invariant: unrequested dirty/concurrent file never enters reviewed scope. Test: TestCommitGate_ReviewDiffBase PASS (crux subtests: acquire_emits_head_at_acquire_anchor, reviewed_tree_excludes_unrequested_dirty_file, acquire_anchor_excludes_concurrent_phantom_files). go test ./... fully green. Builds on P1-GATE-002 (which added head_at_acquire RECORDING). | 4bc821e |
| P1-GATE-004 | done | gate | S2 (commit-time, fail-closed) commit-scope integrity — when the CAS commit path would commit a merged tree ≠ the reviewed tree (e.g. a disjoint concurrent commit lands mid-review), REFUSE with status `rebased_refused` (reason `reviewed_tree_diverged`) and require re-acquire, instead of silently substituting the merged tree. | build | source:decision-memo · memo:researches/decisions/2026-07-27-commit-scope-integrity.md (d87e6f7d) · studied:2026-07-28. DONE 2026-07-28 (ses commit-scope-integrity-reframe, commit 4bc821e, co-landed with S1=P1-GATE-003). Touch sites: commit-gate.sh adds the rebased_refused refuse path (after write-tree, before commit-tree) + closeout-ledger logging of the terminal refusal; committer.md step 4 routes rebased_refused/could_not_land to re-acquire. Invariant: committed_tree==reviewed_tree OR refusal. Test: TestCommitGate_CloseoutLedger PASS (crux subtest: rebased_refused_on_disjoint_concurrent_commit + ledger-persistence subtests). Dogfooded live: this commit was itself refused 3× by S2 (disjoint concurrent commits 03ca41d/ec34c6b/5f3fb17 each landed mid-review) before landing on the 4th acquire — committed_tree==reviewed_tree held end-to-end. go test ./... fully green. Builds on P1-GATE-002 closeout ledger. | 4bc821e |
| P1-GATE-002 | done | gate,doctor | P0-B (HEAD-progress / `no_head_progress` / `could_not_land` commit-failure surfacing) — adopted-gate-shaped in disposition §4.2 but NEVER STARTED (F1-class untracked adoption). Three sub-items, gate/doctor ACT + coordinator reads (§5). |  | source:adopted-disposition · disposition:researches/decisions/2026-07-23-vh-solara-orchestration-field-report-disposition.md §4.2 (the 2026-07-26 addendum item 1) · rc:RC3 · studied:2026-07-26. The disposition is the ONLY place P0-B's corrective machinery is named: the NEW signal vocab (`no_head_progress`/`could_not_land`/`could_not_progress`) is 0 matches in code; the per-session `head_at_acquire` (commit-gate.sh:698-952) already exists as the prerequisite sub-item 1 extends. Three sub-items: (1) committer closeout records post-commit HEAD alongside the existing per-session `head_at_acquire`; (2) `doctor` synchronous HEAD-staleness check WARNing when N successful committer closeouts precede an unchanged HEAD; (3) extend commit-gate status vocabulary with `no_head_progress` / `could_not_land`. Files: `.opencode/scripts/commit-gate.sh`, `internal/cli/doctor.go`, templates/core closeout/contract templates. Validation: new status emitted on a no-progress acquire→commit; doctor WARN on synthetic N-closeouts-unchanged-HEAD; go test + commit-reviewer on the slice. Near-term mitigation for the ~6h commit-freeze symptom (Pattern 4 same-file tangle); does NOT address root cause (P1-A worktree/G1/lease, long-term). NOT the unrelated visual-parity "P0-B" in researches/sources/2026-07-24-tree2-rewrite-visual-parity-addendum.md (different namespace). DONE 2026-07-26 (ses p1-gate-002-p0b): 3 slices landed via gated-commit — A=`a4afe6e8` (durable closeout ledger `.git/commit-gate/closeouts.log` JSON-lines, flock-serialized append+trim, count-cap GC; post-commit HEAD recorded at both commit-success sites), B=`6f524bdb` (`no_head_progress`/`could_not_land` status vocab; merge-fail sites emit `could_not_land`, gate now emits ZERO `cas_conflict`; README status-vocab bullet), C=`5495a9ee` (doctor check #19 `head-progress`: WARN on N=3 `post_commit_head` flatline across successful closeouts, env `COMMIT_GATE_STALE_HEAD_THRESHOLD`; SKIP greenfield; never FAIL). Files: templates/core/.opencode/scripts/commit-gate.sh (+rendered .opencode pair), internal/cli/commit_gate_closeout_ledger_test.go (new), internal/cli/doctor_head_progress.go (new), internal/cli/doctor_head_progress_test.go (new), internal/cli/doctor.go, README.agent.md. Verified: `go test ./...` + `gofmt` + `go vet` clean; behavioral-closure PROVEN (crux #1 `TestCommitGate_CloseoutLedger/could_not_land_on_merge_conflict` — gate emits `could_not_land` + ledger record carries `post_commit_head`==unchanged HEAD; crux #2 `TestDoctorHeadProgress_Integration` — flatline→WARN section, greenfield→SKIP section); doctor HEALTHY with managed-drift+dev-stale-embed PASS (rendered byte-equals templates/core via transitivity). WARN-only authority per §5 (doctor INFORMs, coordinator reads; never exit-code change). Cheap surfacing mitigation — does NOT fix root cause (P1-A worktree/lease; follow-up card filed). 3 review DEFERs filed as follow-up cards (cas_conflict doc-drift; no_head_progress status-logic; crux could_not_land post_commit_head assertion). |  |
| P1-LINEAGE-002 | done | lineage,render,seam,overlay | Detect & report/prune rendered `.opencode/skills/<name>/` dirs whose overlay source was removed. | build | 2026-07-15 REOPENED-then-reverified (v1.1, build/lineage-orphans): v1 commit 136c79f was prematurely marked done; /ship-review returned needs-changes with 3 blockers (#1 manifest provenance untruthful under live-write failure — `Apply` has no write-error signal, so the rendered-outputs manifest persisted even when a tracked overlay-skill dest failed its live write; #2 directory-level cleanup guidance risked removing active sibling files in a mixed dir; #3 boolean source checker false-positive'd a transient/unreadable source as a definite orphan). FIXED in v1.1 commit 0047142 (same report-only scope, NO prune): #1 additive typed `substrate.FileOutcome.WriteState` (not_attempted/succeeded/failed) + `seamApply` manifest-skip gate on failed tracked overlay-skill dests (prior manifest byte-intact, stderr warning; non-skill failures do NOT gate; `substrate.Apply` return semantics UNCHANGED — broader truthfulness deferred to P1-SUBSTRATE-001); #2 file-level reporting (`DestinationPath`) + file-level removal advice in README/migration; #3 tri-state `CheckSource` present/missing/indeterminate (only `fs.ErrNotExist`=missing; transient/permission=indeterminate→warn+skip; incl. a direct project-local-pack probe so `OpenPackFor`'s stat-error swallow cannot false-positive as missing). `internal/lineage/lineage.go` untouched (S1-only invariant). Self-verified `go test ./...`/gofmt/go vet green; commit-reviewer APPROVED 4/4; awaiting coordinator `/ship-review` re-review. --- ORIGINAL premature-done v1 note (superseded by v1.1, kept for audit): report-only orphan detection shipped — NEW `internal/renderstate/` pkg + `.vh-agent-harness/rendered-outputs.json` manifest (separate from lineage.yml); seam loads prior manifest + Compare (dry-run+live) attaching "Preserved orphan skill(s)" notices to `update`/`update --dry-run`; definite orphan = source MISSING (not deselected); pack-deselection + project-added dirs NOT flagged; NO deletion/prune in v1. Tests: renderstate unit suite (incl. non-normalized-prior key hardening) + 8 seam lifecycle tests. README.agent.md + `templates/migrations/v0.10.0.md` (credits TrueAI; supersedes misleading v0.9.0 rollback line on re-render removing skills). Code commit 136c79f. go test/gofmt/go vet green. Originally 2026-07-14 reported by TrueAI (vh-agent-harness v0.9.0). Bug: removing an overlay skill source (`.vh-agent-harness/overlays/<pack>/skills/<name>/`) then re-running `vh-agent-harness update` leaves the previously-rendered `.opencode/skills/<name>/` live, unreported, and invisible to `update --dry-run`. Orphaned rendered skills stay auto-discoverable (SKILL.md auto-loaded), drift from canonical sources, and can contradict newly-adopted core skills (reporter's case: removed overlay `tdd`/`diagnosing-bugs` left rendered copies contradicting new core `tdd-loop`/`debugging-loop`); required manual `rm -rf` + commit to clear. ROOT CAUSE (confirmed): `.vh-agent-harness/lineage.yml` records only template/copier/render metadata (no per-render file/dir manifest); seam-apply (`internal/cli/seam.go` seamApply@126, renderSeamStaging@~384, seamClassifierWithOverlays@642) builds `overlayFiles` from LIVE overlay paths only, so a removed source simply drops out of `overlayFiles` → never classified → left untouched (renderer's deliberate conservative non-deleting posture). DESIGN QUESTION (blocks build): (a) WHERE to persist a rendered-outputs manifest — `lineage.yml` as bare paths (COLLIDES with the S1-only invariant: `internal/lineage/lineage.go` `AssertLineageOnly` + `forbiddenTopLevel` reject S2/S3 keys) vs a separate manifest file vs `substrate.ApplyReport`; (b) report-only orphan list under `--dry-run` vs explicit `--prune`/`prune` command vs auto-prune; (c) provenance mechanism to distinguish a true orphan (rendered from a now-gone overlay/core source) from an operator-added project skill dir, since `.opencode/skills/<name>/` is also a legitimate project-owned location — required to avoid deleting operator content. NEXT STEP: `/solution-brief` on the design question. Precedent: P1-SEC-001 (TrueAI commit-gate bypass) — same reporter-credit + migration-note pattern applies (note credited in `templates/migrations/v0.4.2.md`). NON-NEGOTIABLES for the eventual fix: README.agent.md MUST update in the SAME change for any new flag/orphan-reporting surface; ownership is the safety contract (preview with `--dry-run`); `templates/core` must stay domain-free; `go test ./...`, `gofmt`, `go vet` must pass; the renderer's conservative non-deleting posture is deliberate; a migration note is needed under `templates/migrations/` for the release carrying the fix (reporter on v0.9.0). Adjacent open rows: P2-LINEAGE-001, P2-DOCTOR-001. |  |
| P2-SKILLS-004 | done | skills | Make harness `AGENTS.md` testing section localizable (dogfood finding). | skill-craft-import | 2026-07-15 done. Committed f1fcaa7. Stripped prescriptive pytest/tests{unit,integration,e2e}/ testing boilerplate from AGENTS.core.md; replaced with universal testing discipline + S1 seam-map authority + absence-contract pointer. Decision memo: researches/decisions/2026-07-14-skill-s2-promotion-gate.md. | templates/core/.vh-agent-harness/AGENTS.core.md, commit f1fcaa7 |
| P1-DIST-001 | done | overlay | Promote auto-classifier-pilot from dogfood-local to EMBEDDED in templates/overlays/ — consumers select by name out-of-the-box; no new CLI/distribution surface. | build | 2026-07-14. Reported by TrueAI: selecting the pack by name failed "open .: file does not exist" and blocked the whole render (pack body was not distributed). DONE: committed ecc06ad (25 files: 7 renames + 18 modified). Embed needs zero new code infra (release precedent; KnownPacks/OpenPack auto-discover templates/overlays/ dirs). Research ses_0a2f2a92 confirmed; planner ses_0a2eaf36 landed brief. commit-review APPROVE (unanimous 4-leaf, high confidence). D2/W9 whole-render-block on missing pack left as-is (intentional invariant). |  |
| P2-SKILLS-001 | done | skills | Promote tdd-loop core skill (red→green→refactor in-loop, seam localization, S1 absence-contract) | skill-craft-import | 2026-07-14 done. Committed f56b964 (SKILL.md +84, references/seam-localization.md +81). S2 gate satisfied via TrueAI overlay pilot (validated in-loop refactor + seam discipline). Design decisions: D1A in-loop refactor (canonical Beck TDD, overturns mattpocock in-review stance); D3 localization authority-honesty + AGENTS.md co-localization. Decision memo: researches/decisions/2026-07-14-skill-s2-promotion-gate.md. | commit f56b964 |
| P2-SKILLS-002 | done | skills | Promote debugging-loop core skill (build-red-first + reproduce→minimise→hypothesise→instrument→fix→regress + D2C downgrade/handoff escape) | skill-craft-import | 2026-07-14 done. Committed c52268a (SKILL.md +110, references/red-signal-recipes.md +66, references/downgrade-protocol.md +90). S2 satisfied via VH-Solara forward pilot — D2C escape validated against their Firefox/WebRender GPU/thermal heat saga (mask-image gradient defect under real GPU load); the MEDIUM-conf-on-wording hold resolved when the pilot returned positive signal on the specific uncertain design. All FIVE guardrails fire (human-observation-never-promoted-to-deterministic-red; do-not-continue-theorizing-after-downgrade; diagnostics-export packages handoff; bgshell-job only for non-GPU probes; END loop after handoff). The `human-observed \| non-deterministic \| not agent-runnable` triple is the label TAXONOMY the five guardrails operate on — NOT a sixth guardrail (use FIVE as the count). Statistical-sample serial ×50 red correctly stayed in-loop and was NOT downgraded (downgrade-protocol.md:46-49). S2 core discipline previously satisfied via TrueAI pilot (step-0 absence-contract + step-7 post-mortem). Decision memo: researches/decisions/2026-07-14-skill-s2-promotion-gate.md. Non-blocking follow-up: P2-SKILLS-003 (red-signal-recipes.md fast-property vs slow aggregate reds). | commit c52268a |
| P2-SKILLS-003 | done | skills | Clarify `red-signal-recipes.md` "fast" property vs legitimate slow predeclared-aggregate reds. | skill-craft-import | 2026-07-14 done. Committed c5e9cec. The pre-specified carve from the VH-Solara forward pilot landed: blessed the predeclared-aggregate red shape (predeclared threshold + reproducible count + no cheaper seam + bgshell-hosted) into the "fast" property + "slow" anti-pattern so a validated slow aggregate red is not falsely flagged. SKILL.md guardrails + `downgrade-protocol.md:46-49` unchanged (the statistical-sample red correctly stays in-loop today). Consistent — the carve structurally excludes the flaky-race downgrade shape. Decision memo: researches/decisions/2026-07-14-skill-s2-promotion-gate.md. | templates/core/.opencode/skills/debugging-loop/references/red-signal-recipes.md, commit c5e9cec |
| P1-CORE-001 | done | schema | Make an absent/empty `modules:` survive `vh-agent-harness update`: add `omitempty` to the 4 append-only array fields in `harnessProfileData` so reconciled output drops empty arrays instead of re-emitting `[]`; convert the noop-only intent test to a real Apply-path assertion; drop the deprecated `modules:` block from the examples template. | modules-reconcile-readd | 2026-07-13 DONE. Bug fix: profile reconcile marshaler re-emitted empty arrays as `[]` on every non-noop reconcile, silently defeating the documented `modules:` deprecation (uncompletable + undetectable since the warning is silent-on-empty by design). FILES (3): (1) `internal/schema/harness_profile.go` (M) — added `,omitempty` to the four append-only array field tags (Modules/Overlays/PolicyPacks/Capabilities) in `harnessProfileData` + a doc comment on the struct explaining the omitempty rationale (deprecation survival; silent-on-empty warning). Profile (scalar) + Features (map) untouched. (2) `internal/schema/harness_profile_test.go` (M) — converted `TestHarnessProfileReconcile_CapabilitiesAbsentStaysAbsent` from a noop-fast-path-only test (byte-identical inputs that hit `bytesEqualish` and never asserted on merged bytes) into a REAL Apply-path assertion (project `profile: coordination` vs platform-default `profile: minimal`) expecting OutcomeApply + asserting merged bytes contain NO `modules:`/`capabilities:`/`overlays:`/`policy_packs:` line; ADDED `TestHarnessProfileReconcile_AbsentModulesNotReadded` focused regression (project omits `modules:` + diverges on `profile` ⇒ no `modules:` in merged). (3) `templates/examples/.vh-agent-harness/vh-harness-profile.yml` (M) — removed the deprecated `modules:` block (and `# modules:` comment) so the `vh-agent-harness example` surface stops advertising a deprecated field. DESIGN: omitempty drops empty arrays in the reconciled MERGED output only; non-empty arrays still emit normally (backward compat); `modules` remains in `harnessProfileAllowedTopLevel` so consumers who set it explicitly are still accepted. Corpus platform-default (`templates/core/.../vh-harness-profile.yml`) deliberately unchanged — it is the INPUT to Reconcile written verbatim by greenfield seeding, not round-tripped. README.agent.md unchanged — its field-contract section (L93-107) documents `modules:` deprecated + warns-on-non-empty + backward-compat parsed, all still accurate. VERIFY: `gofmt -l .` empty; `go vet ./...` clean; `go test ./...` all pass (schema + cli + substrate + integration); commit-review APPROVED (4/4 free-tier leaves, confidence high, risk low, 0 blocking, 1 non-blocking DEFER on `templates/migrations/v0.2.0.md:41` watch-out #3 staleness — historical docs debt to supersede in the next release's own migration note). LANDED in commit 98736eab. Release required for v0.8.0 consumers to receive this. |  |
| P1-SEC-002 | done | security | SECDATA-1 — Close the `_session_meta_path` traversal-`--uuid` class in commit-gate.sh: arbitrary-file-deletion (release no-lock `rm -f "$session_meta"`) AND arbitrary-file-overwrite (heartbeat lock-free python3 read+write `'w'` block). Centralized uuid-shape guard `^[A-Za-z0-9_-]+$` at the single path-construction chokepoint (reuses the charset established at `_cleanup_own_scratch` ~L245 and `cmd_stage_message` ~L1606); non-conforming uuid echoes `""`+`return 0` (NOT 1 — `var="$(...)"` assignment sites run under `set -euo pipefail`, so a non-zero return would kill the shell; empty string is the reject signal) so every downstream sink (rm/cat/write) is a safe no-op. | build | source:review-defer · trigger:path_touched(.opencode/scripts/commit-gate.sh) · studied:2026-07-07 · promoted_from:secdata-1-session-meta-path-traversal. 2026-07-07 DONE. Pre-existing finding surfaced during the commit-gate message-cleanup arc (commits 44ac429/049d186/7956538) but deferred because that slice's scope was `_cleanup_own_scratch` msg- sinks only; trigger fired when `.opencode/scripts/commit-gate.sh` was touched. VULN: `_session_meta_path()` interpolated a raw caller-controlled uuid into `${GATE_INDEX_DIR}/meta-${1}` with NO shape validation; the result feeds `rm -f` (deletion class — release's no-lock branch forwards `--uuid` verbatim), `cat` (read class — cmd_commit), and a python3 read+write `'w'` block (overwrite class — cmd_heartbeat lock-free branch). Confirmed live: `release --uuid 'x/../../config'` deletes `.git/config` when `.git/commit-gate/meta-x/` exists as a dir (the `..` segments then resolve out of `commit-gate/`). FIX (decision: centralized guard inside `_session_meta_path`, chosen over the per-sink `_safe_meta_reclaim_path` alternative — single construction point protects all 14 call sites + future uniformly; internal `_uuid` from gen-uuid is `[0-9a-f-]+`, a strict charset subset, so legit acquire→commit→release never trips). Call-site survey: caller-rm deletion (release no-lock, acquire stale-session L409), caller-write overwrite (heartbeat lock-free L1277), caller-read (commit L855), internal-uuid writes/rms (L702/L722/L732/L745/L762/L775/L804/L967/L992/L1070 — all gen-uuid or lock-meta-sourced, never caller-controlled). FILES (3): templates/core/.opencode/scripts/commit-gate.sh (source — guard inside `_session_meta_path` ~L99-106 + SECURITY comment block ~L83-98 documenting return-0-vs-1 + coverage), .opencode/scripts/commit-gate.sh (rendered, byte-identical), internal/cli/commit_gate_msgcleanup_test.go (2 new subtests: `release_rejects_traversal_meta_uuid` deletion class, `heartbeat_rejects_traversal_meta_uuid` overwrite class with `bytes.Equal` victim). LANDED in commit 5cbdc1a. VERIFY: gofmt -l . empty; go vet ./... clean; go test ./... all pass; 10/10 TestCommitGate_MessageCleanup subtests pass; TEETH-PROVEN by bypassing the guard (`if false`) and confirming BOTH new subtests fail with the exact SECDATA-1 assertion (release: `.git/config` deleted; heartbeat: victim JSON overwritten with injected heartbeat_at) then restoring via `make update`; template==rendered byte-identical (dogfood: `update --dry-run` → 0 managed-overwrite, 142 managed-unchanged); commit-review APPROVED (4 unanimous leaves, high confidence, low risk, 0 blocking). README.agent.md NOT changed — `_session_meta_path` is an internal helper with no agent-facing CLI/configurable-file-set/ownership/runtime-exec-contract change. |  |
| P1-SEC-001 | done | security | F1 — Close the `vh-agent-harness exec git <global-flag> <mutation>` commit-gate bypass (A1 Go binary backstop + A2 JS source-of-truth parser fix in shell-guard-core.js harness branch). Wraps `walkGitGlobals` + reuses `permconfig.GitMutationVerbsSet`; no regex, no local verb copy. | build | 2026-07-07 DONE. F1 closed at both layers via command parsing (not regex). A1 (Go): denyExecGitMutationPayload in runExec (internal/cli/exec_git_guard.go, NEW) reuses permconfig.GitMutationVerbsSet via new exported execro.GitVerbPastGlobals (internal/execro/classifier.go); git-mutation-scoped only — does NOT over-deny exec pytest/mkdir/bash. A2 (JS source of truth): detectWrappedGitMutation in shell-guard-core.js reuses walkGitGlobals to inspect exec/exec-ro git payloads; closes both the opencode-plugin path AND the Go evaluateGate→node eval.js bridge. All four declared bypass vectors (--no-pager/-C/--git-dir, incl. space-separated --git-dir) pinned by named tests at both layers. LANDED in commit 886a17e (10 files): templates/core/.opencode/plugins/shell-guard-core.js, .opencode/plugins/shell-guard-core.js (rendered), internal/cli/exec_git_guard.go, internal/cli/exec_git_guard_test.go, internal/cli/exec_shell.go, internal/execro/classifier.go, internal/cli/exec_gate_test.go, internal/permission/shellguard_hook_test.go, templates/migrations/v0.4.2.md (credit TrueAI), .vh-agent-harness/lineage.yml. PENDING: README.agent.md F1 bullet — commit-gate cannot do hunk-level staging (concurrent exec-sandbox stream owns other README hunks); deferred to v0.4.2 release slice. VERIFY: go test ./... green, go vet clean, gofmt clean, commit-review APPROVED (4 unanimous leaves, approve-with-nits). v0.4.2 release is a follow-up slice. Reported by TrueAI (responsible disclosure, root-cause analysis). |  |
| P1-DOCS-003 | done | docs | Document @opencode-ai/plugin auto-injection interaction + plan-state.js runtime dep (decision B3, gated on A). | build | 2026-07-05 DONE. Slice B-doc (decision B3) of the C→A→B maintainer fix. Gated-on A landed (P1-DRIFT-001, f54aa75); F3 refinement in 797ed77. FILES (1): `docs/ai/plugin-auto-injection.md` (NEW) — standalone durable note covering: (1) the runtime dep — `tools/plan-state.js` opens with `import { tool } from "@opencode-ai/plugin";` and `tool`/`tool.schema` build the entire `planStateTool` surface (NOT type-only); (2) why `.opencode/package.json` does NOT pin it — opencode auto-injects `@opencode-ai/plugin@<ver>` into its cache node_modules at startup (log signature `opencode add @opencode-ai/plugin@... --exact`), already satisfying the import; a template pin would be redundant + risk version-conflict; (3) load-bearing maintenance warning — do NOT strip the import or add the package to package.json; (4) corrects the retracted "type-only/cosmetic" mischaracterization (runtime ESM, ~49KB, 4 runtime deps: zod/effect/@ai-sdk/provider/@opencode-ai/sdk); (5) brief cross-ref to the related template-vs-emitter `edit:deny` seam (commit-gate.sh stage-message docstring, 8875f39) as a known doc seam, out of scope. VERIFY (source facts, not memory): plan-state.js L1 `import { tool } from "@opencode-ai/plugin";` confirmed + L83 `export const planStateTool = tool({...})` + `tool.schema` arg-typing throughout; package.json pins only tree-sitter-bash@0.25.0 + web-tree-sitter@0.25.10, NO @opencode-ai/plugin. Go gates no-op pass (docs-only slice, no Go touched): `go test ./...` clean. House style matched to docs/ai/template-authoring.md. Docs-only — NO commit, NO template render, NO `vh-agent-harness update`. | B3 |
| P1-DRIFT-001 | done | drift | A2 — add non-failing `preserved`/informational outcome to checkManagedDrift; consult readOwnershipOverrides in doctor.go + preflight.go call sites (decision A2). | build | 2026-07-05 DONE (Slice A of the C→A→B maintainer fix). Made `checkManagedDrift` ownership-override-aware: reads `readOwnershipOverrides(target)` + `ownership.Resolve(defaults, overrides)` and filters the byte-compare on the EFFECTIVE class (mirrors seamInventory/computeSeamDrift). A path raised above platform_managed (e.g. project_owned via harness-ownership.yml) is no longer counted as drifted; a present+divergent raised path surfaces as a non-failing `preserved` signal on a new `tierInfo` tier. Absent override file => nil overrides => behavior byte-identical to before (no-op for the common case). Added `tierInfo = "INFO"` constant; doctor's `applyTier` needed no change (default no-op => no problem, no warn, HEALTHY preserved). preflight counts `tierInfo` as passed for summary arithmetic + never blocks (failed stays 0 => exit 0). Outcome detail: `"%d managed file(s) in sync; %d project-preserved (ownership override)"`. Closes the catch-22: update (substrate.Apply ActionProjectPreserved) already preserves owned divergences; doctor now agrees instead of FAILing forever. FILES (3): (1) `internal/cli/doctor.go` (M) — `checkManagedDrift` override-aware + `preserved` counter + `tierInfo` outcome + two-layer error handling (readOwnershipOverrides / ownership.Resolve). (2) `internal/cli/preflight.go` (M) — `tierInfo` constant (documented) + summary count includes INFO as passed. (3) `internal/cli/doctor_drift_test.go` (NEW, 6 tests): no-override PASS baseline; no-override divergent still FAILs (regression guard); override→project_owned divergent => INFO/preserved (the fix); override→project_owned missing => silent (not preserved, not missing — distinct condition); invalid override class => FAIL clean; preflight entry path => PASS non-blocking with INFO row surfaced. VERIFIED: `gofmt -l .` empty; `go vet ./...` clean; `go test ./...` all pass (incl. existing TestSeamDoctor_UnhealthyOnManagedDrift regression). Smoke: `vh-agent-harness doctor` runs clean on the dogfood repo (no crash; format intact). README.agent.md NOT changed (only mentions managed-drift as a high-level check name at L159; does not document the tier vocabulary or output format). SCOPE: only `checkManagedDrift`; `computeSeamDrift`/`seamDriftReport` (seam_drift.go) left alone (already override-aware + correct). | A2 |
| P1-DRIFT-002 | done | drift | F3 fix — narrow `checkManagedDrift` preserved branch to genuine override-raises only (`Origin == OriginOverrideRaise`), so default-class non-managed files (project_owned seeds, platform_armed profile, external_generated recon) that diverge by design are silently skipped instead of mislabeled "project-preserved (ownership override)". Fixes the F3 release-blocker over-broadening introduced by P1-DRIFT-001 (commit f54aa75, Slice A/A2). | build | 2026-07-05 DONE. A2 intent (surface override-raised divergences as non-failing INFO instead of perpetual FAIL) was correct, but the gate was `effective class != platform_managed` — fired for ANY non-managed file including defaults. Empirical proof on this dogfood repo (NO harness-ownership.yml): doctor emitted `INFO 135 in sync; 8 project-preserved (ownership override)` on every install, mislabeling README.md/Makefile/backlog.md/roadmap.md/forbidden-patterns.project.js/.gitignore/CLAUDE.md/vh-harness-profile.yml. FIX = Option A: gate the preserved-counting branch on `eff[path].Origin == ownership.OriginOverrideRaise` (`internal/ownership/resolve.go:50`, `EffectiveEntry.Origin` resolve.go:68) — only a path genuinely RAISED above its platform default by an override counts as preserved. Default-class non-managed files hit a separate silent-skip branch (pre-A2 behavior restored). The platform_managed drift byte-compare is unchanged. Absent override => every path OriginDefault => preserved=0 => tierPass (byte-identical to pre-A2). FILES (3): (1) `internal/cli/doctor.go` (M) — checkManagedDrift loop replaced `effClass, _ := eff.ClassOf(path)` + `effClass != ClassPlatformManaged` with `entry := eff[path]` + `entry.Origin == OriginOverrideRaise` (preserved) / `entry.Class != ClassPlatformManaged` (silent skip); branch comments + the checkManagedDrift doc comment updated to state the narrowed Origin-based gate. (2) `internal/cli/doctor_drift_test.go` (M) — NEW regression test `TestManagedDrift_DefaultNonManaged_Divergent_NotPreserved` (the exact gap that let F3 slip: a default project_owned/platform_armed file that diverges must NOT increment preserved and must read tierPass "in sync", not tierInfo) + NEW helper `findLiveDefaultNonManagedPath`; existing 6 tests unchanged and passing. (3) `docs/planning/backlog.md` (M) — this row + P1-DRIFT-003 (F4) + P2-PERMCFG-004 (D-F1). VERIFY: `gofmt -l .` empty; `go vet ./...` clean; `go test ./...` all pass (7 drift tests = 6 existing + 1 new); empirical doctor on this repo — F3 INFO row GONE (preserved=0, no "project-preserved" label); `TestManagedDrift_NoOverride_Pass` proves the clean-tree managed-drift line is tierPass "in sync" (the task's PASS acceptance, via the test harness). NOTE: live `doctor` currently reports `FAIL 3 drifted, 1 missing of 136` — this is REAL drift from CONCURRENT `templates/core/` edits (AGENTS.core.md / docs/coordination/README.md / TASK_MODES.md + new untracked PROMOTER_RUNBOOK.md) not yet rendered into the live tree; file-for-file explained and orthogonal to F3 (this slice is Go-source only, no render regen per task constraint; concurrent files left untouched). Not committed — left dirty for coordinator → /commit-review → committer (gated-commit). | F3 |
| P1-DOCS-002 | done | docs | Fix stale stage-message docstring + deprecate stage-message subcommand in templates/core/.opencode/scripts/commit-gate.sh (decision C3, agreed with consumer). | build | 2026-07-05 DONE. Slice C of the C→A→B maintainer fix. FILES (2): (1) `templates/core/.opencode/scripts/commit-gate.sh` — rewrote the wrong `edit:deny` rationale (committer edit perm is the scoped object form `{"*":"deny","tmp/commit-gate-message/**":"allow"}` via internal/permconfig emit, tables.go EditOverrides + CommitGateMessageGlob + emit.go computeEditBlock); documented the Write-tool → `tmp/commit-gate-message/msg-${UUID}` → `acquire`/`commit --message-file` flow as primary (symmetric on both); marked `stage-message` DEPRECATED (stderr notice + usage-banner `(deprecated)` tag); kept function, both dispatch sites (normal + SKIP_COMMIT_GATE), arg parsing, and atomic-write behavior intact. The .tmpl flat-`edit:deny` seam left for a separate slice per task scope. (2) `docs/planning/backlog.md` — this row + A (P1-DRIFT-001) + B (P1-DOCS-003). README.agent.md NOT changed: it does not mention `stage-message`/`--message-file`/heredoc/`edit:deny` (single generic `commit-gate` ref at L141, not command surface). VERIFY: `gofmt -l .` empty; `go vet ./...` clean; `go build ./...` + main binary BUILD_OK; `go test ./...` all pass; `./bin/vh-agent-harness diff` = exactly 1 drifted file (`.opencode/scripts/commit-gate.sh`), `update --dry-run` = 1 managed-overwrite / 138 unchanged / 8 preserved / 1 armed-merged (no unexpected drift); functional test confirmed `stage-message` stdout JSON `{"status":"staged",...}` valid + deprecation notice on STDERR only (RC=0). Render NOT applied (diff left for committer per task constraint). | C3 |
| P2-DOGS-002 | done | overlay | Fix prompt defects + coverage gaps surfaced by dogfooding the `harness-release-readiness` agent (Step 1 closeout). Prose-only on the agent source + profile yml housekeeping; NO `internal/` Go production change (render-mechanics test untouched). | build | 2026-07-02 DONE. Step 1 of the capability-installer/release-agent arc closeout (build arc e37d6fc). DOGFOOD caught two prompt defects + three coverage gaps; this slice fixes them. CHANGES (5 source edits to the agent .md + 1 opencode-append.jsonc description-label fix (G1–G5→G0–G5, surfaced by commit-review A-F1) + 1 profile-yml drop): (1) CRITICAL shell-guard-safe evidence commands — `git tag --list 'v*'` and `git describe --tags` were DENIED to the agent (raw `git tag` matches the `git-mutation-bypass` forbidden regex regardless of `--list`; `git describe`/`git for-each-ref` are not in the agent's git_readonly allowlist so they `*:deny`); swapped tag discovery to `git show-ref --tags` (the only shell-guard-safe tag verb in the agent's allowlist); added a per-call/no-bundling note — each git verb its OWN bare call, NEVER `&&`-chain or `bash -c` wrap (defeats per-verb allowlist matching); applied to BOTH INVARIANTS #2 read-only-verb list AND EVIDENCE COMMANDS arc-discovery block. (2) G1↔G2 fallback — G2 now derives the highest-plausible version name when `intended_version` is null (MINOR for a BREAKING arc like the Phase-5 roster flip 20→8, PATCH otherwise) and SURFACES it as a `human_decision` (derive is a hint, human confirms); G1 cross-ref so a null intended_version does not deadlock it. (3) G5/G1 same-artifact note — WHERE the G1 migration note IS the curated consumer note, G5 is SATISFIED by closing G1 (no double-flag). (4) NEW G0 green-tree gate — added before G1; checklist range became G0–G5; schema blockers enum + delegated_owners updated. DEVIATION (advisory): the mission framed G0 as commands the agent runs (`vh-agent-harness exec go test/vet/build/gofmt`) — ALL unrunnable by this read-only agent (wrapper `devSh:deny` + go cmds not readonly); framed G0 instead as a release PREREQUISITE the agent FLAGS for build/operator confirmation (records HEAD via allowed `git rev-parse HEAD`), BLOCKER while unconfirmed, PASS on operator confirm. (5) NEW G0b dirty-tree warning — `git status --short` (allowed), WARNING not blocker, schema warnings enum updated. (6) `.vh-agent-harness/vh-harness-profile.yml` — dropped the `modules: [core]` regression (re-added by the dogfood commit e37d6fc; fired a spurious deprecation warning on every update/doctor); armed reconcile emits canonical `modules: []` (Modules field has no omitempty so it cannot be fully absent — empty list is the durable steady state; warning GONE). callable-graph-snippet.md UNTOUCHED (it only FORBIDS `git tag`/`git push`, recommends no evidence cmd). ROOT CAUSE of the prior failed renders: the installed `~/.local/bin/vh-agent-harness` was v0.1.9 (predates project-overlay rendering); fixed via canonical dogfood loop — `make update` builds `./bin/vh-agent-harness` (harness/0.1.0-dev) THEN renders, picking up project-overlay edits + overlay-perm edges. FILES (12): MODIFIED `.vh-agent-harness/overlays/harness-dogfood/agents/harness-release-readiness.md`, `.vh-agent-harness/overlays/harness-dogfood/opencode-append.jsonc` (G0–G5 description label), `.vh-agent-harness/vh-harness-profile.yml`, `.vh-agent-harness/lineage.yml`, `.opencode/docs/agents/callable-graph.md`, `opencode.jsonc`, `docs/planning/backlog.md`, `docs/planning/archive/backlog-archive-2026-q2.md` (normalizer), `docs/planning/archive/index.md` (normalizer); UNTRACKED (rendered) `.opencode/agents/harness-release-readiness.md`, `.opencode/agents/releaser.md`, `.opencode/sys-scripts/permission-packs/`. Verify: `go test ./...` / `go vet ./...` / `go build ./...` all pass; `gofmt -l .` empty; `./bin/vh-agent-harness doctor` (0.1.0-dev, built via `make update` — PATH-installed v0.1.9 is stale and predates overlay rendering) HEALTHY (0 problems, 0 warnings; the 0-warnings confirms modules deprecation GONE); render-mechanics test `internal/cli/harness_dogfood_render_test.go` unchanged and passing (prose-only edits, as predicted). |  |
| P2-DOGS-003 | done | docs | Author the v0.2.0 migration note (6 consumer-facing changes) + close the G3 docs-coverage gaps (profile/capabilities/modules model + release overlay pack) in `internal/cli/guide.go`, `README.agent.md`, and the harness-operator `SKILL.md` (core source + render). Docs-authoring + one guidance-string edit to `guide.go`; NO production logic change. | build | 2026-07-02 DONE. Step 2 of the capability-installer/release-agent arc closeout (build arc f58a1d2). Closes the harness-release-readiness agent's G3 docs check across all three surfaces. FILES (5): (1) `templates/migrations/v0.2.0.md` (NEW) — minor/BREAKING release note covering all six consumer-facing changes: Phase-5 roster flip 20→8 (BREAKING, `profile: minimal` now baseline-only); `modules:` deprecation (one-line warning on update/doctor); new `capabilities:` union field; `profile:` enum real semantics (minimal/coordination/web→baseline; supervised→[core/gated-commit,core/debate]; unknown→baseline); the `release` embedded overlay pack (`core/release` via capabilities OR `release` via overlays — converge, pulls core/gated-commit via hard-dep); embedded-default modules removal. Carries the canonical heading set (TestMigrationNotes_Canonical) + the self-update→version→update --dry-run→update→doctor sequence; one-line remediation (profile: supervised OR minimal+capabilities:[core/gated-commit,core/debate]) + 3 watch-outs (BREAKING flip; v0.1.9 binaries PREDATE overlay-pack rendering; `modules: []` empty-list after armed reconcile is harmless). (2) `internal/cli/guide.go` (M) — installed-phase nextSteps gains a domain-free step surfacing profile preset / capabilities union / modules deprecation / release pack; token-only (references core/release, core/gated-commit, core/debate which are core catalog capabilities; NO dogfood-local agent refs). (3) `README.agent.md` (M) — added "Shipped overlay packs" subsection documenting the embedded `release` pack (configurable-file-set addition per the non-negotiable rule); the field-contract subsection (P2-RESOLVER-007) already covered profile/capabilities/modules/overlays. (4) `templates/core/.opencode/skills/harness-operator/SKILL.md` (M, CORE source) + (5) `.opencode/skills/harness-operator/SKILL.md` (M, render) — new "Profile and capabilities (the agent roster)" section; CORE source kept domain-free (references the shipped `core/release` capability generically; the dogfood-local `harness-release-readiness` agent is NOT referenced in core); regen via `make update` (build `./bin/vh-agent-harness` from source + update). SKILL.md provenance = CORE-RENDERED (source `templates/core/.../harness-operator/SKILL.md`, `{{PROJECT_NAME}}` token in description). ADVISORY: my first `update` accidentally ran the installed PATH `0.1.9` binary (predates overlay-pack rendering) and reverted Step-1 overlay renders + re-introduced the `modules:[core]` regression — restored via `commit-gate.sh revert` then the correct `make update` (fresh-source binary); materialized the very Watch-out #2 the v0.2.0 note now documents. Verify: `go test ./...` (incl. TestMigrationNotes_Canonical on v0.2.0.md + guide tests — no nextSteps test update needed, additions are append-only), `go vet ./...`, `go build ./...`, `gofmt -l .` empty; `make doctor` HEALTHY 0 warnings. |  |
| P2-RESOLVER-007 | done | resolver | Cleanup/hygiene slice (residual polish after capability-installer + release pack committed): dead-code removal, double-call caching, stale fixtures, grammar/comment fixes, end-to-end + byte-compare test coverage, field-contract docs, deprecation cleanup. Low-risk, NO behavior change to rendered output. | build | 2026-07-01 DONE. (1) `internal/resolver/merge.go` — deleted dead `resolveShadowing` (zero production callers after `ResolveContributions` export); rewrote stale cross-reference doc comment. (2) `internal/cli/profile.go` — cached double `activeOverlays(target)` call in `resolveCapabilityAnswers` into one local; F4 grammar fix in `modulesDeprecationWarning` (pluralize entry/entries for N>1); updated `liveProfileModules` comment (embedded default no longer ships `modules:`). (3) `internal/overlay/overlay_test.go` — F3 stale fixture `knownPackNames` `[]string(nil)`→`["release"]` + comment block. (4) `internal/cli/seam.go` — F5 softened `emitModulesDeprecationWarning` comment to state real fire surface (every renderSeamStaging path). (5) NEW tests: Case-1 end-to-end `TestDiscoverPackContributions_ManifestProjectPackReplacesEmbedded` (project pack WITH manifest replaces embedded; companion to Case-2 no-manifest test) + `TestSeamRender_ReleasePathsByteConverge` (byte-compare opencode.jsonc + agent roster across capabilities vs overlays selection paths). (6) `README.agent.md` — F3 field-contract subsection (profile enum / capabilities union / overlays override / modules deprecated). (7) `templates/overlays/release/agents/releaser.md` — F5 wrapper is project-supplied note. (8) F2 `templates/core/.vh-agent-harness/vh-harness-profile.yml` — removed vestigial `modules: [core]` block (domain-neutral, verified via git diff). Test delta: +2 `func Test` (baseline 472 → 474); item 4 updated 1 existing test case. Verify: `go build ./...`, `go vet ./...`, `gofmt -l .` (empty), `go test ./...` all pass; `vh-agent-harness doctor` HEALTHY. `templates/core/` domain-free invariant held. |  |
| P2-RESOLVER-005 | done | resolver | Capability installer — Phase 5 (capstone): the behavior flip. Replace the Phase-3 `phase3BackwardCompatDefault` bridge with real `profile:`-enum capability presets so `vh-agent-harness update` actually produces minimal/supervised rosters instead of always the full 20. Makes the profile enum mean something; migrates the dogfood profile; deprecates `modules:`. | build | 2026-07-01 DONE. Phase 5 capstone slice (approved capability-installer plan). DESIGN: `profile:` enum maps to a base capability PRESET — minimal/coordination/web → empty (baseline-only = the 8 universal agents); supervised → [core/gated-commit, core/debate] (both clusters). coordination/web aliased to minimal (no cluster intrinsically scoped to them yet — documented). Explicit `capabilities:` is UNIONED onto the preset (not replace), so `profile: minimal` + `capabilities: [core/debate]` = baseline+debate only. Unknown enum → empty (safe default). FILES (6): (1) `internal/cli/profile.go` (M) — removed `phase3BackwardCompatDefault` + `readProfileCapabilities` + the `// PHASE 5 TODO` marker; added `profileCapabilityPresets` map, `presetCapabilities`, `unionCapabilities`, `parseProfileSelection` (pure), `corpusDefaultProfileSelection`, `readProfileSelection`; `resolveCapabilityAnswers` now resolves preset∪explicit instead of the bridge. modules-deprecation machinery: `profileDeprecationSink io.Writer` (swappable, mirrors bareStderr/permission stderr precedent), `modulesDeprecationWarning` (pure, empty for nil/empty, NO value interpolation — only count), `liveProfileModules` (LIVE profile only, nil on greenfield so seeding is quiet), `emitModulesDeprecationWarning`. (2) `internal/cli/seam.go` (M) — `emitModulesDeprecationWarning(target)` called inside `renderSeamStaging` after the capability-answers merge so it fires on BOTH update (seamApply) and doctor (checkManagedDrift); stale "backward-compat default" comment updated. (3) `.vh-agent-harness/vh-harness-profile.yml` (M) — dogfood migrated minimal→supervised, dropped legacy `modules: [core]`, keeps features.backlog. (4) `internal/cli/capability_render_test.go` (M) — reframed the two Phase-3 backward-compat tests to preset semantics: `BackwardCompatDefaultRendersAllCapabilities`→`SupervisedProfileRendersAllCapabilities` (install, write supervised, update, assert all 21 + task edges); `DoctorLikeForLikeNoDriftOnBackwardCompat`→`DoctorLikeForLikeNoDriftOnSupervised`; ADDED `MinimalProfileRendersBaselineOnly` (the headline minimal=8-universal-only proof incl. no dangling task edges); updated graceful-degradation test comment to union wording. (5) `internal/cli/seam_cli_test.go` (M) — `TestSeamInstall_GateExemptAgentOmitsGateCommands` (pre-existing, broke because bare install now renders minimal=baseline-only so committer is absent) updated to switch to supervised+update before asserting; gate-exempt/gate-enabled contract unchanged. (6) `internal/cli/profile_preset_test.go` (NEW) — pure-unit coverage: `TestPresetCapabilities` (incl. coordination/web aliases + unknown safe-default + mutation-safety), `TestUnionCapabilities` (dedup, preset-order-first, the union-not-replace contract), `TestParseProfileSelection`, `TestReadProfileSelection_PresetUnion` (the plan's pinned union test, I/O path), `TestReadProfileSelection_MalformedBaselineOnly`, modules-deprecation (warning text, no-value-interpolation, emit-via-swapped-sink, malformed-quiet). templates/core UNTOUCHED (zero diff — domain-free preserved). VERIFY: `go build ./...`, `go vet ./...`, `gofmt -l .` (empty) all clean; `go test ./...` all ok (457 func Test repo-total, internal/cli=144; +11 net vs Phase-4 baseline 446/133 — 10 new profile_preset + 1 new capability_render; 3 reframes net-0). DOGFOOD BINARY PROOF: built `tmp/vh-agent-harness`, installed into a clean temp dir, switched to `profile: supervised`, update → `doctor` reports `result: HEALTHY`, `managed-drift PASS 135 managed file(s) in sync`; roster check confirms all 21 agents render (9 ungated incl. dormant plan + 7 gated-commit + 5 debate). Non-goals deferred: optional fold of features.backlog→core/backlog capability (scope creep — new catalog entry+template gate+test surface); README.agent.md profile/capabilities/modules field-contract doc (README only documents the profile FILE at L79, not the field contract → out of scope); guide menu surface changes. |  |
| P2-RESOLVER-006 | done | resolver | Capability installer — overlay integration: wire the `// PHASE 3 HOOK` so ANY embedded/project overlay pack with a `capability-manifest.yml` participates in the resolver, and ship `release` as the first embedded overlay pack (dogfood/reference). Selecting `core/release` (via `capabilities:`) OR listing `release` (via `overlays:`) both render the pack AND pull `core/gated-commit` via hard-dep closure; the two selection paths converge. | build | 2026-07-01 DONE. (1) `internal/resolver/merge.go` (M) — exported `ResolveContributions(contribs) []PackContribution` (project-over-embedded shadowing, sorted by pack name, preserves Pack+Source); refactored `MergeCatalogs` to use it; `resolveShadowing` demoted to a manifest-only projection wrapper. (2) `internal/cli/profile.go` (M) — replaced the `// PHASE 3 HOOK` `resolveCapabilityAnswers` with full overlay integration returning `(answers, renderPacks, err)`; added `discoverPackContributions(target)` iterating `overlay.KnownPacks()` (embedded) + the project overlay dir (project-first) reading `ReadCapabilityManifest` from each, fail-closed on malformed; selection = preset(profile) ∪ explicit(capabilities:) ∪ {cap-ids implied by overlays:-listed packs}; renderPacks = explicit overlays: list ∪ packs owning resolved caps. (3) `internal/cli/seam.go` (M) — pack render loop now iterates `renderPacks` (was `activeOverlays`), so capability selection drives rendering; fail-closed error message generalized. (4) NEW `templates/overlays/release/` (5 files): `capability-manifest.yml` (id core/release, provides [releaser], hard_deps [core/gated-commit]); `agents/releaser.md` (thin spine + default tag-driven/conventional-commits adapter in one file — spine owns flow-control/safety/JSON-schema/commit-gate-separation, adapter owns discover/decide/prepare/execute; never raw git tag/push/add/commit — shell-guard `git-mutation-bypass` denies; sort -V/integer-tuple never lexical; JSON reports release_model_detected vs adapter_selected, refuses on mismatch; file-level adapter-override DEFERRED to backlog); `opencode-append.jsonc` (releaser agent metadata, empty permission placeholders); `permission-pack.jsonc` (location readonly/git_readonly/gate:deny/devSh:allow, task deny, delegateFrom [build,coordination,project-coordinator]); `callable-graph-snippet.md` (inbound edges + commit-gate separation). (5) `corpus.go` + `templates/overlays/.gitkeep` (M) comments updated. (6) Tests (NEW, 15 func Test): `internal/resolver/release_overlay_test.go` (6 — ResolveContributions shadowing/sort/preserve + release manifest merge + hard-dep closure), `internal/overlay/release_pack_test.go` (4 — real embedded pack manifest shape + RenderUnits/UnitPaths exclude merge files), `internal/cli/release_render_test.go` (5 — capabilities & overlays converge, unselected renders neither, supervised keeps release dormant; +TestDiscoverPackContributions_NoManifestProjectPackShadowsEmbeddedWholly from B-F1). (7) `internal/overlay/overlay_test.go` (M) — 2 test renames for the now-shipped pack. Verify: `go build`/`go vet` clean, `gofmt -l .` empty, `go test ./...` ok; clean-room doctor HEALTHY for supervised (release dormant), capabilities:[core/release], and overlays:[release] (latter two converge: releaser.md + committer cluster + delegateFrom edges + release permission-pack materialized). Delivers the P1-CLI-001 release-subagent ask. |  |
| P2-DOGS-001 | done | overlay | Ship the `harness-dogfood` project-local overlay pack (first project-local pack) — a read-only `harness-release-readiness` agent that orchestrates ABOVE the existing `releaser`, answering "is this repo ready to hand off to releaser?" via a G1–G5 evidence checklist. No `internal/` Go production change (one render-mechanics test added); the resolver discovers project packs via `OpenPackFor` natively. | build | 2026-07-01 DONE. No `internal/` Go production change (one render-mechanics test added). NEW pack (5 files under `.vh-agent-harness/overlays/harness-dogfood/`): `capability-manifest.yml` (id `project/harness-dogfood`, provides [harness-release-readiness], hard_deps [core/release] → transitively core/gated-commit); `agents/harness-release-readiness.md` (read-only reporter — INVARIANTS never tag/commit/push/edit, read-only evidence, refuse over guess; G1 migration-note / G2 roster-shrink significance / G3 docs coverage / G4 GoReleaser changelog exclusions / G5 curated release-note need; JSON report schema with ready/blockers/warnings/human_decisions/delegated_owners/handoff_to_releaser; handoff to releaser only when ready AND human-approved; commit-gate separation, gate:deny, zero mutation); `opencode-append.jsonc` (agent block, prompt `{file:.opencode/agents/harness-release-readiness.md}` — the A-F1 contract); `permission-pack.jsonc` (readonly+git_readonly allow, gate:deny, devSh:deny, task asterisk-deny, delegateFrom build/coordination/project-coordinator, no gateExempt); `callable-graph-snippet.md` (inbound/handoff/delegation edges). NEW test `internal/cli/harness_dogfood_render_test.go` (4 func Test: ManifestGrammarValid, DiscoverFindsProjectPack, ClosurePullsReleaseAndGatedCommit, SeamRender via-overlays renders pack+closure+prompt-contract+doctor-HEALTHY). MODIFIED `.vh-agent-harness/vh-harness-profile.yml` (overlays:[harness-dogfood] to opt the dogfood repo in). No `internal/` production change — one render-mechanics test added under `internal/cli/` (OpenPackFor handles project packs natively; grammar accepts `project/` namespace). Verify: `go build`/`vet`/`test ./...` pass (incl. 4 new tests); `gofmt -l .` empty; `make build` + `update` + `doctor` HEALTHY (overlay-perm PASS 1 overlay agent across 1 pack; config-refs PASS 38 {file:} refs; managed-drift PASS 135 in sync). Generated `.opencode/agents/harness-release-readiness.md` (13105B) + `opencode.jsonc` agent block with the `{file:...}` prompt + 3 delegateFrom edges injected. Working tree dirty (not committed — left for gated-commit protocol). Honest scope: the agent G1–G5 reasoning is not model-testable; the 4 tests cover pack mechanics (discovery/closure/render/prompt-form) only. |  |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
