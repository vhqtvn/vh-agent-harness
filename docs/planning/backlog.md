# vh-agent-harness — Backlog

Source of truth for task status — the harness coordination model reads this file.

**Agents:** before substantial work, move the matching row to `in_progress` with
owner + date; on finish move it to `done` recording changed files + verification;
add follow-ups as NEW rows (new ID), never overload an existing task; if blocked,
set `blocked` with the exact blocker and the next decision needed. After any
status change run `/backlog-cleanup` (or `vh-agent-harness exec node
.opencode/scripts/normalize-backlog.js`) to keep the active sections tidy and
archive `done`/`cancelled` rows under `docs/planning/archive/`.

- **IDs:** stable `<phase>-<AREA>-<NNN>`, e.g. `P1-CORE-001`, `P2-API-003`.
- **Statuses:** `todo`, `in_progress`, `blocked` (active) · `done`, `cancelled` (history).
- **Sections:** `Now` (active focus) · `Next` (queued) · `Later` (deferred). Active
  sections hold active statuses only; the normalizer enforces and archives the rest.
- **Columns:** `ID | Status | Area | Task | Owner | Notes | Links` (Notes may carry a `YYYY-MM-DD`).

## Archive Index

Older `done` and `cancelled` history lives under [docs/planning/archive/index.md](archive/index.md) and is meant for on-demand reading instead of auto-loading into the active backlog context.

- [2026 Q2](archive/backlog-archive-2026-q2.md) - 10 done / 0 cancelled

## Now

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |

## Next

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-PERM-002 | todo | permission | Harden the shared `inspectorAllowIf` / `ALLOW_IF_INSPECTOR_FULL` / `INSPECTOR_FULL` builder for the infra-lifecycle rules (apt-install-ad-hoc, user-group-mutation, ssh-host-key-bypass, scp-upload). |  | Follow-up validated during the `f88d29f` commit (P1-PERM-001). Same gap class as the git-lane fix: (1) the shared inspector set still contains the `command` executor builtin, so `command apt-get install x` / `command usermod ...` / `command ssh ...` / `command scp ...` would carve the rule out; (2) it carries the narrower L104 chain-guard missing `\n`/`\r`/`<(`/`>(`. VALIDATED as a real reachable FP (not theoretical): during the `f88d29f` commit, `user-group-mutation` false-positive'd on the literal `usermod` token in commit-message prose (committer worked around it byte-identically via adjacent-string concatenation). Fix shape mirrors the git lane: per-rule inspector sets excluding `command`, upgrade the shared chain-guard to the full 11-op set, with its own LiveBridge regression matrix for apt/usermod/ssh/scp. Open question: `command` exclusion may need to be per-rule because the shared set serves non-mutation rules too — confirm whether `command` can stay shared or needs per-rule narrowing. |  |
| P1-CONFIG-001 | todo | config | Wire coordination-hints-lib.js to read product_prefixes from .vh-agent-harness/project.config.json (remove the phase-later TODO). |  | deferred from project.config consumption |  |

## Later

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-DOCS-001 | todo | docs | Author real docs/ai/* operational primitives for this repo (dogfood) so its own AGENTS.md refs resolve — or formally document them as project-authored. |  | dogfood self-consistency |  |
| P1-CLI-001 | todo | cli | Evaluate a release subagent/overlay (auto semver from changes, tag, push) — the consumer ask that surfaced the discoverability gap; possibly ship as an example overlay. |  |  |  |
| P2-INSTALL-001 | todo | install | Make `AGENTS.md` composition reachable for a cold consumer agent after `install`. Today `install` writes only `.vh-agent-harness/AGENTS.core.md`; the composed top-level `AGENTS.md` (core+mission) appears only after `.vh-agent-harness/AGENTS.mission.md` exists AND `update` runs. Investigate: seed a minimal mission stub on install, have install run an initial compose, and/or strengthen guide/install next-step messaging. |  | 2026-06-28 surfaced by Slice-1 validation in a throwaway consumer repo. Pre-existing, documented in install next-step #1, but a cold agent reading AGENTS.md right after install finds it missing. See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
| P2-LINEAGE-001 | todo | lineage | Decide whether `lineage.yml` should record per-file ownership labels (overlay_extension/platform_managed/project_owned). Today ownership is enforced at render time only; there is no ownership tag in lineage, so 'verify overlay_extension' is provable only behaviorally (overwrite-resilience), not by reading a label. Design question — scope before implementing. |  | 2026-06-28 surfaced by Slice-1 validation. Behavioral proof works today; this is about agent-inspectability |  |
| P2-DOCTOR-001 | todo | doctor | Reconcile `doctor` 'managed in sync' count with `update` 'managed-overwrite' count — doctor's in-sync baseline appears to exclude overlay_extension files while update includes them, so the counts differ by the number of overlay-rendered files. Either align the baselines or document the difference explicitly in doctor output. |  | 2026-06-28 surfaced by Slice-1 validation. Harmless (0 problems reported) but could make a careful agent second-guess whether an overlay file is tracked |  |

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P2-RESOLVER-005 | done | resolver | Capability installer — Phase 5 (capstone): the behavior flip. Replace the Phase-3 `phase3BackwardCompatDefault` bridge with real `profile:`-enum capability presets so `vh-agent-harness update` actually produces minimal/supervised rosters instead of always the full 20. Makes the profile enum mean something; migrates the dogfood profile; deprecates `modules:`. | build | 2026-07-01 DONE. Phase 5 capstone slice (approved capability-installer plan). DESIGN: `profile:` enum maps to a base capability PRESET — minimal/coordination/web → empty (baseline-only = the 8 universal agents); supervised → [core/gated-commit, core/debate] (both clusters). coordination/web aliased to minimal (no cluster intrinsically scoped to them yet — documented). Explicit `capabilities:` is UNIONED onto the preset (not replace), so `profile: minimal` + `capabilities: [core/debate]` = baseline+debate only. Unknown enum → empty (safe default). FILES (6): (1) `internal/cli/profile.go` (M) — removed `phase3BackwardCompatDefault` + `readProfileCapabilities` + the `// PHASE 5 TODO` marker; added `profileCapabilityPresets` map, `presetCapabilities`, `unionCapabilities`, `parseProfileSelection` (pure), `corpusDefaultProfileSelection`, `readProfileSelection`; `resolveCapabilityAnswers` now resolves preset∪explicit instead of the bridge. modules-deprecation machinery: `profileDeprecationSink io.Writer` (swappable, mirrors bareStderr/permission stderr precedent), `modulesDeprecationWarning` (pure, empty for nil/empty, NO value interpolation — only count), `liveProfileModules` (LIVE profile only, nil on greenfield so seeding is quiet), `emitModulesDeprecationWarning`. (2) `internal/cli/seam.go` (M) — `emitModulesDeprecationWarning(target)` called inside `renderSeamStaging` after the capability-answers merge so it fires on BOTH update (seamApply) and doctor (checkManagedDrift); stale "backward-compat default" comment updated. (3) `.vh-agent-harness/vh-harness-profile.yml` (M) — dogfood migrated minimal→supervised, dropped legacy `modules: [core]`, keeps features.backlog. (4) `internal/cli/capability_render_test.go` (M) — reframed the two Phase-3 backward-compat tests to preset semantics: `BackwardCompatDefaultRendersAllCapabilities`→`SupervisedProfileRendersAllCapabilities` (install, write supervised, update, assert all 21 + task edges); `DoctorLikeForLikeNoDriftOnBackwardCompat`→`DoctorLikeForLikeNoDriftOnSupervised`; ADDED `MinimalProfileRendersBaselineOnly` (the headline minimal=8-universal-only proof incl. no dangling task edges); updated graceful-degradation test comment to union wording. (5) `internal/cli/seam_cli_test.go` (M) — `TestSeamInstall_GateExemptAgentOmitsGateCommands` (pre-existing, broke because bare install now renders minimal=baseline-only so committer is absent) updated to switch to supervised+update before asserting; gate-exempt/gate-enabled contract unchanged. (6) `internal/cli/profile_preset_test.go` (NEW) — pure-unit coverage: `TestPresetCapabilities` (incl. coordination/web aliases + unknown safe-default + mutation-safety), `TestUnionCapabilities` (dedup, preset-order-first, the union-not-replace contract), `TestParseProfileSelection`, `TestReadProfileSelection_PresetUnion` (the plan's pinned union test, I/O path), `TestReadProfileSelection_MalformedBaselineOnly`, modules-deprecation (warning text, no-value-interpolation, emit-via-swapped-sink, malformed-quiet). templates/core UNTOUCHED (zero diff — domain-free preserved). VERIFY: `go build ./...`, `go vet ./...`, `gofmt -l .` (empty) all clean; `go test ./...` all ok (457 func Test repo-total, internal/cli=144; +11 net vs Phase-4 baseline 446/133 — 10 new profile_preset + 1 new capability_render; 3 reframes net-0). DOGFOOD BINARY PROOF: built `tmp/vh-agent-harness`, installed into a clean temp dir, switched to `profile: supervised`, update → `doctor` reports `result: HEALTHY`, `managed-drift PASS 135 managed file(s) in sync`; roster check confirms all 21 agents render (9 ungated incl. dormant plan + 7 gated-commit + 5 debate). Non-goals deferred: optional fold of features.backlog→core/backlog capability (scope creep — new catalog entry+template gate+test surface); README.agent.md profile/capabilities/modules field-contract doc (README only documents the profile FILE at L79, not the field contract → out of scope); guide menu surface changes. |  |
| P2-RESOLVER-004 | done | resolver | Capability installer — Phase 4: post-render fail-closed reference validation (resolver step 13). After permconfig emit, assert no `permission.task` allow/ref and no `agent.<name>.prompt` path targets a non-rendered agent or a missing `.opencode/agents/<x>.md`. Defense-in-depth on top of Phase 3's present-agent filter; true no-op on a consistent render. No dogfood behavior flip (Phase 5). | build | 2026-06-30 DONE. Phase 4 validation slice (approved capability-installer plan). (1) `internal/cli/ref_validate.go` (NEW): `validateRenderedRefs(staging, emitted)` — parses the just-emitted canonical opencode.jsonc, builds the rendered-agent set from the `agent` map, then cross-checks HARD refs: every non-`*` `permission.task` target must be a rendered agent (dangling = a capability manifest hard-dep whose cluster did not fully render, or a render path that bypassed the Phase-3 permconfig filter), and every `agent.<name>.prompt` `{file:.opencode/agents/<x>.md}` reference must resolve to a file present in the staging tree (dangling = conditional rendering removed the prompt). Other `{file:...}` refs (per-agent model files under `.local/`) are intentionally out of scope (seeded empty separately). `*` wildcard is a decision, never flagged. Collects ALL dangling refs into one deterministic sorted error listing the source agent + ref kind + missing target + the rendered roster. Returns nil when consistent (the no-op invariant). (2) `internal/cli/seam.go` (M): one-line wiring — `validateRenderedRefs(staging, emitted)` invoked inside `renderSeamStaging` right AFTER permconfig emit writes `opencode.jsonc` and BEFORE allowed-commands.js generation, so ALL 4 render paths (seamApply install/update, doctor managed-drift re-render, inventory/diff, seam_cli_test) validate like-for-like; chose renderSeamStaging (not seamApply) for the same like-for-like reason as the Phase-3 resolver injection — a seamApply-only placement would make doctor skip validation. (3) `internal/cli/ref_validate_test.go` (NEW, 6 tests): NoopWhenConsistent, DanglingTaskRefFailsClosed (names source/kind/target), DanglingPromptFailsClosed, DanglingPromptUnderNonAgentsPathFailsClosed (ORCH-F1 regression: a `prompt:{file:.opencode/prompts/missing.md}` ref — a non-`.opencode/agents/` path — fails closed), WildcardNeverFlagged, ReportsAllDanglingRefs (3 dangling refs surfaced in one pass). ORCH-F1 fix (commit-reviewer block on this slice): widened the prompt contract — removed the `.opencode/agents/` prefix gate so EVERY `{file:<path>}` prompt target is stat-resolved regardless of path; validator still reads `prompt` only (`model` field out of scope); folded ORCH-F2 polish (os.Stat failure narrowed to `errors.Is(err, fs.ErrNotExist)` so permission-denied isn't misreported as dangling). Hard vs optional semantics: ALL surviving refs treated as hard (fail-closed) because Phase 3 already pruned the optional ones (capability-gated task edges via permconfig computeTaskBlock present-agent filter); any ref still dangling at validation time is by definition a hard inconsistency. `templates/core` untouched (domain-free invariant held). `phase3BackwardCompatDefault` UNTOUCHED (Phase 5). Verify: `go build ./...` OK, `go vet ./...` OK, `gofmt -l .` empty, `go test ./...` all ok (repo total 445 `func Test`, +5 in internal/cli = 132, internal/permconfig unchanged at 18); real binary `doctor` on fresh install = HEALTHY, managed-drift PASS 135/135, exit 0 (validation is a true no-op on the dogfood render). No command-surface change → README.agent.md untouched. |  |
| P2-RESOLVER-003 | done | resolver | Capability installer — Phase 3: wire the Phase-2 resolver into the render seam and add capability gates to the core template. Backward-compat preserved (current dogfood profile renders all 20 agents byte-identically); graceful degradation verified (a gated-out capability drops its agent blocks AND every inbound task edge in universal agents). No post-render validation (Phase 4) and no dogfood behavior flip (Phase 5). | build | 2026-06-30 DONE. Phase 3 render-seam + gate slice (approved capability-installer plan). (1) `internal/cli/profile.go` (M): `capabilityTemplateKey(id)` (strip namespace, `-`→`_`: core/gated-commit→gated_commit, core/debate→debate); `readProfileCapabilities(target)`; `resolveCapabilityAnswers(target)` — CoreCatalog() + backward-compat default + closed projection to `capabilities.<key>:"true"/"false"` for EVERY catalog id; `phase3BackwardCompatDefault=[core/gated-commit, core/debate]` marked `// PHASE 5 TODO` (flip to empty = the one-line baseline-only behavior flip). (2) `internal/cli/seam.go` (M): resolver injected INSIDE `renderSeamStaging` (after projectConfigAnswers merge, before renderer.Render) — preserves the like-for-like invariant across ALL 4 callers (seamApply install/update, doctor managed-drift re-render, seamInventory/diff, seam_cli_test); the mission-suggested seamApply-only injection would have false-drifted on every doctor re-render of a gated block. (3) `internal/permconfig/emit.go` (M): present-agent filter in `computeTaskBlock` — `permconfig.Emit` authoritatively overwrites `permission.task` from Go canonical tables, so the template task-edge gates alone were demolished post-render; the filter drops any task edge whose target agent is absent from the just-rendered `agent` map (capability gating enforced via agent presence, no capability knowledge in permconfig); no-op when all agents present (backward-compat byte-identical). (4) `templates/core/opencode.jsonc.tmpl` (M): `{{- if .capabilities.<key> }}` gates mirroring the existing features.backlog trim-marker idiom — debate cluster (debate+proposer+critic+synth+solution-brief), gated-commit cluster (commit-message+reviewer+a/b/c/d+committer), and per-edge gates in build/coordination/project-coordinator task maps (commit-message/commit-reviewer/committer/debate/solution-brief) + the docs-steward→committer headline graceful-degradation edge. 8 universal agents stay ungated. (5) `internal/resolver/resolver_test.go` (M, +1 test): F1 carry-forward `TestResolve_BaselineAgentsNeverSatisfyCapabilityGate` pins that the 8 baseline agent names return `Has()==false` even with capabilities resolved (the invariant the template gates depend on). (6) `internal/permconfig/emit_test.go` (M, +2 tests): present-agent filter drops-absent-edges + no-op-when-all-present. (7) `internal/cli/capability_render_test.go` (NEW, +3 tests): backward-compat renders all 21 (9 universal incl. dormant `plan` + 7 gated-commit + 5 debate), doctor like-for-like no-drift, and explicit `capabilities:[core/debate]` graceful-degradation (14 agents, gated-commit agents+edges dropped, docs-steward→committer dropped). Tests added: 6 total (resolver 1, permconfig 2, cli 3). One key deviation: `readProfileCapabilities` treats empty `capabilities: []` the SAME as nil/absent → backward-compat default, because the profile normalizer materializes an absent key as `capabilities: []` on the first update (honoring nil-vs-empty would make the default vanish post-update → false drift on every gated block); the genuine baseline-only opt-out is Phase 5's flip of `phase3BackwardCompatDefault` to empty. Verify: `go build ./...` ok; `go vet ./...` clean; `gofmt -l .` empty; `go test ./...` all pass; REAL dogfood repo `doctor` = HEALTHY (managed-drift PASS 135/135) — the definitive render-parity proof that the pre-Phase-3 `.opencode/opencode.jsonc` is byte-identical to the new gated render. No command-surface change → README.agent.md untouched. Non-goals (Phase 4 post-render dangling-ref validation, Phase 5 dogfood minimal→baseline flip, modules deprecation, overlay-pack manifest contributions via MergeCatalogs at render time) deferred. |  |
| P2-RESOLVER-002 | done | resolver | Capability installer — Phase 2: resolver engine (`Resolve` with hard-dep transitive closure + cycle detection + optional-dep graceful absence) and pack-manifest recognition (`capability-manifest.yml` pack root file + strict fail-closed merge with project-over-embedded shadowing). No render-seam wiring yet. | build | 2026-06-30 DONE. Phase 2 resolver + merge slice (approved capability-installer plan). New `internal/resolver/resolver.go`: `CapabilityID`=string alias; `CapabilitySet` struct (`Has`/`All`/`Baseline`/`Agents`); pure `Resolve(selected []CapabilityID, *Catalog) (*CapabilitySet, error)` — DFS white/gray/black coloring for hard-dep transitive closure + cycle detection in one pass (chosen over Tarjan SCC: same traversal yields both closure and back-edge detection; recursion stack reconstructs exact cycle path; Tarjan is overkill needing a separate closure pass). Full closure not minimal cover (minimal cover is NP-hard, no benefit). OptionalDeps excluded unless explicitly selected (npm optionalDependencies semantics). Unknown hard/selected dep = hard error; unknown optional dep tolerated. Baseline carried as a parallel field in CapabilitySet (not in the caps map), folded into `Agents()` — keeps the render set self-contained and preserves the Phase-1 'capability=selectable' distinction so baseline never satisfies a capability gate. New `internal/resolver/merge.go`: `PackContribution`/`ContributionSource`; `MergeCatalogs(core, contribs)` — project-over-embedded pack-name shadowing (project REPLACES embedded manifest, no implicit dep inheritance; mirrors `OpenPackFor` FS shadowing, encoded at the merge layer for testability); strict fail-closed validation (duplicate id/provides/unknown-hard-dep = blocker; a pack re-declaring a core-seed id is also a duplicate-id blocker — core seed caps are not shadowable); `validateOutputPaths` no-op extension point (Phase-1 manifests carry no output paths yet). `internal/overlay/overlay.go` (M): added `capabilityManifestFileName` const; `isUnitFile` now excludes it (switch over the 4 merge-content files); new `(*Pack) ReadCapabilityManifest()` (parse-only via `resolver.ParseManifest`, fs.ErrNotExist→ok=false, errors wrap the pack name). Tests (co-located `*_test.go`): resolver_test.go (+15), merge_test.go (+13), overlay/capability_manifest_test.go (+8, NEW), overlay_test.go (M, +1 case for `isUnitFile`). Verify: `go build ./...`, `go vet ./...`, `gofmt -l .` empty; `go test ./...` all pass (resolver 52 incl. 24 Phase-1; overlay +8 new). No command-surface change → README.agent.md untouched. Non-goals (Phase 3+): wiring `Resolve` into `internal/cli/seam.go` before `renderSeamStaging`; capability gates `{{- if .capabilities.X }}` in the opencode.jsonc template; profile preset expansion + modules deprecation; post-render dangling-ref validation; dogfood migration. Deviations (documented): `Resolve` takes `*Catalog` and returns `*CapabilitySet` (pointer, matches Phase-1 pointer-receiver convention where `CoreCatalog` returns `*Catalog`); `type CapabilityID = string` alias rather than a defined type (zero conversion cost, doc clarity). |  |
| P2-RESOLVER-001 | done | resolver | Capability installer — Phase 1: capability schema (`capabilities:` in vh-harness-profile.yml), `CapabilityManifest` type, hardcoded central core `Catalog` seed (`core/gated-commit`, `core/debate` + 8-agent baseline). No resolver/rendering yet. | build | 2026-06-30 DONE. Phase 1 foundation slice (approved capability-installer plan). Added `Capabilities []string` to `harnessProfileData` + `harnessProfileAllowedTopLevel` + `appendStringArrayErrors` validate + append-only reconcile union (so a reconcile never silently drops a declared capability; mirrors modules/overlays exactly). New `internal/resolver` package: `manifest.go` (`CapabilityManifest` with ID/Provides/HardDeps/OptionalDeps, namespace/name grammar regex, `Validate() []error`, `ParseManifest`/`MarshalManifest` via existing `gopkg.in/yaml.v3`) and `catalog.go` (`Catalog` with by-id + by-agent indexes + baseline list, `Validate() []error` enforcing unique IDs, unique cross-capability Provides, disjoint baseline, unknown-hard-dep rejection, unknown-OPTIONAL-dep tolerance; `CoreCatalog()` seed). Baseline representation = separate baseline list on Catalog (NOT an AlwaysOn capability) — documented rationale on the `Catalog` type. Files (5): internal/schema/harness_profile.go (M), internal/schema/harness_profile_test.go (M, +4 tests), internal/resolver/{manifest.go,catalog.go,manifest_test.go,catalog_test.go} (NEW, +24 tests). Verify: `go build ./...`, `go vet ./...`, `gofmt -l .` all clean; `go test ./...` ok (resolver 24/24, schema incl. 4 new all pass). No command-surface change → README.agent.md untouched. Non-goals (resolver algorithm, rendering gates, overlay recognition, dogfood migration, modules/preset semantics) deferred to Phase 2+. One justified deviation: also wired `capabilities` into `Reconcile`/`marshalHarnessProfile` (not strictly "validation only") to avoid a silent field-drop regression through update reconcile. |  |
| P1-PERM-001 | done | permission | shell-guard `git-mutation-bypass` false-positive on descriptive prose containing git command tokens | build | 2026-06-30 DONE. Fixed the `git-mutation-bypass` shell-guard false-positive on descriptive prose (e.g. commit-message/echo/printf text containing `git checkout`/`git status`). Added a command-position-anchored inspector carve-out via a new `GIT_MUTATION_INSPECTORS` set (excludes the `command` executor builtin) + a full bash-separator chain-guard on both alternatives (alt-A `COMMIT_GATE_PREFIX` and alt-B inspector); added `printf *` to the readonly allowlist for echo↔printf parity. Engine `shell-guard-core.js` UNTOUCHED. Commit `f88d29f16f99d7d1423908affa05bcf3a77acb55` (8 files: `templates/core/.opencode/repo-configs/forbidden-patterns.core.js` source + rendered mirror, `templates/core/.opencode/repo-configs/allowed-commands.js` source + rendered mirror, `internal/permconfig/tables.go`, `internal/permission/shellguard_hook_test.go`, `opencode.jsonc`, `.vh-agent-harness/lineage.yml`). Verify: 4 build rounds caught & closed 5 distinct bypass vectors (unanchored shell-`-c` forward vector, newline separator, process-sub `<(`/`>(`, `command` executor, pre-existing alt-A `COMMIT_GATE_PREFIX` composite); commit-reviewer APPROVE unanimous; `go test ./...` / `gofmt` / `go vet` / `go build` clean; 26-case LiveBridge regression matrix (3 Allow, 23 Deny). |  |
| P1-CORPUS-011 | done | corpus | Builder-side concurrent-dirty-tree commit guidance (pass only this session's paths to the committer) | build | 2026-06-29 DONE. Added the builder-side counterpart to the reviewer's "do not let unrelated dirty files dominate" rule: a concurrently-dirty tree is normal (concurrent sessions); pass ONLY this session's explicit file/path list to the committer; no raw `git checkout`/`git status` cleanup; use `commit-gate.sh revert <paths>` (restores to HEAD, no lock/CAS/index/ref mutation) for stray files another session left dirty. Touched AGENTS.core.md rule 5, build.md rule 5, and the CLAUDE.md.template committer bullet. Files (6): templates/core/.vh-agent-harness/AGENTS.core.md, templates/core/.opencode/agents/build.md, templates/core/CLAUDE.md.template + their 3 renders (.vh-agent-harness/AGENTS.core.md, .opencode/agents/build.md, root AGENTS.md). Commit `7ce46a0196e796003870c960d3124094f2bb26b9` on `main` (not yet pushed). Verify: `commit-reviewer` APPROVE (unanimous 4-leaf, docs-only, low risk); `gofmt`/`go vet`/`go test ./...` clean; `git show --stat` = exactly 6 files, 0 strays. Propagation: consumer repos (e.g. TrueAI) inherit the rule on their next `vh-agent-harness update` (regenerates AGENTS.md + build.md from the updated embedded corpus). |  |
| P1-CLI-012 | done | cli | Interactive guard on `vh-agent-harness update` — confirm before scaffolding harness files (`.opencode/` etc.) into an uninitialized target when run by hand, preventing accidental writes into a wrong/empty folder. | build | 2026-06-29 DONE. Guard trigger: `update` prompts before proceeding when the target has no `.vh-agent-harness/vh-harness-profile.yml` AND stdin is a TTY AND the call is not bypassed; decline → exit 0, no writes. Bypasses: `-f`/`--force`, env `RUN_FROM_AGENT=1` (truthy), non-TTY (agents/CI/`make update`/`/harness`), and `--dry-run` (safe anywhere). Initialized targets never prompt (current behavior preserved). Files: internal/cli/update.go (guard logic + new `--force`/`-f` flag + injectable `updateStdinIsTTY`/`updateConfirm` test seams + help text), internal/cli/update_guard_test.go (NEW, 8 tests), README.agent.md (new "Update guard" section), README.md (command table + guard note). Verify: `go test ./...` all ok; `gofmt -l .` empty; `go vet ./...` exit 0. |  |
| P2-VERIFY-001 | done | verify | Run/encode the permission-pack contract checks (`validateRules()`-equivalent — e.g. a gate-exempt agent must NOT carry a `gate` key) inside the Go `update`/`doctor` (or `overlay new` apply), not only in the operator-run node script. Today `validateRules()` lives only in `node .opencode/sys-scripts/update-opencode-config.js` (templates/core/.opencode/sys-scripts/update-opencode-config.js ~924-948); the Go binary never invokes it, so an active overlay pack can be "valid" per the binary (0 conflicts, doctor healthy) yet FAIL the permission contract on the first real node-script run. This is exactly what surfaced the B-F1 block in Slice 2. Candidate: Go-side validation pass, or a doc note. | build | 2026-06-29 Slice-1 (detection+honesty) DONE: added doctor `checkOverlayPermissionState` (read-only — Signal A: defensive `__placeholder__` sentinel [harness scaffolder never emits it; catches hand-authored packs]; Signal B: missing `"<agent>": "allow \| ask"` delegateFrom edge for a pack-declared agent; FAIL tier names the exact resolver script path) inserted after `checkManagedDrift`; guide.go `nextSteps()` honesty fix (overlay+permission-pack users explicitly told they MUST run `node .opencode/sys-scripts/update-opencode-config.js`, since `update` does not). Files: internal/cli/doctor.go, internal/cli/guide.go, internal/cli/doctor_test.go (NEW, 6 tests). Verified: `go test ./...`, `go vet ./...`, `gofmt -l .` all clean; `make update` clean (no .opencode render regression, runtime-audit absent). SCOPE: this delivers the read-only detection + honesty half only; the deeper Go-side `validateRules()` contract port + resolver-into-update pipeline integration (collapsing the dual source of truth, byte-stable JSONC) remain open under P2-PIPELINE-001 + P2-CONFIG-001 (Slice 2 / O5 redesign). |  |
| P2-CONFIG-001 | done | config | Stop `node .opencode/sys-scripts/update-opencode-config.js` from diverging the managed `opencode.jsonc` from the corpus render, OR reconcile `doctor`/the corpus render to the node script's canonical form. Two observed manifestations of one root cause: (a) the script drops the `features.backlog` permission line (~7-line deletion diff, pre-existing known gap); (b) for a repo with an active agent-bearing overlay, the script canonicalizes the overlay agent's permission.bash/task blocks (from the overlay's simplified 3-allow form to the full readonly+git_readonly+devSh group render), so `doctor` reports `managed-drift` on `opencode.jsonc` AFTER the node script — meaning README.agent.md's golden-path ordering (update → node script → `doctor healthy`) only holds for core-only repos, not overlay repos. | build | 2026-06-29 DONE — resolved by P2-PIPELINE-001 single-source collapse. The Go-native emitter (`internal/permconfig/`) is now the sole permission authority, invoked inside `vh-agent-harness update`'s render pipeline. The Node resolver is demoted to a deprecation stub (cannot mutate opencode.jsonc). Root cause eliminated: there is no longer a second source of truth to diverge from. Both manifestations fixed: (a) features.backlog is first-class in the Go model (Q3a); (b) overlay permissions resolved in-Go-path (no separate script step). |  |
| P2-PIPELINE-001 | done | pipeline | O5 canonical permission-pipeline redesign: collapse the dual permission source of truth (corpus template bash/task literals AND resolver JS tables) into ONE Go-native emitter (`internal/permconfig/`). Demote Node resolver to migration aid. Macro architecture SETTLED (O5); micro-decisions locked (Q1b normalized deterministic JSONC, Q2c Go-owned command tables, Q3a features.backlog first-class, Q4c resolver demoted, Q5c version-gated migration). | build | 2026-06-29 DONE. 5 commits (slice 2a–2d): 9edc1b2 (emitter pkg `internal/permconfig/` + `internal/jsonc/` shared stripper + 16 unit tests), 0daa6a5 (deterministic delegateFrom + wildcard validation fixes), f1ae28e (wire emitter into `renderSeamStaging` — update produces final opencode.jsonc directly, no Node resolver step), a78d99b (allowed-commands.js migration detection + GENERATED header), 7ceeff9 (Node resolver demoted to 38-line deprecation stub; all docs/README.agent.md/skills aligned). Verified: go test/gofmt/vet clean; make update clean; doctor HEALTHY; features.backlog=true→normalize-backlog present, =false→absent; overlay delegateFrom edges emitted in Go path; resolver cannot mutate canonical permissions; single-source collapse demonstrable. Resolves P2-CONFIG-001. |  |
| P1-OVERLAY-001 | done | overlay | Research + design how to make vh-agent-harness reliably operable by coding agents in consumer repos (overlay discoverability, the customize-opencode conflict, guide/example). Deliverable: ranked fix set — A AGENTS.core.md "extending" section, B /harness command, C `overlay new` scaffolder. | build | 2026-06-28 research resolved → ranked fix set (A–E) + 3 operator-confirmed decisions in docs/checkpoints/2026-06-28-harness-agent-operability.md. Implementation decomposed into P1-OVERLAY-003 (Slice 1) + P1-OVERLAY-004 (Slice 2) | README.agent.md |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-OVERLAY-002 | cancelled | overlay | Implement the chosen discoverability fixes (A/B/C) from P1-OVERLAY-001; keep templates/core domain-free. |  | 2026-06-28 superseded — decomposed into P1-OVERLAY-003 (Slice 1: A+B+D+E) and P1-OVERLAY-004 (Slice 2: fix C). See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
