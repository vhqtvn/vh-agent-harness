# vh-agent-harness — Backlog

Source of truth for task status — the harness coordination model reads this file.

**Agents:** before substantial work, move the matching row to `in_progress` with
owner + date; on finish move it to `done` recording changed files + verification;
add follow-ups as NEW rows (new ID), never overload an existing task; if blocked,
set `blocked` with the exact blocker and the next decision needed. After any
status change run `/backlog-cleanup` (or `vh-agent-harness exec node
.opencode/scripts/normalize-backlog.js`) to keep the active sections tidy and
archive `done`/`cancelled` rows under `docs/planning/archive/`.

- **IDs:** stable `<phase>-<AREA>-<NNN>`, e.g. `P1-CORE-001`, `P2-API-003`.
- **Statuses:** `todo`, `in_progress`, `blocked` (active) · `done`, `cancelled` (history).
- **Sections:** `Now` (active focus) · `Next` (queued) · `Later` (deferred). Active
  sections hold active statuses only; the normalizer enforces and archives the rest.
- **Columns:** `ID | Status | Area | Task | Owner | Notes | Links` (Notes may carry a `YYYY-MM-DD`).

## Archive Index

Older `done` and `cancelled` history lives under [docs/planning/archive/index.md](archive/index.md) and is meant for on-demand reading instead of auto-loading into the active backlog context.

- [2026 Q3](archive/backlog-archive-2026-q3.md) - 30 done / 0 cancelled
- [2026 Q2](archive/backlog-archive-2026-q2.md) - 21 done / 0 cancelled

## Now

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |

## Next

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P2-EVAL-001 | todo | eval,verify | Lever-E seat-routing live verification — two sequential acceptance layers (mechanism proof + outcome proof). Mechanism-alone is NOT sufficient; both layers required. |  | source:review-defer · promoted-from:.local/coordinator/tasks/defer-lever-e-seat-routing-live-verification.json · studied:2026-07-31 · operator-override-promote:2026-08-02 (solution-brief replan closeout; trigger always_before(evaluation-closeout-claims-lever-E-recall-improvement) precondition now live after 5c9f370 'feat(commit-reviewer): add stakes-based seat routing metadata (lever E)'). GROUNDED in the actual lever-E mechanism: commit 5c9f370 added the 'Seat routing metadata (Lever E)' section to templates/core/.opencode/agents/commit-reviewer.md (the orchestrator prompt) and activated the panel section in templates/core/.opencode/config/review-tiers.json. Seats (fixed 4-leaf count): commit-reviewer-a = specialist (role r2_contract_data, lens contract_data, focus classes contract_drift + data_integrity); commit-reviewer-b/c/d = generalist (role r1_redundant, lens block_finder, all classes). Routing-metadata mechanism is PROMPT-LEVEL: the orchestrator (a model reading commit-reviewer.md) reads panel.leaf_roles[<leaf>], panel.lenses[<lens>].finding_classes, panel.roles[<role>].description from review-tiers.json, constructs a compact DOMAIN-FREE seat_routing block, and forwards it to each leaf invocation. The seat_routing block is ADDITIVE per-invocation metadata, NOT leaf prompt expansion; disposition/evidence/aggregation/quorum rules are UNCHANGED (attention steering ONLY — per review-tiers.json panel._comment and commit-reviewer.md seat-routing rules). PRIMARY METRIC: final-surviving recall against upheld reference findings (recall-improvement-plan researches/decisions/2026-07-29-commit-review-recall-improvement-plan.md §6 Primary metric). BASELINE: pre-E uniform panel (every leaf {role: r1_redundant, lens: block_finder}). TWO SEQUENTIAL ACCEPTANCE LAYERS (both required): (L1 MECHANISM PROOF) a live orchestrator cascade run forwards per-seat routing metadata correctly — seat A receives a specialist block naming r2_contract_data/contract_data + focus classes contract_drift,data_integrity + the prioritize-declared-contract-consistency/state-transition/data-invariants/concurrency emphasis; seats B/C/D receive generalist blocks naming r1_redundant/block_finder + independently-attributable-all-classes/no-special-emphasis. Evidence channel: capture the seat_routing block actually forwarded to each leaf in a live cascade (leaf invocation payloads / opencode task records — the forwarding is prompt-level so the proof is OBSERVATION of the forwarded block, not a static assertion). (L2 OUTCOME PROOF) at the FIXED 4-seat count, lever-E routing improves final-surviving recall on the targeted classes (contract_drift + data_integrity) vs the pre-E uniform-panel baseline, per the E-gate (recall plan Stage 2) and E-falsifier (recall plan §6). MECHANISM-ALONE-IS-NOT-SUFFICIENT (encoded explicitly): L1 proving metadata reaches leaves does NOT establish lever-E success — L2 (outcome) is REQUIRED; the seat_routing mechanism being correctly forwarded is necessary-not-sufficient. OUTCOMES: SUCCESS = L1 verified AND L2 recall improves on targeted classes at fixed seat count without precision/corroboration regression; FAILURE (falsified) = E-falsifier fires (targeted recall unchanged / overall precision falls / displaced-seat lost unique contribution >= routing gain / model concentration reduces independent corroboration without compensating gain); NOT-DEMONSTRABLE = if the verified seam cannot observe the recall outcome, record not-demonstrable (do NOT claim success). NOISE GUARDRAILS (recall plan §6): precision of final-surviving findings, unsupported/immaterial rate, positive-verification rate, duplicate rate, coordinator drop rate (interpreted alongside precision), output/context cost; raw emitted-finding count is diagnostic only. EVIDENCE TO RETAIN: per-seat seat_routing blocks (L1) + recall measurement with baseline-vs-treatment comparison + noise guardrails (L2) + the fixed-seat-count + class-targeting record. CONDITIONAL BRANCHES NOT FULLY CONCRETIZABLE FROM CODE (named honestly per sharpen discipline): (a) the exact reference-finding corpus / upheld-findings set to measure final-surviving recall against is NOT fixed in code — it is operator/test-corpus-dependent; the recall-observational-validation-protocol card (.local/coordinator/tasks/recall-observational-validation-protocol.json, kept) tracks this methodology, and fixing the corpus is a PRECONDITION for L2 (L2 cannot run until the corpus is fixed); (b) the fixed-seat baseline requires temporarily reverting review-tiers.json leaf_roles to all r1_redundant/block_finder and re-running the cascade — the revert is well-defined but operator-gated (temporary config change, reverted after measurement). NON-GOALS: do NOT activate the 5-leaf R4 reshape (r4_blind_spot stays RESERVED); do NOT change disposition/evidence/aggregation/quorum rules (attention steering ONLY); do NOT bundle defer-017 (panel quorum fixtures — different layer, stays parked under its own reopening condition). Files: NO durable source change by default (this is a live-evaluation study producing observational evidence under tmp/agent-runs/; review-tiers.json + commit-reviewer.md are READ for the mechanism; the pre-E baseline revert is a temporary config swap reverted after measurement). Success criteria: L1 mechanism proven (per-seat metadata verified in a live cascade) AND L2 outcome proven (final-surviving recall improves on contract_drift+data_integrity at fixed seat count without guardrail regression) OR honestly recorded as failure/not-demonstrable per the outcome rules. Validation plan: (1) L1 — live cascade run, capture forwarded seat_routing per leaf, assert seat A block carries specialist fields + seats B/C/D carry generalist fields; (2) L2 — fixed-seat baseline-vs-treatment comparison on a fixed reference-finding corpus (precondition: corpus fixed), measure final-surviving recall on contract_drift+data_integrity + all noise guardrails; (3) report success/failure/not-demonstrable per the outcome rules above (do NOT claim success on L1 alone). |  |

## Later

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-DOCS-001 | todo | docs | Author real docs/ai/* operational primitives for this repo (dogfood) so its own AGENTS.md refs resolve — or formally document them as project-authored. |  | dogfood self-consistency |  |
| P2-INSTALL-001 | todo | install | Make `AGENTS.md` composition reachable for a cold consumer agent after `install`. Today `install` writes only `.vh-agent-harness/AGENTS.core.md`; the composed top-level `AGENTS.md` (core+mission) appears only after `.vh-agent-harness/AGENTS.mission.md` exists AND `update` runs. Investigate: seed a minimal mission stub on install, have install run an initial compose, and/or strengthen guide/install next-step messaging. |  | 2026-06-28 surfaced by Slice-1 validation in a throwaway consumer repo. Pre-existing, documented in install next-step #1, but a cold agent reading AGENTS.md right after install finds it missing. See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
| P2-LINEAGE-001 | todo | lineage | Decide whether `lineage.yml` should record per-file ownership labels (overlay_extension/platform_managed/project_owned). Today ownership is enforced at render time only; there is no ownership tag in lineage, so 'verify overlay_extension' is provable only behaviorally (overwrite-resilience), not by reading a label. Design question — scope before implementing. |  | 2026-06-28 surfaced by Slice-1 validation. Behavioral proof works today; this is about agent-inspectability |  |
| P2-DOCTOR-001 | todo | doctor | Reconcile `doctor` 'managed in sync' count with `update` 'managed-overwrite' count — doctor's in-sync baseline appears to exclude overlay_extension files while update includes them, so the counts differ by the number of overlay-rendered files. Either align the baselines or document the difference explicitly in doctor output. |  | 2026-06-28 surfaced by Slice-1 validation. Harmless (0 problems reported) but could make a careful agent second-guess whether an overlay file is tracked |  |
| P2-PERMCFG-001 | todo | permconfig | `internal/permconfig/emit.go` `validate()` does not validate `EditOverride.Decision` values via `validDecision()` (currently only checks `rule.Edit`). |  | v0.2.1 follow-up #3 (DEFER). Safe today: the only consumer is the committer, hardcoded `Allow`. Trigger to fix: an overlay pack introduces `editOverrides`, OR `emit.go validate()` is next touched. Priority: low. |  |
| P2-PERMCFG-002 | todo | permconfig | Remove the retained `stage-message *` allowlist entry (in `internal/permconfig/tables.go` gate group) and its test assertion, now that the corpus BANS the heredoc `stage-message` form. Cross-cuts shell-guard. |  | v0.2.1 follow-up #4. Non-functional today (shell-guard chain-guard refuses any newline-containing command regardless). Schedule for the follow-up that drops the `stage-message` subcommand from `.opencode/scripts/commit-gate.sh`. Priority: low. |  |
| P1-DRIFT-003 | todo | drift | F4 — overlay_extension override path fails loudly in `checkManagedDrift` Resolve; optional fix = mirror seamInventory overlay-path extension so doctor and seamInventory agree on the ownership-defaults map. |  | Surfaced during the F3 fix (P1-DRIFT-002). checkManagedDrift builds its ownership defaults from `corpus.CoreOwnershipDefaults()` (CORE only) then `ownership.Resolve(defaults, overrides)`, while seamInventory/computeSeamDrift extends defaults with OVERLAY paths (overlay_extension files). So a project that raises an overlay_extension path via harness-ownership.yml hits an UnknownPathError in doctor's Resolve (the overlay path is absent from the core-only map) => loud FAIL, while seamInventory accepts the same override. This is fail-safe-loud today (no silent corruption) and no consumer is known to override an overlay_extension path, so deferred. Fix shape: have checkManagedDrift extend defaults with active-overlay paths the way seamInventory does, so both surfaces agree. Priority: low. | F4 |
| P2-PERMCFG-004 | todo | permconfig | D-F1 — stage-message stderr/stdout regression test: assert the deprecation notice goes to STDERR only and the staged-payload JSON to STDOUT (RC=0), so a future refactor cannot regress the stream split. |  | Surfaced during P1-DOCS-002 (the stage-message deprecation slice). Its functional test confirmed the split manually but added no durable regression guard. Complements P2-PERMCFG-002 (removing the retained stage-message allowlist entry once the subcommand is dropped). Priority: low. | D-F1 |
| P2-COMMIT-001 | todo | commit | Committer auto-split for mixed backlog+code path sets — committer detects `docs/planning/backlog.md` alongside code paths and splits internally into a code-only acquire + a backlog-only acquire, retrying the backlog one on CAS conflict. |  | Trigger to ship: dogfood shows real friction from manually pre-splitting code-only vs backlog-only handoffs at task boundaries. O1 gate preflight (commit 77dc6f46) already structurally protects code from backlog blob conflicts — the auto-split is convenience (one delegation instead of two), NOT correctness. No G1/lock needed (backlog conflict bubbles to agent for re-read+reapply). Do NOT bundle with P2-GATE-001. Priority: low. |  |
| P2-GATE-001 | todo | gate | G1 — line-level 3-way merge (`git merge-file`) for declared shared paths (initially `docs/planning/backlog.md`) in `commit-gate.sh cmd_commit`, superseding blob-level `read-tree -m -i` for those paths. |  | Deferred behind a validation packet (decision memo `researches/decisions/2026-07-05-commit-gate-shared-file-coupling.md` §G1/Deferred-Work). Packet must cover: duplicate-ID detection, conflict-marker rejection, `normalize-backlog.js --check` coverage, false-conflict rate. `docs/planning/G1-validation-scope.md` (referenced by an old planner brief) was never authored — packet authorship is the first sub-step. Core-wide blast radius (gate hot path on every acquire); do NOT bundle into another slice. Priority: low-medium. |  |

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-SUBSTRATE-001 | done | substrate,lineage | Make apply/lineage provenance truthful under live-write failure. | build | done:2026-08-02 (build) — commit 4f34d45a. lineage-stability fix; live-write failure gates lineage advance + manifest persist via GenerationFullyApplied; behavioral-closure proven; 3 DEFERs. Follow-up from P1-LINEAGE-002 v1.1 (ship-review blocker #1 option-c scope fence). v1.1 added the additive typed `substrate.FileOutcome.WriteState` field + a NARROW `seamApply` manifest-skip gate (failed tracked overlay-skill dest → skip manifest persist, prior byte-intact), but deliberately LEFT `substrate.Apply` return semantics unchanged: a live-write failure still sets WriteFailed+Note and returns nil, and lineage still advances for a generation that may not have fully applied. This row closes the broader gap: define execution-failure aggregation in `substrate.Apply`; decide whether any live-write failure returns an error / stops vs continues / which post-apply side effects run; ensure lineage does NOT advance for a generation that did not fully apply. Coverage: partial failure + lineage stability + CLI install/update behavior + existing proposal/seed side effects. Reuses the typed `FileOutcome.WriteState` field added in v1.1. NOT the F6/F7 defer card (`.local/coordinator/tasks/defer-p1-lineage-002-001.json` — do not touch). |  |
| P1-INTEGRITY-001 | done | integrity,commit-review | Define the canonical declared-scope and coverage-disposition representations for F4-A, then add a deterministic validator that detects missing, ambiguous, extra, and undisposed scope items. Keep the mechanism non-blocking until fixtures establish a reliable equality contract and an explicit approval transition. | build | done:2026-08-02 (build) — commit b914b06. scopecoverage pkg: declared-scope/disposition representations + pure Validate(); 6 fixtures green; non-blocking (INFORMS-only); defer-019. source:decision-2026-07-28-success-report-integrity · Finding A · evidence E-A-PARTIAL-SEAM, E-A-SCOPE-ANCHOR, E-AUTHORITY · studied:2026-07-28. Files likely affected: templates/core/.opencode/agents/commit-reviewer.md; canonical template/source for docs/coordination/CLOSEOUT_TEMPLATE.md; relevant internal/cli/ doctor source+tests if a doctor diagnostic is added; README.agent.md only if the user-visible command or operating contract changes; generated .opencode/ files only through the dogfood render/update path, never by direct editing. Validation plan: (1) fixture declared {a,b} vs reported {a} → incomplete/missing-`b`; (2) declared {a,b} vs {a,b} → pass; (3) undeclared {c} → mismatch reported; (4) fail-fast termination cannot produce complete approval while any declared item lacks a terminal disposition; (5) validator claims structural coverage only, not semantic examination; (6) run the narrow verified repository seam, then `go test ./...`, `go vet ./...`, `gofmt`, and required dogfood render/doctor checks. Hard-gate promotion conditional on canonical scope identity + passing fail-closed fixtures. |  |
| P1-INTEGRITY-002 | done | integrity,closeout,release | Update the canonical prompt and closeout guidance so "green" distinguishes full verification, targeted/smoke verification, and clean transition state. Preserve existing B1/G0 and B2/G0b as separate controls. Explicitly state that release requires global cleanliness while ordinary commit-gate integrity is exact-slice based and must tolerate unrelated concurrent dirt. | build | done:2026-08-02 (build) — commit b914b06. green-distinction docs (full/targeted/clean-transition); B1/G0+B2/G0b preserved; release=global vs commit-gate=exact-slice. source:decision-2026-07-28-success-report-integrity · Findings B1, B2 · evidence E-BEHAVIORAL-CLOSURE, E-FALSE-GREEN, E-B1-G0, E-B2-G0B, E-COMMIT-LEDGER · studied:2026-07-28. Files likely affected: canonical source for the AGENTS.md behavioral-closure and testing guidance; canonical template/source for docs/coordination/CLOSEOUT_TEMPLATE.md; gated-commit documentation or skill source; release-readiness documentation; researches/decisions/2026-07-28-success-report-integrity-and-working-tree-stewardship.md; README.agent.md only if the operating or command contract changes. Validation plan: (1) docs clearly distinguish B1 from B2; (2) targeted tests cannot be labeled full green; (3) missing revision-bound full-build evidence maps to `inconclusive`/`not-demonstrable`; (4) release docs match G0 and G0b behavior in scripts/release-tag.sh; (5) commit-gate docs do not introduce a global-clean-tree requirement; (6) behavioral-closure wording continues to state the token is a declaration, not proof; (7) run documentation/render consistency checks plus `go test ./...`, `go vet ./...`, `gofmt`. |  |
| P2-M1-001 | done | m1 | Run the M1 natural-miss classification in-lane under floored `exec-sandbox --sandbox=strict` — `researcher` executes `tmp/commit-study/m1_classify.py` (reads the DB copy `mode=ro`, writes the neutral dump to `tmp/`), classifies the 71 sampled misses, and reproduces the (e) genuine-capability share (already established in the source packet as 27/69 = 39.1%). The in-lane dogfood payoff of the exec-sandbox Level-B grant (no python3 wildcard, no build handoff). | build | done:2026-08-02 (build) — research run (no commit). dogfood PROVEN in-lane under floored strict; (e)=39.1% reproduced delta 0; P5 bypass clamped; tmp outputs only. source:decision-payoff · memo:researches/decisions/2026-07-29-exec-sandbox-level-b-mode-floor.md §Payoff · source-packet:researches/sources/2026-07-28-m1-natural-miss-mechanism-study.md · studied:2026-07-30. The exec-sandbox Level-B decision promoted `researcher` to Level B (kernel-contained arbitrary read-code); its §Payoff documents that once `researcher` has the floored grant it runs `m1_classify.py` under `vh-agent-harness exec-sandbox --sandbox=strict` (DB copy read `mode=ro`, neutral dump to `./tmp/`), classifies, and computes the (e) share — no python3 wildcard grant, no build handoff. This row tracks that in-lane run as work; it is currently UNTRACKED. Files: no durable source change by default (the run produces gitignored `tmp/commit-study/` outputs — sensitive `m1_classify_input.json` stays in tmp, only neutral `m1_classify_output.json` is promotable; the script already exists gitignored). Success criteria: `researcher` runs the full Leg-2 pipeline in-lane under the strict floor (no wildcard, no build handoff) and the (e) share reproduces — the dogfood proof (a read-only agent runs arbitrary read-code safely in-lane) is demonstrated. Validation plan: (1) `vh-agent-harness exec-sandbox --sandbox=strict -- python3 tmp/commit-study/m1_classify.py` runs in-lane from the researcher profile (DB copy `mode=ro`; neutral dump to tmp/); (2) the strict floor contains every side effect (write-outside-tmp → EACCES, network denied) — re-confirm the P5 bypass is blocked under the floored config; (3) the (e) genuine-capability share + Wilson 95% CI is computed and reconciled with the source-packet headline (27/69 = 39.1%, CI [28.5%, 50.9%]); (4) persist-guard held (sensitive input stays gitignored; only neutral output promotable); (5) `go test ./...`/`go vet`/`gofmt` unaffected (no code change); doctor HEALTHY. |  |
| P3-SCRIPTS-001 | done | release,scripts | Rename check-defer-triggers.js → .mjs (ESM robustness for Go scratch tests). | build | done:2026-08-02 (build) — commit b6d8e3a. live-only rename check-defer-triggers.js→.mjs (37 live refs, 16+ historical left). source:decision-memo · memo:researches/decisions/2026-08-02-defer-liveness-provenance-scope-divergence.md:146 · robustness not ceremony fix · studied:2026-08-02. Authorized-but-unlanded slice tracked only in memo prose (original card was `rm`'d during the v0.19.0 ceremony); needs this row to become schedulable. Rename so Go scratch tests that `node`-execute the evaluator in a bare tempdir (no package.json / `type:module`) resolve ESM unambiguously via the `.mjs` extension. File scope: 8 Go `filepath.Join` path-literal sites — `check_defer_release_test.go:68,78` · `release_tag_manifest_test.go:59,74` · `check_defer_triggers_test.go:34,44` · `release_tag_test.go:129,146`; +1 render-assertion `harness_dogfood_render_test.go:419`; +2 CI/shell — `.github/workflows/release.yml:62`, `scripts/release-tag.sh:388`; +2 overlay-agent prompts +~8 live-doc prose refs. Validation plan: node-version matrix 18/20/22/23 in BOTH in-repo and bare-scratch-no-package.json contexts; `make update --dry-run`; `go test ./...`; `gofmt -l .`; `go vet ./...`; `vh-agent-harness doctor` HEALTHY. NON-GOALS: NOT a ceremony fix; independent of option (b) widen-manifest (P2-REL-001). |  |
| P2-REL-001 | done | release,release-gate | Widen release DEFER manifest scope to all firing cards (option b) + runbook complement. | build | done:2026-08-02 (build). source:decision-memo · memo:researches/decisions/2026-08-02-defer-liveness-provenance-scope-divergence.md · resolves option (b) widen manifest + (c) runbook complement · external-study=release-relevant · confidence:MEDIUM · studied:2026-08-02. Authorized-but-not-landed slice: update releaser/release-readiness agent policy so EVERY firing card the release-prep enumerator surfaces gets an explicit, committed disposition (today the manifest commits dispositions only for `source:review-defer`-tagged cards; other firing cards are surfaced-but-undisposed). Go gate UNTOUCHED. Files likely affected: .vh-agent-harness/overlays/harness-dogfood/agents/harness-release-readiness.md (policy: manifest ALL firing cards, not just source:review-defer); templates/overlays/release/agents/releaser.md (releaser ceremony policy); internal/cli/harness_dogfood_render_test.go:411 (replace the `"source:p2-followup exclusion"` assertion with the widened-scope assertion); ceremony runbook docs (document `VH_HARNESS_DEFER_OVERRIDE_IDS` escape hatch + "do NOT `rm` transport cards to clear the gate"). NON-GOALS (explicit): do NOT touch the Go defer-liveness gate (`checkDeferLiveness`/`loadLivenessCards`); do NOT add a source filter to `loadLivenessCards`; do NOT narrow silencer-immunity; the `.js → .mjs` rename of `check-defer-triggers.js` is a SEPARATE lower-priority slice (track independently, not here). Validation plan: (1) `go test ./...` (esp. `TestHarnessDogfoodRender*` + the render assertion at harness_dogfood_render_test.go:411 now asserting widened scope); (2) `gofmt -l .`; (3) `go vet ./...`; (4) `vh-agent-harness doctor` HEALTHY; (5) `make update --dry-run` confirms no platform_managed drift. Gated on the memo commit landing (happening concurrently). DONE 2026-08-02 (build): option (b) widen-manifest + (c) runbook complement LANDED. Changed: .vh-agent-harness/overlays/harness-dogfood/agents/harness-release-readiness.md (provenance-scope policy block widened to ALL sources incl source:external-study=release-relevant-frequently-disclose; evidence-cmd candidate phrase widened; new "Go defer-liveness gate recovery (VH_HARNESS_DEFER_OVERRIDE_IDS)" subsection: non-destructive override + do-not-rm rule + two-surface distinction); templates/overlays/release/agents/releaser.md (matching "Go defer-liveness gate recovery" section); internal/cli/harness_dogfood_render_test.go:411 (assertion replaced: "source:p2-followup exclusion"→"external-study release-relevant (widened scope)" pinning source:external-study, a token the pre-widening doc lacked); README.agent.md (concise two-gate + all-sources + override/do-not-rm cross-reference); rendered mirrors .opencode/agents/{harness-release-readiness,releaser}.md + .vh-agent-harness/lineage.yml + rendered-outputs.json regenerated via make update. Go gate UNTOUCHED (checkDeferLiveness/loadLivenessCards/release_gate.go/claim.go — non-goal preserved). Verification: go test ./... GREEN (TestHarnessDogfood_ReleaseReadinessCarriesG7Gate PASS source+mirror incl new assertion; TestDeferRecurrence_* incl _LiveRepoIsClean + _ClearsViaOperatorOverride PASS; TestDeferLivenessGate_* incl _LiveRepoIsClean PASS); gofmt -l . empty; go vet ./... clean; go build ./... exit 0; vh-agent-harness doctor HEALTHY (0 problems/0 warnings; dev-stale-embed PASS); make update (2 managed-overwrite, no platform_managed drift) + update --dry-run idempotent (204 managed byte-identical). NOT committed — routed to coordinator for committer (split-commit: backlog row separate from code/docs). |  |
| P1-RELEASE-002 | done | release | Add tag-ready→tag HEAD-drift detection to `release-tag.sh` so a concurrent landing between tag-ready and tag cannot silently re-target the manifest pin. | build | done:2026-08-02 (build) — commit cab169f2. release-tag.sh HEAD-drift detection: pin (explicit-commit git tag -a HEAD_SHA) + detect (head_drift JSON) + post-create verify. releaser.md updated. Drift-path committed test DEFERed (not-demonstrable through seam; structurally closed). source:p2-followup · defer-release-tag-ready-head-hygiene · studied:2026-07-30 · fired (scripts/release-tag.sh touched 2026-07-30; no drift guard present). Real gap from the v0.15.0 incident: tag target 40e5f74 while a concurrent study commit c3dcf29 landed on main, requiring operator history surgery to preserve the intended pin-to-M. Current scripts/release-tag.sh (~42KB) has NO tag-ready→tag HEAD-drift detection (grep for tag.ready/intervening/pin/pin-to-M returns empty). Files: scripts/release-tag.sh (repo root, project-owned) + templates/overlays/release/agents/releaser.md (canonical source for release-agent guidance). Validation plan: (1) fixture where HEAD advances between the tag-ready assertion and tag application → release-tag.sh detects and fails/pins rather than silently re-targeting; (2) happy path where HEAD is unchanged → proceeds normally; (3) the released tag object's tree matches the manifest-pinned commit, not whatever main pointed at mid-window; (4) `go test ./...`, `go vet`, `gofmt`; (5) dogfood render/doctor if templates touched. |  |
| P2-DOCS-002 | done | docs | Refresh README.agent.md closeout-status vocabulary to include the S1/S2 terms (`rebased_refused`, `reviewed_tree_diverged`, `head_at_acquire`) added by 4bc821e and reconcile the remaining `cas_conflict` contrast references. | build | done:2026-08-02 (build) — commit def2593. README.agent.md closeout vocab: S1/S2 terms added, cas_conflict refs retired. (commit also carried a pre-existing unauthored exec-ro hunk swept by whole-file acquire — attribution recorded.) source:review-defer · defer-readme-agent-rebased-refused-status-vocab · studied:2026-07-30 · fired (README.agent.md grown since the card was authored; vocab section now at lines 937-955). AGENTS.md mission rule: stale README.agent.md is a bug. The section documents `committed`/`no_head_progress`/`could_not_land` but OMITS `rebased_refused`/`reviewed_tree_diverged`/`head_at_acquire` (the S1/S2 closeout terms from 4bc821e). NOTE README still references `cas_conflict` as a contrast term at lines 949/954 (cas_conflict is no longer emitted by the gate — a separate surface from defer-011's trigger set, tracked here for vocab coherence). File: README.agent.md (project-owned repo-root, single copy). Validation plan: (1) README closeout-status vocab lists ALL current gate statuses including the S1/S2 trio; (2) `cas_conflict` references reconciled/removed or correctly scoped; (3) render/doctor consistency if templates touched; README.agent.md is repo-root so go test/vet/gofmt unaffected unless code referenced. |  |
| P1-SUBSTRATE-002 | done | substrate | Consolidated quarantine substrate slice — syntax-invalid JSON filename-level quarantine (state-lib.js) + negative-path/rethrow coverage (D1/D2/F1) + quarantine regression test-coverage gaps (empty/all-healthy + repair→re-scan recovery). | build | done:2026-08-02 (build) — commit 9f7a5f3. Quarantine negative-path (D1/D2/F1) + recovery re-scan tests. Part 1 (syntax quarantine) already complete in e709756; this slice test-only. verify-task-registry.js: verification ok. source:review-defer · consolidation of 3 DEFER cards: defer-quarantine-syntax-invalid-json [ready], defer-quarantine-syntax-rethrow-coverage, defer-quarantine-test-coverage-gaps · studied:2026-07-28/2026-07-31 · fired (state-lib.js 5cd5caf 07-31 > studied 07-28; verify-task-registry.js e709756 07-31 > studied 07-28). THREE PARTS: (1) SYNTAX-INVALID JSON QUARANTINE [defer-quarantine-syntax-invalid-json]: wrap per-card readJson in scanCoordinationTaskCards with try/catch; on JSON parse failure emit quarantine entry {card_id: filename, path, error_type:"syntax", offending_fields:[], problems:[parse error msg]} and continue scanning; reserve error_type:"semantic" for the existing normalized-card path. Today a single corrupt .json bricks the whole scan. (2) NEGATIVE-PATH/RETHROW COVERAGE [defer-quarantine-syntax-rethrow-coverage]: D1 — readCoordinationTask(<corrupt id>) throws SyntaxError-wrapped StateError (only list path tested today); D2 — non-SyntaxError fs error (EACCES/ENOENT/TOCTOU) must THROW not quarantine (rethrow guarantee unasserted e2e); F1 — listCoordinationTaskCards/resolveRecurrenceDedup null-safety against task:null syntax entry (code-reading only today). (3) QUARANTINE TEST-COVERAGE GAPS [defer-quarantine-test-coverage-gaps]: explicit named case for empty/all-healthy registry (quarantine:[], degraded_count:0); quarantine→repairCoordinationTask→re-scan recovery path (degraded card repaired on disk, re-scan shows it leaving quarantine). Files: templates/core/.opencode/scripts/state-lib.js, templates/core/.opencode/scripts/verify-task-registry.js. Validation: (1) sentinel+corrupt-sibling disk fixture — listCoordinationTasks does not throw, sentinel returned, corrupt file in quarantine[] keyed by filename; (2) D1/D2/F1 negative tests; (3) empty/all-healthy + recovery named cases; (4) re-run node .opencode/scripts/verify-task-registry.js + verify-state-validation.js green; (5) make update + go test ./... + go vet + gofmt + doctor HEALTHY (templates/core touched → render/doctor checks). Constraints: do not weaken throw for genuine fs errors (permission denied/missing dir); keep semantic-degradation quarantine path unchanged; parts 2-3 are additive test cases only. |  |
| P2-DOCS-003 | done | docs,commit-gate | Reconcile cas_conflict doc-drift in recovery docs (backlog SKILL.md, committer.md, docs/coordination/README.md) — after P0-B Slice B the gate emits zero cas_conflict (merge-fail sites became could_not_land); recovery docs still teach cas_conflict. | build | done:2026-08-02 (build) — commits ad2a84c+f0019550. cas_conflict doc-drift reconciled -> could_not_land across backlog SKILL.md, committer.md, coordination/README.md (render+source). No emit site reintroduced. source:review-defer · defer-011-cas-conflict-recovery-doc-drift · studied:2026-07-26 · fired (claim.go 09fe9eb 07-30; committer.md/SKILL.md dfa4203 08-02; README.md 7eeb858 07-30 — all > studied 07-26). After P0-B Slice B (6f524bdb) the commit-gate no longer emits JSON status cas_conflict at all: the merge_failed+write_tree_failed sites became could_not_land; concurrent-HEAD-movement retries to error/cas_retry_exhausted. Three recovery docs still teach cas_conflict recovery for what is now the content-tangle path (could_not_land): templates/core/.opencode/skills/backlog/SKILL.md, templates/core/.opencode/agents/committer.md, docs/coordination/README.md. SEPARATE SURFACE from P2-DOCS-002 (which is README.agent.md closeout-status vocab only — P2-DOCS-002 notes explicitly call this "a separate surface from defer-011's trigger set"); own row per backlog rule "add follow-ups as NEW rows, never overload an existing task". Files: templates/core/.opencode/skills/backlog/SKILL.md, templates/core/.opencode/agents/committer.md, docs/coordination/README.md. Validation: (1) grep recovery docs for cas_conflict and confirm retired or correctly scoped to a reintroduced emit site (if any); (2) map the content-tangle path (what was cas_conflict) to could_not_land consistently with README.agent.md status vocab; (3) render/doctor consistency (templates/core touched); (4) make update --dry-run confirms no platform_managed drift. Non-goals: do NOT reintroduce a cas_conflict emit site in the gate (behavior change, not doc fix). |  |
| P1-CLI-002 | done | cli | F1 P-a producer: dedupe EvidenceRefs + CheckedScope in `internal/cli/f1_pa.go` (DROP polish from slice-4 commit-review). | build | done:2026-08-02 (build) — commit 329998e. f1_pa.go dedupes EvidenceRefs+CheckedScope on copies; regression test asserts firstDuplicate+purity+stable IDs. ./internal/cli/ green. source:review-defer · promoted-from:.local/coordinator/tasks/review-defer-f1-pa-producer-dedup.json · studied:2026-07-25 · operator-override-promote:2026-08-02 (f1-surface `path_touched(internal/cli/f1_pa.go)` trigger NOT fired in current effort; operator override supplies the DoR). DROP polish: `GeneratePAProbes` currently sorts CheckedScope but does NOT dedupe EvidenceRefs or CheckedScope; a caller passing duplicate entries produces output that fails `validatePASummary`'s `firstDuplicate` check. Not CI-breaking today (callers pass clean inputs); narrow producer-purity hole. Slice: add dedup on COPIES of the caller slices (never mutate the caller's EvidenceRefs/CheckedScope) in the producer copy/sort helpers; keep stable probe IDs (PA-P1/PA-P2/...) deterministic. Constraints: preserve producer purity (dedupe on copies); keep stable probe IDs deterministic. Non-goals: do NOT change the per-result honesty matrix or the closed-enum coverage semantics. File scope: internal/cli/f1_pa.go (+ new duplicate-input regression test in internal/cli/). Validation plan: (1) new regression test — caller passing duplicate EvidenceRefs/CheckedScope entries produces output that passes `validatePASummary`'s `firstDuplicate` check; (2) `go test ./internal/cli/`; (3) `gofmt -l internal/cli/f1_pa.go`; (4) `go vet ./internal/cli/`. |  |
| P1-TESTS-001 | done | tests,verify | CI regression fixture: run both consumer-render verifiers (`verify-no-unrendered-paths.js` + `verify-f3-authoring-surfaces.js`) against a synthetic non-Go consumer tree — persistent regression-pin for the b69fb1f polyglot `findRepoRoot` repair. | build | done:2026-08-02 (build) — commit 1681e520. consumer_render_verifier_fixture: both verifiers exit 0 over non-Go consumer; negative control fails as expected. source:review-defer · promoted-from:.local/coordinator/tasks/defer-consumer-render-verifier-regression-fixture.json · studied:2026-08-02 · fired (trigger `path_touched(templates/core/.opencode/scripts/verify-no-unrendered-paths.js) OR path_touched(templates/core/.opencode/scripts/verify-f3-authoring-surfaces.js)` fired by b69fb1f 2026-07-26 'fix(corpus): repair source-checkout-only assumptions in consumer renders'). NOTE circular-but-legitimate: the card exists BECAUSE of b69fb1f (which fixed the acute non-Go-exit-1 defect at source via the polyglot findRepoRoot), but the trigger mechanically fired within the 30-day window and the regression-pin value stands — the test catches reversion to pre-b69fb1f findRepoRoot (go.mod-only heuristic) behavior. Acute defect mitigated at source; this row tracks the persistent regression guard. File scope: tests/integration/fixtures/non-go-consumer/ (NEW: pyproject.toml + .git, NO go.mod/corpus.go/templates/core — synthetic consumer tree; resolution of the tests/ vs .opencode/scripts/fixtures/ open question → tests/integration/ so the fixture is CI-visible, not under rendered output), tests/integration/consumer_render_verifier_fixture_test.go (NEW: Go-driven test shelling out to node invoking both verify scripts over the fixture and asserting exit 0 from both). templates/core/.opencode/scripts/verify-*.js are read/invoked, NOT modified. Validation plan: (1) `go test ./tests/integration/... -run TestConsumerRenderVerifierFixture` invokes node over both verify scripts against the fixture, asserts exit 0 from each; (2) negative control — temporarily restore pre-b69fb1f findRepoRoot (go.mod-only heuristic) on a scratch copy, confirm the test FAILs (non-zero exit / ENOENT on .tmpl read / null root), then revert (this is the regression-pin value); (3) gofmt -l tests/integration/ + go vet ./tests/integration/... clean. Non-goals: do NOT modify the verify scripts themselves (only exercise them). |  |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P2-PERMCFG-003 | cancelled | permconfig | Broader git-global-flag normalization redesign (the "O3" option from the v0.2.1 debate): push stripped tokens into `normalizedCommands` so allowlist+routing+deny-re-scan all see bare `git <verb>`, AND remove the now-redundant 12 `git --no-pager <sub> *` config entries (which become inert under that design). Cross-cuts shell-guard. |  | 2026-08-03 cancelled — O3 premise refuted: normalizedCommands cannot affect OpenCode L2; removing the 12 --no-pager entries regresses detect-only. No code/commit. Different command-rewrite redesign deferred in defer-020-shellguard-git-global-rewrite-rescope (draft; requires operator blessing + design blockers). v0.2.1 follow-up #5 (O3). Resolves the multi-flag readonly over-block (`git --no-pager --paging=no log` currently hard-denies under the shipped cde2ac94 design). Needs its own explicit review + scope expansion — do NOT bundle into another slice. Priority: medium. |  |
| P1-CLI-001 | cancelled | cli | Evaluate a release subagent/overlay (auto semver from changes, tag, push) — the consumer ask that surfaced the discoverability gap; possibly ship as an example overlay. |  | 2026-07-01 superseded — delivered by P2-RESOLVER-006 (the `release` overlay pack ships the tag-driven release subagent this evaluate task asked about, as the first embedded overlay pack + reference implementation of the capability-installer overlay integration). |  |
| P1-OVERLAY-002 | cancelled | overlay | Implement the chosen discoverability fixes (A/B/C) from P1-OVERLAY-001; keep templates/core domain-free. |  | 2026-06-28 superseded — decomposed into P1-OVERLAY-003 (Slice 1: A+B+D+E) and P1-OVERLAY-004 (Slice 2: fix C). See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
