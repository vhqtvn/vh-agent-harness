# vh-agent-harness — Backlog

Source of truth for task status — the harness coordination model reads this file.

**Agents:** before substantial work, move the matching row to `in_progress` with
owner + date; on finish move it to `done` recording changed files + verification;
add follow-ups as NEW rows (new ID), never overload an existing task; if blocked,
set `blocked` with the exact blocker and the next decision needed. After any
status change run `/backlog-cleanup` (or `vh-agent-harness exec node
.opencode/scripts/normalize-backlog.js`) to keep the active sections tidy and
archive `done`/`cancelled` rows under `docs/planning/archive/`.

- **IDs:** stable `<phase>-<AREA>-<NNN>`, e.g. `P1-CORE-001`, `P2-API-003`.
- **Statuses:** `todo`, `in_progress`, `blocked` (active) · `done`, `cancelled` (history).
- **Sections:** `Now` (active focus) · `Next` (queued) · `Later` (deferred). Active
  sections hold active statuses only; the normalizer enforces and archives the rest.
- **Columns:** `ID | Status | Area | Task | Owner | Notes | Links` (Notes may carry a `YYYY-MM-DD`).

## Archive Index

Older `done` and `cancelled` history lives under [docs/planning/archive/index.md](archive/index.md) and is meant for on-demand reading instead of auto-loading into the active backlog context.

- [2026 Q3](archive/backlog-archive-2026-q3.md) - 40 done / 0 cancelled
- [2026 Q2](archive/backlog-archive-2026-q2.md) - 21 done / 0 cancelled

## Now

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |

## Next

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P2-EVAL-001 | todo | eval,verify | Lever-E seat-routing live verification — two sequential acceptance layers (mechanism proof + outcome proof). Mechanism-alone is NOT sufficient; both layers required. |  | source:review-defer · promoted-from:.local/coordinator/tasks/defer-lever-e-seat-routing-live-verification.json · studied:2026-07-31 · operator-override-promote:2026-08-02 (solution-brief replan closeout; trigger always_before(evaluation-closeout-claims-lever-E-recall-improvement) precondition now live after 5c9f370 'feat(commit-reviewer): add stakes-based seat routing metadata (lever E)'). GROUNDED in the actual lever-E mechanism: commit 5c9f370 added the 'Seat routing metadata (Lever E)' section to templates/core/.opencode/agents/commit-reviewer.md (the orchestrator prompt) and activated the panel section in templates/core/.opencode/config/review-tiers.json. Seats (fixed 4-leaf count): commit-reviewer-a = specialist (role r2_contract_data, lens contract_data, focus classes contract_drift + data_integrity); commit-reviewer-b/c/d = generalist (role r1_redundant, lens block_finder, all classes). Routing-metadata mechanism is PROMPT-LEVEL: the orchestrator (a model reading commit-reviewer.md) reads panel.leaf_roles[<leaf>], panel.lenses[<lens>].finding_classes, panel.roles[<role>].description from review-tiers.json, constructs a compact DOMAIN-FREE seat_routing block, and forwards it to each leaf invocation. The seat_routing block is ADDITIVE per-invocation metadata, NOT leaf prompt expansion; disposition/evidence/aggregation/quorum rules are UNCHANGED (attention steering ONLY — per review-tiers.json panel._comment and commit-reviewer.md seat-routing rules). PRIMARY METRIC: final-surviving recall against upheld reference findings (recall-improvement-plan researches/decisions/2026-07-29-commit-review-recall-improvement-plan.md §6 Primary metric). BASELINE: pre-E uniform panel (every leaf {role: r1_redundant, lens: block_finder}). TWO SEQUENTIAL ACCEPTANCE LAYERS (both required): (L1 MECHANISM PROOF) a live orchestrator cascade run forwards per-seat routing metadata correctly — seat A receives a specialist block naming r2_contract_data/contract_data + focus classes contract_drift,data_integrity + the prioritize-declared-contract-consistency/state-transition/data-invariants/concurrency emphasis; seats B/C/D receive generalist blocks naming r1_redundant/block_finder + independently-attributable-all-classes/no-special-emphasis. Evidence channel: capture the seat_routing block actually forwarded to each leaf in a live cascade (leaf invocation payloads / opencode task records — the forwarding is prompt-level so the proof is OBSERVATION of the forwarded block, not a static assertion). (L2 OUTCOME PROOF) at the FIXED 4-seat count, lever-E routing improves final-surviving recall on the targeted classes (contract_drift + data_integrity) vs the pre-E uniform-panel baseline, per the E-gate (recall plan Stage 2) and E-falsifier (recall plan §6). MECHANISM-ALONE-IS-NOT-SUFFICIENT (encoded explicitly): L1 proving metadata reaches leaves does NOT establish lever-E success — L2 (outcome) is REQUIRED; the seat_routing mechanism being correctly forwarded is necessary-not-sufficient. OUTCOMES: SUCCESS = L1 verified AND L2 recall improves on targeted classes at fixed seat count without precision/corroboration regression; FAILURE (falsified) = E-falsifier fires (targeted recall unchanged / overall precision falls / displaced-seat lost unique contribution >= routing gain / model concentration reduces independent corroboration without compensating gain); NOT-DEMONSTRABLE = if the verified seam cannot observe the recall outcome, record not-demonstrable (do NOT claim success). NOISE GUARDRAILS (recall plan §6): precision of final-surviving findings, unsupported/immaterial rate, positive-verification rate, duplicate rate, coordinator drop rate (interpreted alongside precision), output/context cost; raw emitted-finding count is diagnostic only. EVIDENCE TO RETAIN: per-seat seat_routing blocks (L1) + recall measurement with baseline-vs-treatment comparison + noise guardrails (L2) + the fixed-seat-count + class-targeting record. CONDITIONAL BRANCHES NOT FULLY CONCRETIZABLE FROM CODE (named honestly per sharpen discipline): (a) the exact reference-finding corpus / upheld-findings set to measure final-surviving recall against is NOT fixed in code — it is operator/test-corpus-dependent; the recall-observational-validation-protocol card (.local/coordinator/tasks/recall-observational-validation-protocol.json, kept) tracks this methodology, and fixing the corpus is a PRECONDITION for L2 (L2 cannot run until the corpus is fixed); (b) the fixed-seat baseline requires temporarily reverting review-tiers.json leaf_roles to all r1_redundant/block_finder and re-running the cascade — the revert is well-defined but operator-gated (temporary config change, reverted after measurement). NON-GOALS: do NOT activate the 5-leaf R4 reshape (r4_blind_spot stays RESERVED); do NOT change disposition/evidence/aggregation/quorum rules (attention steering ONLY); do NOT bundle defer-017 (panel quorum fixtures — different layer, stays parked under its own reopening condition). Files: NO durable source change by default (this is a live-evaluation study producing observational evidence under tmp/agent-runs/; review-tiers.json + commit-reviewer.md are READ for the mechanism; the pre-E baseline revert is a temporary config swap reverted after measurement). Success criteria: L1 mechanism proven (per-seat metadata verified in a live cascade) AND L2 outcome proven (final-surviving recall improves on contract_drift+data_integrity at fixed seat count without guardrail regression) OR honestly recorded as failure/not-demonstrable per the outcome rules. Validation plan: (1) L1 — live cascade run, capture forwarded seat_routing per leaf, assert seat A block carries specialist fields + seats B/C/D carry generalist fields; (2) L2 — fixed-seat baseline-vs-treatment comparison on a fixed reference-finding corpus (precondition: corpus fixed), measure final-surviving recall on contract_drift+data_integrity + all noise guardrails; (3) report success/failure/not-demonstrable per the outcome rules above (do NOT claim success on L1 alone). |  |
| P2-UPSTREAM-001 | todo | upstream | File upstream opencode PR adding a default for `provider.options.chunkTimeout` — schema slot at provider.ts:111-114 exists, only default missing; fixes the stream-stall-hang class for everyone and removes the per-deployment config burden. | operator | source:operator-promotion · override:operator · transport:defer-008-upstream-pr-default-chunktimeout · trigger:operator-request · studied:2026-07-25 · promoted:2026-08-09. Paired with P2-UPSTREAM-002 (task-await timeout) + the harness backstop. Enabling condition = stall recurrence (external event); filing the upstream PR is operator action. Open Qs: 300000ms global default vs per-provider; chunkTimeout as power-user knob vs safety default. Re-derive the schema slot against upstream main at conversion time (4-tuple premise-recheck). Confirm the default does not regress providers that legitimately emit no bytes for long stretches. |  |
| P2-UPSTREAM-002 | todo | upstream | File upstream opencode PR passing `timeout` into `background.wait` in the task tool (param supported via Effect.timeoutOption at background-job.ts:292-301, but task.ts:317-347 never passes it) — fixes blast radius: parent task-tool await no longer hangs when a child stream stalls. | operator | source:operator-promotion · override:operator · transport:defer-009-upstream-pr-task-await-timeout · trigger:operator-request · studied:2026-07-25 · promoted:2026-08-09. Study Q3 option (d) item 3; this is why the 10h hang reached the parent. Open Qs: right default timeout (must not kill legit long child sessions; configurable); absolute vs idle-based. Re-derive the background.wait timeoutOption signature + task.ts call site against upstream main at conversion time. Confirm the timeout fires cleanly into the existing retry/abort path and does not double-fire with chunkTimeout retry. |  |
| P2-SAFETY-001 | todo | safety,policy | Re-scope the shell-guard git-global-flag command rewrite so opencode L2 sees bare `git <verb>` — reverses the deliberate v0.4.0 detect-only decision (2ac2f41); 4 blockers incl operator safety-policy blessing + execution-semantics decision. Replaces cancelled P2-PERMCFG-003 (O3 refuted). | operator | source:operator-promotion · override:operator · transport:defer-020-shellguard-git-global-rewrite-rescope · trigger:operator-request · studied:2026-08-03 · promoted:2026-08-09. Originally source:review-defer. SOLO slice (own explicit review; do NOT bundle). BLOCKER-1 safety-policy: reverses v0.4.0 detect-only — needs operator/maintainer blessing of a closed Git-global-only grammar. BLOCKER-2 execution-semantics: --no-pager/--paginate/-C are NOT inert (stripping re-engages pager / changes cwd); params.command feeds BOTH permission AND execution — policy decision required. BLOCKER-3 motivating-example mismatch: `git --no-pager --paging=no log` is NOT unblockable (--paging=no unknown/non-strippable); the real recurring multi-flag readonly command needs a telemetry corpus. BLOCKER-4 test seam: no integrated L2-to-execution test exists in the repo; must design before any rewrite is provable. Constraints: parser source-spans NOT tokens.join; original-form forbidden-pattern deny-scan FIRST; never generalize beyond Git globals; 12 `git --no-pager <sub> *` entries at tables.go:93-104 are load-bearing under detect-only. Behavioral-closure requires result:proven via live opencode (not not-demonstrable). Decision-memo target: researches/decisions/2026-08-03-shellguard-git-global-rewrite-rescope.md. |  |
| P2-RELEASE-001 | todo | release,policy | Decide whether high-risk release seams also block on F1 P-a result=unavailable (operator open-question #5) — baseline paCoverageSatisfies stays a STRUCTURAL coverage rule; the high-risk-seam extension layers ON TOP, never by tightening the structural gate. | operator | source:operator-promotion · override:operator · transport:review-defer-f1-unavailable-highrisk-policy · trigger:operator-request · studied:2026-07-25 · promoted:2026-08-09. Originally source:review-defer + source:slice-4-commit-review (UF2 convergent across tier1_a/b/c/d in both review rounds). Operator-owned: open-question #5 in the F1 design memo — build MUST NOT decide unilaterally. Baseline unavailable-needs-limitation rule IS shipped (slice 4); only the high-risk-seam extension is open. If YES: a POLICY-layer check (separate from ValidatePACoverage) refuses an emit/transition at a high-risk seam when coverage rests solely on unavailable + a test. If NO: update the deferral comment in f1_pa.go/f1_pa_gate.go/f1_validator.go (no code change). Constraint: closed-enum coverage semantics unchanged. | researches/decisions/2026-07-25-f1-synthesis-family-and-s2a-topology.md |

## Later

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-DOCS-001 | todo | docs | Author real docs/ai/* operational primitives for this repo (dogfood) so its own AGENTS.md refs resolve — or formally document them as project-authored. |  | dogfood self-consistency |  |
| P2-INSTALL-001 | todo | install | Make `AGENTS.md` composition reachable for a cold consumer agent after `install`. Today `install` writes only `.vh-agent-harness/AGENTS.core.md`; the composed top-level `AGENTS.md` (core+mission) appears only after `.vh-agent-harness/AGENTS.mission.md` exists AND `update` runs. Investigate: seed a minimal mission stub on install, have install run an initial compose, and/or strengthen guide/install next-step messaging. |  | 2026-06-28 surfaced by Slice-1 validation in a throwaway consumer repo. Pre-existing, documented in install next-step #1, but a cold agent reading AGENTS.md right after install finds it missing. See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
| P2-LINEAGE-001 | todo | lineage | Decide whether `lineage.yml` should record per-file ownership labels (overlay_extension/platform_managed/project_owned). Today ownership is enforced at render time only; there is no ownership tag in lineage, so 'verify overlay_extension' is provable only behaviorally (overwrite-resilience), not by reading a label. Design question — scope before implementing. |  | 2026-06-28 surfaced by Slice-1 validation. Behavioral proof works today; this is about agent-inspectability |  |
| P2-DOCTOR-001 | todo | doctor | Reconcile `doctor` 'managed in sync' count with `update` 'managed-overwrite' count — doctor's in-sync baseline appears to exclude overlay_extension files while update includes them, so the counts differ by the number of overlay-rendered files. Either align the baselines or document the difference explicitly in doctor output. |  | 2026-06-28 surfaced by Slice-1 validation. Harmless (0 problems reported) but could make a careful agent second-guess whether an overlay file is tracked |  |
| P2-PERMCFG-001 | todo | permconfig | `internal/permconfig/emit.go` `validate()` does not validate `EditOverride.Decision` values via `validDecision()` (currently only checks `rule.Edit`). |  | v0.2.1 follow-up #3 (DEFER). Safe today: the only consumer is the committer, hardcoded `Allow`. Trigger to fix: an overlay pack introduces `editOverrides`, OR `emit.go validate()` is next touched. Priority: low. |  |
| P2-PERMCFG-002 | todo | permconfig | Remove the retained `stage-message *` allowlist entry (in `internal/permconfig/tables.go` gate group) and its test assertion, now that the corpus BANS the heredoc `stage-message` form. Cross-cuts shell-guard. |  | v0.2.1 follow-up #4. Non-functional today (shell-guard chain-guard refuses any newline-containing command regardless). Schedule for the follow-up that drops the `stage-message` subcommand from `.opencode/scripts/commit-gate.sh`. Priority: medium. 2026-08-31: a downstream adopter certification (TrueAI v0.26.1 adoption, UPDATE 2026-08-31 in `.local/coordinator/reports/scoped-project-engine-allow-seam/2026-08-29-trueai-downstream-interest.md`) independently routed two findings upstream: this stale `stage-message *` gate-group entry AND a core-emitter stage-message deny dedupe request. The dedupe request auto-resolves when this item lands — `emit.go` emits every gate-group command per agent, so removing the `tables.go` gate-group entry removes all ~24 per-agent stage-message emissions in one stroke. Standalone emitter dedupe was REJECTED upstream: those deny emissions are load-bearing for vanilla adopters while the gate-group entry exists. |  |
| P1-DRIFT-003 | todo | drift | F4 — overlay_extension override path fails loudly in `checkManagedDrift` Resolve; optional fix = mirror seamInventory overlay-path extension so doctor and seamInventory agree on the ownership-defaults map. |  | Surfaced during the F3 fix (P1-DRIFT-002). checkManagedDrift builds its ownership defaults from `corpus.CoreOwnershipDefaults()` (CORE only) then `ownership.Resolve(defaults, overrides)`, while seamInventory/computeSeamDrift extends defaults with OVERLAY paths (overlay_extension files). So a project that raises an overlay_extension path via harness-ownership.yml hits an UnknownPathError in doctor's Resolve (the overlay path is absent from the core-only map) => loud FAIL, while seamInventory accepts the same override. This is fail-safe-loud today (no silent corruption) and no consumer is known to override an overlay_extension path, so deferred. Fix shape: have checkManagedDrift extend defaults with active-overlay paths the way seamInventory does, so both surfaces agree. Priority: low. | F4 |
| P2-PERMCFG-004 | todo | permconfig | D-F1 — stage-message stderr/stdout regression test: assert the deprecation notice goes to STDERR only and the staged-payload JSON to STDOUT (RC=0), so a future refactor cannot regress the stream split. |  | Surfaced during P1-DOCS-002 (the stage-message deprecation slice). Its functional test confirmed the split manually but added no durable regression guard. Complements P2-PERMCFG-002 (removing the retained stage-message allowlist entry once the subcommand is dropped). Priority: low. | D-F1 |
| P2-COMMIT-001 | todo | commit | Committer auto-split for mixed backlog+code path sets — committer detects `docs/planning/backlog.md` alongside code paths and splits internally into a code-only acquire + a backlog-only acquire, retrying the backlog one on CAS conflict. |  | Trigger to ship: dogfood shows real friction from manually pre-splitting code-only vs backlog-only handoffs at task boundaries. O1 gate preflight (commit 77dc6f46) already structurally protects code from backlog blob conflicts — the auto-split is convenience (one delegation instead of two), NOT correctness. No G1/lock needed (backlog conflict bubbles to agent for re-read+reapply). Do NOT bundle with P2-GATE-001. Priority: low. |  |
| P2-GATE-001 | todo | gate | G1 — line-level 3-way merge (`git merge-file`) for declared shared paths (initially `docs/planning/backlog.md`) in `commit-gate.sh cmd_commit`, superseding blob-level `read-tree -m -i` for those paths. |  | Deferred behind a validation packet (decision memo `researches/decisions/2026-07-05-commit-gate-shared-file-coupling.md` §G1/Deferred-Work). Packet must cover: duplicate-ID detection, conflict-marker rejection, `normalize-backlog.js --check` coverage, false-conflict rate. `docs/planning/G1-validation-scope.md` (referenced by an old planner brief) was never authored — packet authorship is the first sub-step. Core-wide blast radius (gate hot path on every acquire); do NOT bundle into another slice. Priority: low-medium. |  |
| P2-SECURITY-001 | blocked | security | Enforce the trusted project-scoped projection (replace the raw co-mingled DB as the read surface) — the implementation half of the settled read-isolation policy; GATED on 7 unresolved impl contracts + the raw-DB provisioning seam trace. |  | source:operator-promotion · override:operator · transport:defer-harness-read-isolation-enforcement · studied:2026-08-09 · promoted:2026-08-09 · blocker:7 impl contracts (G1-G7 in read-isolation-policy.md §7) unresolved + raw-DB provisioning seam trace (G1) untraced. POLICY half is DONE (6 binding points settled in the memo); this is the IMPLEMENTATION half. Contracts: G1 raw-DB provisioning ownership · G2 project-identity canonicalization · G3 parent/child session ownership · G4 projection tables/schema (artifact does not exist yet) · G5 multi-project operator-authz allowlist · G6 M1 read-path compat · G7 versioned persistence schema. Do NOT re-open policy; resolve implementation only. permconfig stays capability-routing; shell-guard stays defense-in-depth. | researches/decisions/read-isolation-policy.md |
| P2-REVIEW-001 | blocked | review | Wire `scopecoverage.Validate` into the commit-review approval boundary (F4-A structural completeness, NOT doctor) once 7 prerequisites are specified; `HasGaps()` refusal predicate. |  | source:operator-promotion · override:operator · transport:defer-scopecoverage-validate-gate-attachment-when-wired · studied:2026-08-08 · promoted:2026-08-09 · blocker:7 prerequisites unresolved — (1) versioned declared-scope/disposition schema · (2) acquire-time-exact file→canonical-identity mapping · (3) review-result transport w/ tree+revision binding · (4) deterministic non-model consumer at approval boundary · (5) fail-closed handling · (6) refusal fixtures · (7) activation/ramp rule. Policy decided: WIRE into commit-review as primary host (NOT doctor); F4-A structural-completeness IS a deterministic invariant distinct from semantic review quality. Validator currently INFORMS-only/non-blocking. Residual of defer-019 (closed for the realistic-scope pure-library e2e). | researches/decisions/2026-07-28-success-report-integrity-and-working-tree-stewardship.md |
| P2-RELEASE-002 | blocked | release | Build an executable agent-runtime ceremony harness to observe AUTO-RECOVER recipe #1 behavior (make-build, retry-once, log, no STOP-AND-ASK) by the releaser agent. |  | source:operator-promotion · override:operator · transport:defer-release-agent-runtime-auto-recover-observation · studied:2026-08-08 · promoted:2026-08-09 · blocker:no executable agent-runtime ceremony harness exists — current `verify-release-interaction-contract.js` only checks prompt anchors and never runs the releaser. Wrapper-only half drained in commit e7dd02e (`TestReleaseTag_G0c_StalePathBinaryIgnored_GreenPastG0cAllowsTag`); this is the not-demonstrable agent-runtime residual. G4 guardrail: do NOT claim behavioral-closure result:proven until an end-to-end releaser observation exists. |  |
| P2-REVIEWER-001 | blocked | reviewer | Add merge-layer fixtures exercising per-class quorum (`owner_or_redundant` uphold/downgrade) once a non-owner/non-redundant leaf disagreement becomes observable. |  | source:operator-promotion · override:operator · transport:defer-017-panel-quorum-specialization-fixtures · studied:2026-08-02 · promoted:2026-08-09 · blocker:UPGRADES-TO-BLOCK on (i) a second leaf taking a specialist role OR (ii) the quorum rule changing away from `owner_or_redundant` OR (iii) `tier_cascade.py` consuming `panel.class_ownership`/`panel.quorum` (currently inert; defer-018 watches that layer). lever-E (5c9f370) specialized leaf A + repointed class_ownership to r2_contract_data, but under the current panel (A specialized; B/C/D r1_redundant) the quorum still reduces to 'any leaf raises it with evidence' — fixtures would be non-discriminating today. Do NOT promote on the metadata-attention-steering fact alone. |  |
| P2-TESTING-001 | blocked | testing | Build the full route-not-node / OFF-row behavioral-parity matrix distinguishing 'test exercises the load-bearing path' from 'a test in the right directory passed'. |  | source:operator-promotion · override:operator · transport:defer-behavioral-parity-matrix-full · source:field-report-disposition · studied:2026-07-31 · promoted:2026-08-09 · blocker:recorded/replayable LLM harness needed for the runtime-route half (likely INFEASIBLE in this Go repo — live route = LLM reading the rendered prompt, no Go test can exercise it) + `behavioral_rows` schema addition for the generalized structural-rows half. OFF-row structural seam ALREADY EXISTS at `capability_render_test.go:340` (media-perception pattern). UNION sibling with topology F4-A (declared-scope coverage), NOT covered by it. Maturation trigger: a new opt-in capability ships without behavioral_rows OR a real test-path-coverage recurrence. | researches/decisions/2026-07-23-vh-solara-orchestration-field-report-disposition.md |
| P2-COMMITGATE-001 | blocked | commitgate | Root-cause the ~6h commit-freeze symptom (worktree isolation / G1 lease / same-file tangle) — consciously EXCLUDED from P0-B surfacing; named P1-A in the disposition. |  | source:operator-promotion · override:operator · transport:p2-followup-p1a-worktree-lease-root-cause · source:p2-followup · studied:2026-07-26 · promoted:2026-08-09 · blocker:deterministic reproduction (symptom intermittent — next freeze gives repro) + operator greenlight. P0-B surfacing shipped (a4afe6e8 durable closeout ledger + 6f524bdb no_head_progress/could_not_land vocab + 5495a9ee doctor #19 head-progress WARN) but does NOT prevent the freeze — only makes it visible. Root-cause candidates: worktree-isolation failure (committer B on a stale snapshot while A's HEAD moved), G1 lease/lock starvation (B waits on a lease A never releases across the long review window), or the same-file tangle itself. | researches/decisions/2026-07-23-vh-solara-orchestration-field-report-disposition.md |

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-CI-002 | done | ci | Narrow assurance-convergence adoption (deepseek-harness study debate→planner verdict 2026-08-18): P1 CI workflow-contract tests, P2 stable aggregate CI status, P3 commit-gate lock/cleanup audit (audit-only), P4 postmortem→guardrail convention + decision-record checkpoint. | build | done:2026-08-18 — commit 679172d (code+tests+docs) + this backlog commit. Changed files: internal/ci_contract_test.go (new; 4 TestCIContract_* tests via yaml.v3 — blocking matrix + skip-to-red, render-check's DELIBERATE dryrun+Fail-on-drift pair, release DEFER-evaluator-before-GoReleaser ordering, ci-aggregate needs-completeness incl. reviewer-F2 fix), .github/workflows/go-test.yml (new ci-aggregate job: if:always(), fails on failure/cancelled/skipped, per-dependency summary, B1-vs-B2 kept distinct), docs/ai/postmortem-convention.md (class→mechanism→one-guardrail→verifier, existing records only), docs/checkpoints/2026-08-18-assurance-convergence-adoption.md (adopt-now/trigger-gated-adopt-later/rejected-this-wave). P3 AUDIT-ONLY: commit-gate.sh unchanged — all six audited properties hold (atomic mkdir; identity-bound stale-break; never delete unidentifiable lock; path validation before privileged rm; flock-serialized closeout ledger; cleanup swallowed only post-outcome). Verification: gofmt clean; go vet OK; go test ./... -count=1 32ok/0fail; go test ./internal/ -run TestCIContract_ 4/4 (mutation-checked: if:success() and unwired Fail-on-drift each → RED); make test-js 157/0. RESIDUAL (reviewer DEFER, honest): ci-aggregate hosted live-run not-demonstrable pre-merge — observe first push/PR run (green + one failing-dependency case) BEFORE claiming live-proven; OPERATOR ASK: pin ci-aggregate as the required branch-protection check (retire per-leg pins) after first green hosted run. |  |
| P2-OPS-001 | done | ops,dogfood | Live `/solution-brief` dogfood proving the model invokes Write to `tmp/agent-runs/<alias>/brief.md` — persist-brief is precondition-proven (permission map + softened prompt) but OUTCOME-not-proven; needs an operator-driven live opencode run. | coordination | done:2026-08-16 (coordination session; solution-brief subagent ses_ff60578c6ffe) — OUTCOME PROVEN per this row's own validation plan: one live /solution-brief run (post-review-debt next-actions question); tmp/agent-runs/next-actions-20260816/brief.md created on disk post-run (19452 bytes, 2026-08-16 17:14; gitignored transport by design) AND the return message cited the path; both preconditions already proven stood (opencode.jsonc edit deny + tmp/** allow unchanged since 78f9541; softened prompt directs persist). verdict:proven / result:proven (persist+cite outcome). No /job-cleanup run this session — receipt artifact retained. Prior: source:operator-promotion · override:operator · transport:defer-034-solution-brief-write-dogfood · trigger:operator-request · studied:2026-08-07 · promoted:2026-08-09. Originally source:review-defer (commit-reviewer F1 on the solution-brief prompt-softening slice). Preconditions proven: (a) opencode.jsonc `edit:{'*':'deny','tmp/**':'allow'}` unchanged since 78f9541; (b) the softened prompt directs the orchestrator to persist the consolidated brief to tmp/agent-runs/<alias>/brief.md. NOT proven: the model actually invokes Write + returns the path. Build cannot drive a live opencode run. Validation: drive one /solution-brief run; observe tmp/agent-runs/<alias>/brief.md created post-run AND the return message cites the path → record verdict:proven/result:proven. If the model does NOT write despite the softened prompt → prompt needs further direction (not a permission issue). |  |
| P2-CI-001 | done | ci | Add a full-suite go-test CI job + wire `VH_REQUIRE_LIVE_BRIDGE=1` so a LiveBridge matrix skip is a red signal, not green (no full-suite job exists today). | build | done:2026-08-16 — HOSTED-PROVEN. Commits 0d646d7 (.github/workflows/go-test.yml: matrix node 18.17.1+24.18.0 exact, step-scoped VH_REQUIRE_LIVE_BRIDGE=1, go test ./... -count=1, no -short/continue-on-error) + 0354a9d (push trigger). First hosted run green: https://github.com/vhqtvn/vh-agent-harness/actions/runs/31961195046 (run #1, event push, head 0354a9d, main) — both legs success: go-test (24.18.0) job 95199326005 with 'Full Go suite with required LiveBridge' step success (~98s), go-test (18.17.1) job 95199326020; zero LiveBridge skips by construction (env var converts every skip condition to failure). Blocker drained (job now exists + green); wiring half landed with it; mechanism half previously drained (1f7f493). Prior: source:operator-promotion · override:operator · transport:defer-ci-livebridge-env-var-wiring · studied:2026-08-08 · promoted:2026-08-09 · blocker:no full-suite go-test CI job exists among the 4 `.github/workflows/*.yml` files (docker-validate.yml / release.yml / render-check.yml / rendered-reference.yml are all build-only; rg "go test" = NO MATCHES). Mechanism half drained via commit 1f7f493 (sentinel-fails-when-missing); this card tracks the wiring half — adding a full-suite job is the prerequisite and the env-var wiring rides on it. |  |
| P1-CORE-030 | done | shell-guard | F1: shell-guard over-match denies accept-platform/diff on commit-gate.sh paths | build | done:2026-08-12 (build) — commit 35ca243. shell-guard inert-verb exemption for accept-platform/diff; JS-only grammar, Go test bridges to real JS; 4 gateInspectionCases pass. |  |
| P1-CORE-032 | done | ownership | F2-narrow: root AGENTS.md not in origin-hash set -> doctor blind to composition drift | build | done:2026-08-12 (build) — commit 4483896. agents-composition doctor check (SKIP/WARN/PASS, never FAIL); compose refactored to shared bytes. |  |
| P2-CORE-033 | done | cli | F8: add batch --all/--stale-only to accept-platform | build | done:2026-08-12 (build) — commit 96a572d. --all/--stale-only flags; committer gate caught+resolved doc-vs-code overclaim. |  |
| P2-CORE-034 | done | scripts | F6: dangling test reference in verify-task-registry.js | build | done:2026-08-12 (build) — commit 3282880. scoped dangling test refs as internal/dev-only. |  |
| P2-CORE-035 | done | cli/docs | F7: qualify bare "drifted" token in doctor/diff output | build | done:2026-08-12 (build) — commit da940cf. drifted → drifted-vs-render across diff/doctor/preflight + goldens. |  |
| P2-CORE-036 | done | docs | F5-narrow: stale prose "once Slice-2 retirement CODE lands" | build | done:2026-08-12 (build) — commit 3282880. refreshed stale Slice-2 prose to past tense. |  |
| P1-CORE-031 | done | commit-gate | F3: commit-gate GC reaps in-use protected file via stat-fallback | build | done:2026-08-12 (build) — commit c39d733. _file_age_seconds L171 stat-fallback fixed (`\|\| echo 0` -> `\|\| echo "$now"` => age=0 => retention on stat failure); repairs _protected_uuids + _gate_gc_sweep (L462/L492) reap layers at once. Verified via isolated repro tmp/repro-f3/repro.sh: TEST1+TEST2 RED->GREEN, TEST3+TEST4 stayed GREEN. _lock_age_seconds L159-165 deliberately untouched (separate lock-TTL concern). commit-reviewer APPROVED (high/low, 4/4). Wave 2b unified render still pending. |  |
| P1-SUBSTRATE-001 | done | substrate,lineage | Make apply/lineage provenance truthful under live-write failure. | build | done:2026-08-02 (build) — commit 4f34d45a. lineage-stability fix; live-write failure gates lineage advance + manifest persist via GenerationFullyApplied; behavioral-closure proven; 3 DEFERs. Follow-up from P1-LINEAGE-002 v1.1 (ship-review blocker #1 option-c scope fence). v1.1 added the additive typed `substrate.FileOutcome.WriteState` field + a NARROW `seamApply` manifest-skip gate (failed tracked overlay-skill dest → skip manifest persist, prior byte-intact), but deliberately LEFT `substrate.Apply` return semantics unchanged: a live-write failure still sets WriteFailed+Note and returns nil, and lineage still advances for a generation that may not have fully applied. This row closes the broader gap: define execution-failure aggregation in `substrate.Apply`; decide whether any live-write failure returns an error / stops vs continues / which post-apply side effects run; ensure lineage does NOT advance for a generation that did not fully apply. Coverage: partial failure + lineage stability + CLI install/update behavior + existing proposal/seed side effects. Reuses the typed `FileOutcome.WriteState` field added in v1.1. NOT the F6/F7 defer card (`.local/coordinator/tasks/defer-p1-lineage-002-001.json` — do not touch). |  |
| P1-INTEGRITY-001 | done | integrity,commit-review | Define the canonical declared-scope and coverage-disposition representations for F4-A, then add a deterministic validator that detects missing, ambiguous, extra, and undisposed scope items. Keep the mechanism non-blocking until fixtures establish a reliable equality contract and an explicit approval transition. | build | done:2026-08-02 (build) — commit b914b06. scopecoverage pkg: declared-scope/disposition representations + pure Validate(); 6 fixtures green; non-blocking (INFORMS-only); defer-019. source:decision-2026-07-28-success-report-integrity · Finding A · evidence E-A-PARTIAL-SEAM, E-A-SCOPE-ANCHOR, E-AUTHORITY · studied:2026-07-28. Files likely affected: templates/core/.opencode/agents/commit-reviewer.md; canonical template/source for docs/coordination/CLOSEOUT_TEMPLATE.md; relevant internal/cli/ doctor source+tests if a doctor diagnostic is added; README.agent.md only if the user-visible command or operating contract changes; generated .opencode/ files only through the dogfood render/update path, never by direct editing. Validation plan: (1) fixture declared {a,b} vs reported {a} → incomplete/missing-`b`; (2) declared {a,b} vs {a,b} → pass; (3) undeclared {c} → mismatch reported; (4) fail-fast termination cannot produce complete approval while any declared item lacks a terminal disposition; (5) validator claims structural coverage only, not semantic examination; (6) run the narrow verified repository seam, then `go test ./...`, `go vet ./...`, `gofmt`, and required dogfood render/doctor checks. Hard-gate promotion conditional on canonical scope identity + passing fail-closed fixtures. |  |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-CORE-037 | cancelled | pause-new-work | F4 (pause-new-work overclaim) — REFUTED, not a defect |  | 2026-08-12 cancelled — REFUTED, not a defect; all three consumers wired in HEAD. |  |
| P2-PERMCFG-003 | cancelled | permconfig | Broader git-global-flag normalization redesign (the "O3" option from the v0.2.1 debate): push stripped tokens into `normalizedCommands` so allowlist+routing+deny-re-scan all see bare `git <verb>`, AND remove the now-redundant 12 `git --no-pager <sub> *` config entries (which become inert under that design). Cross-cuts shell-guard. |  | 2026-08-03 cancelled — O3 premise refuted: normalizedCommands cannot affect OpenCode L2; removing the 12 --no-pager entries regresses detect-only. No code/commit. Different command-rewrite redesign deferred in defer-020-shellguard-git-global-rewrite-rescope (draft; requires operator blessing + design blockers). v0.2.1 follow-up #5 (O3). Resolves the multi-flag readonly over-block (`git --no-pager --paging=no log` currently hard-denies under the shipped cde2ac94 design). Needs its own explicit review + scope expansion — do NOT bundle into another slice. Priority: medium. |  |
| P1-CLI-001 | cancelled | cli | Evaluate a release subagent/overlay (auto semver from changes, tag, push) — the consumer ask that surfaced the discoverability gap; possibly ship as an example overlay. |  | 2026-07-01 superseded — delivered by P2-RESOLVER-006 (the `release` overlay pack ships the tag-driven release subagent this evaluate task asked about, as the first embedded overlay pack + reference implementation of the capability-installer overlay integration). |  |
| P1-OVERLAY-002 | cancelled | overlay | Implement the chosen discoverability fixes (A/B/C) from P1-OVERLAY-001; keep templates/core domain-free. |  | 2026-06-28 superseded — decomposed into P1-OVERLAY-003 (Slice 1: A+B+D+E) and P1-OVERLAY-004 (Slice 2: fix C). See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
