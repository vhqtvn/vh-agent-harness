# vh-agent-harness — Backlog

Source of truth for task status — the harness coordination model reads this file.

**Agents:** before substantial work, move the matching row to `in_progress` with
owner + date; on finish move it to `done` recording changed files + verification;
add follow-ups as NEW rows (new ID), never overload an existing task; if blocked,
set `blocked` with the exact blocker and the next decision needed. After any
status change run `/backlog-cleanup` (or `vh-agent-harness exec node
.opencode/scripts/normalize-backlog.js`) to keep the active sections tidy and
archive `done`/`cancelled` rows under `docs/planning/archive/`.

- **IDs:** stable `<phase>-<AREA>-<NNN>`, e.g. `P1-CORE-001`, `P2-API-003`.
- **Statuses:** `todo`, `in_progress`, `blocked` (active) · `done`, `cancelled` (history).
- **Sections:** `Now` (active focus) · `Next` (queued) · `Later` (deferred). Active
  sections hold active statuses only; the normalizer enforces and archives the rest.
- **Columns:** `ID | Status | Area | Task | Owner | Notes | Links` (Notes may carry a `YYYY-MM-DD`).

## Archive Index

Older `done` and `cancelled` history lives under [docs/planning/archive/index.md](archive/index.md) and is meant for on-demand reading instead of auto-loading into the active backlog context.

- No archive files yet.

## Now

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |

## Next

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-CONFIG-001 | todo | config | Wire coordination-hints-lib.js to read product_prefixes from .vh-agent-harness/project.config.json (remove the phase-later TODO). |  | deferred from project.config consumption |  |

## Later

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-DOCS-001 | todo | docs | Author real docs/ai/* operational primitives for this repo (dogfood) so its own AGENTS.md refs resolve — or formally document them as project-authored. |  | dogfood self-consistency |  |
| P1-CLI-001 | todo | cli | Evaluate a release subagent/overlay (auto semver from changes, tag, push) — the consumer ask that surfaced the discoverability gap; possibly ship as an example overlay. |  |  |  |
| P2-INSTALL-001 | todo | install | Make `AGENTS.md` composition reachable for a cold consumer agent after `install`. Today `install` writes only `.vh-agent-harness/AGENTS.core.md`; the composed top-level `AGENTS.md` (core+mission) appears only after `.vh-agent-harness/AGENTS.mission.md` exists AND `update` runs. Investigate: seed a minimal mission stub on install, have install run an initial compose, and/or strengthen guide/install next-step messaging. |  | 2026-06-28 surfaced by Slice-1 validation in a throwaway consumer repo. Pre-existing, documented in install next-step #1, but a cold agent reading AGENTS.md right after install finds it missing. See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
| P2-LINEAGE-001 | todo | lineage | Decide whether `lineage.yml` should record per-file ownership labels (overlay_extension/platform_managed/project_owned). Today ownership is enforced at render time only; there is no ownership tag in lineage, so 'verify overlay_extension' is provable only behaviorally (overwrite-resilience), not by reading a label. Design question — scope before implementing. |  | 2026-06-28 surfaced by Slice-1 validation. Behavioral proof works today; this is about agent-inspectability |  |
| P2-DOCTOR-001 | todo | doctor | Reconcile `doctor` 'managed in sync' count with `update` 'managed-overwrite' count — doctor's in-sync baseline appears to exclude overlay_extension files while update includes them, so the counts differ by the number of overlay-rendered files. Either align the baselines or document the difference explicitly in doctor output. |  | 2026-06-28 surfaced by Slice-1 validation. Harmless (0 problems reported) but could make a careful agent second-guess whether an overlay file is tracked |  |
| P2-VERIFY-001 | todo | verify | Run/encode the permission-pack contract checks (`validateRules()`-equivalent — e.g. a gate-exempt agent must NOT carry a `gate` key) inside the Go `update`/`doctor` (or `overlay new` apply), not only in the operator-run node script. Today `validateRules()` lives only in `node .opencode/sys-scripts/update-opencode-config.js` (templates/core/.opencode/sys-scripts/update-opencode-config.js ~924-948); the Go binary never invokes it, so an active overlay pack can be "valid" per the binary (0 conflicts, doctor healthy) yet FAIL the permission contract on the first real node-script run. This is exactly what surfaced the B-F1 block in Slice 2. Candidate: Go-side validation pass, or a doc note. | build | 2026-06-28 surfaced by Slice-2 (`overlay new`) validation. Pre-existing. See docs/checkpoints/2026-06-28-harness-agent-operability.md (Slice 2 completion) |  |
| P2-CONFIG-001 | todo | config | Stop `node .opencode/sys-scripts/update-opencode-config.js` from diverging the managed `opencode.jsonc` from the corpus render, OR reconcile `doctor`/the corpus render to the node script's canonical form. Two observed manifestations of one root cause: (a) the script drops the `features.backlog` permission line (~7-line deletion diff, pre-existing known gap); (b) for a repo with an active agent-bearing overlay, the script canonicalizes the overlay agent's permission.bash/task blocks (from the overlay's simplified 3-allow form to the full readonly+git_readonly+devSh group render), so `doctor` reports `managed-drift` on `opencode.jsonc` AFTER the node script — meaning README.agent.md's golden-path ordering (update → node script → `doctor healthy`) only holds for core-only repos, not overlay repos. | build | 2026-06-28 (a) pre-existing known gap; (b) surfaced by Slice-2 validation. See docs/checkpoints/2026-06-28-harness-agent-operability.md (Slice 2 completion) |  |

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-OVERLAY-001 | done | overlay | Research + design how to make vh-agent-harness reliably operable by coding agents in consumer repos (overlay discoverability, the customize-opencode conflict, guide/example). Deliverable: ranked fix set — A AGENTS.core.md "extending" section, B /harness command, C `overlay new` scaffolder. | build | 2026-06-28 research resolved → ranked fix set (A–E) + 3 operator-confirmed decisions in docs/checkpoints/2026-06-28-harness-agent-operability.md. Implementation decomposed into P1-OVERLAY-003 (Slice 1) + P1-OVERLAY-004 (Slice 2) | README.agent.md |
| P1-CORPUS-010 | done | corpus | Fix validate-opencode-config.py JSONC parsing (trailing comma + interleaved comments) — unblocked all commits in consumer repos. |  | 2026-06-28 v0.1.3 |  |
| P1-CONFIG-010 | done | config | Stop shipping *.example scaffolds; add `vh-agent-harness example`; consume project.config.json at render (mission/architecture/db). |  | 2026-06-28 v0.1.4 |  |
| P1-DOCTOR-010 | done | doctor | gitignore check warns when runtime-state + __pycache__ dirs aren't ignored. |  | 2026-06-28 v0.1.2/v0.1.4 |  |
| P1-BACKLOG-010 | done | backlog | Seed docs/planning + default the backlog feature on; greenfield profile fallback fixes opencode.jsonc drift. |  | 2026-06-28 |  |
| P1-OVERLAY-003 | done | overlay | Slice 1 (A+B+D+E) — make vh-agent-harness agent-operable: AGENTS.core.md "Extending the harness" section + reason-gated customize-opencode warning (+ add `harness` to command enum ~line 218); new `/harness` command (templates/core/.opencode/commands/harness.md); skill-creator "Where skills live" + init_skill.py overlay target; guide.go overlay step + always-on footer + `_pack-skeleton` under example. Keep templates/core domain-free. | build | 2026-06-28 DONE. What changed (A+B+D+E): always-on `## Extending the harness` section in AGENTS.core.md (anti-patterns + reason-gated customize-opencode warning + overlay one-liner + `guide`/`/harness` pointers); new shipped `/harness` command (templates/core/.opencode/commands/harness.md); skill-creator overlay-first guidance + init_skill.py overlay-target/generated-tree warning; guide.go installed-step file names + always-on `/harness` footer; embed-only overlay-pack skeleton under templates/examples/.vh-agent-harness/overlays/_pack-skeleton/; README.agent.md documents `/harness`. Touch-up: shipped `/harness` now points at `vh-agent-harness example` (ships) instead of non-shipped docs/adoption-examples/web/ (AG-F4); token-name clarity (AG-F2); checkpoint wording (AG-F5); init_skill.py matcher tightened (AG-F3). Key files: templates/core/.vh-agent-harness/AGENTS.core.md, templates/core/.opencode/commands/harness.md, templates/core/.opencode/skills/skill-creator/{SKILL.md,scripts/init_skill.py}, internal/cli/guide.go, templates/examples/.vh-agent-harness/overlays/_pack-skeleton/*, README.agent.md + rendered dogfood outputs (AGENTS.md, .opencode/commands/harness.md, .opencode/skills/skill-creator/*). Verify: go build/gofmt/go vet/go test ./... pass; dogfood `vh-agent-harness update` clean (0 conflicts); `example` lists `_pack-skeleton`; `guide` prints named files + `/harness` footer; domain-free audit clean; commit-reviewer verdict CLEAN/approve. Deferred: fix C (`overlay new`) -> Slice 2, tracked as P1-OVERLAY-004 (Next/todo). See docs/checkpoints/2026-06-28-harness-agent-operability.md | README.agent.md |
| P1-OVERLAY-004 | done | overlay | Slice 2 (fix C) — `vh-agent-harness overlay new <name>` scaffolder with optional `--agent` / `--command` / `--skill` selector and `--dry-run`; register in internal/cli/root.go; embedded domain-free skeleton. Appends `<name>` to `overlays:` in vh-harness-profile.yml (ownership `platform_armed`) — MUST use the schema/reconcile path, NOT a naive text edit. Strict `--dry-run` (prints file manifest + profile diff, writes nothing); validate name collisions and partial packs; unit tests required. New command surface → README.agent.md update. | build | 2026-06-28 DONE (fix C / `overlay new` scaffolder). What changed: new `vh-agent-harness overlay new <name> [--agent \| --command \| --skill <n>] [--dry-run] [--target <dir>]` command — scaffolds an overlay pack (agents/commands/skills unit skeletons + always-on opencode-append.jsonc [active agent wiring when --agent, no-op shell otherwise] + permission-pack.jsonc + callable-graph-snippet.md) and appends <name> to `overlays:` via schema.HarnessProfile{}.AppendOverlay (the same load/marshal path `update` reconciles with — NOT a text edit; platform_armed file stays drift-clean). Strict `--dry-run` writes nothing (prints file manifest + profile before→after diff). Fail-closed: rejects existing pack dir / existing files / bad names / missing .vh-agent-harness. Key files: internal/cli/overlay_new.go (+root.go registration), internal/schema/harness_profile.go (AppendOverlay), templates/overlay-skeleton/{agent,command,skill}.md + corpus.go embed, internal/cli/overlay_new_test.go + agf6_output_assertions_test.go, README.agent.md. AG-F6 follow-up done: guide footer /harness + example _pack-skeleton output assertions (no source defect found). Verify: go build/gofmt/go vet/go test ./... pass; e2e in throwaway (install→overlay new --dry-run writes nothing→overlay new apply→update --dry-run 0 conflicts→update renders agent into .opencode/agents/foo.md + wires opencode.jsonc→doctor HEALTHY→node .opencode/sys-scripts/update-opencode-config.js run from throwaway CWD exits 0 [validateRules green]). Commit-review re-review caught B-F1: buildPermissionPack had emitted gate:"deny" + gateExempt:true in the same location block, which validateRules() rejects (gate deny bleeds into committer subagent). Fixed: gateExempt agents now omit the gate key entirely (only wildcard/readonly/git_readonly/devSh remain). Reproduced the failure on the pre-fix binary (node script EXIT 1: "LOCATION_RULES.foo must NOT have a gate key") then confirmed green post-fix (EXIT 0). Prior e2e overclaim corrected: `update` + `doctor` (both Go) do NOT invoke the node permission script — it is an operator-run, CWD-relative step — so validateRules was never exercised in the first session's e2e. Regression test added (TestOverlayNew_PermissionPackIsValidForActiveAgent). Fold-in advisories: D-F6 (read-error distinguish), C-F3 (stderr warning), D-F3 (dead loop removed), A-F1 (file-content assertions), D-F4 (opencode-append structural parse), A-F3/C-F2/D-F5 (README). Deferred: D-F2 (apply atomicity). domain-free audit clean. See docs/checkpoints/2026-06-28-harness-agent-operability.md | README.agent.md |
| P1-RELEASE-001 | done | release | Cut v0.1.5 once the discoverability fixes land. |  | 2026-06-28 v0.1.5 |  |
| P1-CLI-011 | done | cli | exec: run mutating commands (fix shell-guard double-gate); make `--` optional. |  | 2026-06-27 v0.1.2 |  |
| P1-CLI-010 | done | cli | Seam-aware diff/uninstall/preflight (work on seam installs, not just legacy manifest). |  | 2026-06-27 v0.1.1 |  |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-OVERLAY-002 | cancelled | overlay | Implement the chosen discoverability fixes (A/B/C) from P1-OVERLAY-001; keep templates/core domain-free. |  | 2026-06-28 superseded — decomposed into P1-OVERLAY-003 (Slice 1: A+B+D+E) and P1-OVERLAY-004 (Slice 2: fix C). See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
