# vh-agent-harness — Backlog

Source of truth for task status — the harness coordination model reads this file.

**Agents:** before substantial work, move the matching row to `in_progress` with
owner + date; on finish move it to `done` recording changed files + verification;
add follow-ups as NEW rows (new ID), never overload an existing task; if blocked,
set `blocked` with the exact blocker and the next decision needed. After any
status change run `/backlog-cleanup` (or `vh-agent-harness exec node
.opencode/scripts/normalize-backlog.js`) to keep the active sections tidy and
archive `done`/`cancelled` rows under `docs/planning/archive/`.

- **IDs:** stable `<phase>-<AREA>-<NNN>`, e.g. `P1-CORE-001`, `P2-API-003`.
- **Statuses:** `todo`, `in_progress`, `blocked` (active) · `done`, `cancelled` (history).
- **Sections:** `Now` (active focus) · `Next` (queued) · `Later` (deferred). Active
  sections hold active statuses only; the normalizer enforces and archives the rest.
- **Columns:** `ID | Status | Area | Task | Owner | Notes | Links` (Notes may carry a `YYYY-MM-DD`).

## Archive Index

Older `done` and `cancelled` history lives under [docs/planning/archive/index.md](archive/index.md) and is meant for on-demand reading instead of auto-loading into the active backlog context.

- [2026 Q3](archive/backlog-archive-2026-q3.md) - 14 done / 0 cancelled
- [2026 Q2](archive/backlog-archive-2026-q2.md) - 21 done / 0 cancelled

## Now

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |

## Next

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-SUBSTRATE-001 | todo | substrate,lineage | Make apply/lineage provenance truthful under live-write failure. |  | Follow-up from P1-LINEAGE-002 v1.1 (ship-review blocker #1 option-c scope fence). v1.1 added the additive typed `substrate.FileOutcome.WriteState` field + a NARROW `seamApply` manifest-skip gate (failed tracked overlay-skill dest → skip manifest persist, prior byte-intact), but deliberately LEFT `substrate.Apply` return semantics unchanged: a live-write failure still sets WriteFailed+Note and returns nil, and lineage still advances for a generation that may not have fully applied. This row closes the broader gap: define execution-failure aggregation in `substrate.Apply`; decide whether any live-write failure returns an error / stops vs continues / which post-apply side effects run; ensure lineage does NOT advance for a generation that did not fully apply. Coverage: partial failure + lineage stability + CLI install/update behavior + existing proposal/seed side effects. Reuses the typed `FileOutcome.WriteState` field added in v1.1. NOT the F6/F7 defer card (`.local/coordinator/tasks/defer-p1-lineage-002-001.json` — do not touch). |  |
| P1-PERM-002 | todo | permission | Harden the shared `inspectorAllowIf` / `ALLOW_IF_INSPECTOR_FULL` / `INSPECTOR_FULL` builder for the infra-lifecycle rules (apt-install-ad-hoc, user-group-mutation, ssh-host-key-bypass, scp-upload). |  | Follow-up validated during the `f88d29f` commit (P1-PERM-001). Same gap class as the git-lane fix: (1) the shared inspector set still contains the `command` executor builtin, so `command apt-get install x` / `command usermod ...` / `command ssh ...` / `command scp ...` would carve the rule out; (2) it carries the narrower L104 chain-guard missing `\n`/`\r`/`<(`/`>(`. VALIDATED as a real reachable FP (not theoretical): during the `f88d29f` commit, `user-group-mutation` false-positive'd on the literal `usermod` token in commit-message prose (committer worked around it byte-identically via adjacent-string concatenation). Fix shape mirrors the git lane: per-rule inspector sets excluding `command`, upgrade the shared chain-guard to the full 11-op set, with its own LiveBridge regression matrix for apt/usermod/ssh/scp. Open question: `command` exclusion may need to be per-rule because the shared set serves non-mutation rules too — confirm whether `command` can stay shared or needs per-rule narrowing. |  |
| P1-CONFIG-001 | todo | config | Wire coordination-hints-lib.js to read product_prefixes from .vh-agent-harness/project.config.json (remove the phase-later TODO). |  | deferred from project.config consumption |  |
| P2-PERMCFG-003 | todo | permconfig | Broader git-global-flag normalization redesign (the "O3" option from the v0.2.1 debate): push stripped tokens into `normalizedCommands` so allowlist+routing+deny-re-scan all see bare `git <verb>`, AND remove the now-redundant 12 `git --no-pager <sub> *` config entries (which become inert under that design). Cross-cuts shell-guard. |  | v0.2.1 follow-up #5 (O3). Resolves the multi-flag readonly over-block (`git --no-pager --paging=no log` currently hard-denies under the shipped cde2ac94 design). Needs its own explicit review + scope expansion — do NOT bundle into another slice. Priority: medium. |  |
| P1-INTEGRITY-001 | todo | integrity,commit-review | Define the canonical declared-scope and coverage-disposition representations for F4-A, then add a deterministic validator that detects missing, ambiguous, extra, and undisposed scope items. Keep the mechanism non-blocking until fixtures establish a reliable equality contract and an explicit approval transition. |  | source:decision-2026-07-28-success-report-integrity · Finding A · evidence E-A-PARTIAL-SEAM, E-A-SCOPE-ANCHOR, E-AUTHORITY · studied:2026-07-28. Files likely affected: templates/core/.opencode/agents/commit-reviewer.md; canonical template/source for docs/coordination/CLOSEOUT_TEMPLATE.md; relevant internal/cli/ doctor source+tests if a doctor diagnostic is added; README.agent.md only if the user-visible command or operating contract changes; generated .opencode/ files only through the dogfood render/update path, never by direct editing. Validation plan: (1) fixture declared {a,b} vs reported {a} → incomplete/missing-`b`; (2) declared {a,b} vs {a,b} → pass; (3) undeclared {c} → mismatch reported; (4) fail-fast termination cannot produce complete approval while any declared item lacks a terminal disposition; (5) validator claims structural coverage only, not semantic examination; (6) run the narrow verified repository seam, then `go test ./...`, `go vet ./...`, `gofmt`, and required dogfood render/doctor checks. Hard-gate promotion conditional on canonical scope identity + passing fail-closed fixtures. |  |
| P1-INTEGRITY-002 | todo | integrity,closeout,release | Update the canonical prompt and closeout guidance so "green" distinguishes full verification, targeted/smoke verification, and clean transition state. Preserve existing B1/G0 and B2/G0b as separate controls. Explicitly state that release requires global cleanliness while ordinary commit-gate integrity is exact-slice based and must tolerate unrelated concurrent dirt. |  | source:decision-2026-07-28-success-report-integrity · Findings B1, B2 · evidence E-BEHAVIORAL-CLOSURE, E-FALSE-GREEN, E-B1-G0, E-B2-G0B, E-COMMIT-LEDGER · studied:2026-07-28. Files likely affected: canonical source for the AGENTS.md behavioral-closure and testing guidance; canonical template/source for docs/coordination/CLOSEOUT_TEMPLATE.md; gated-commit documentation or skill source; release-readiness documentation; researches/decisions/2026-07-28-success-report-integrity-and-working-tree-stewardship.md; README.agent.md only if the operating or command contract changes. Validation plan: (1) docs clearly distinguish B1 from B2; (2) targeted tests cannot be labeled full green; (3) missing revision-bound full-build evidence maps to `inconclusive`/`not-demonstrable`; (4) release docs match G0 and G0b behavior in scripts/release-tag.sh; (5) commit-gate docs do not introduce a global-clean-tree requirement; (6) behavioral-closure wording continues to state the token is a declaration, not proof; (7) run documentation/render consistency checks plus `go test ./...`, `go vet ./...`, `gofmt`. |  |
| P1-RELEASE-002 | todo | release | Add tag-ready→tag HEAD-drift detection to `release-tag.sh` so a concurrent landing between tag-ready and tag cannot silently re-target the manifest pin. |  | source:p2-followup · defer-release-tag-ready-head-hygiene · studied:2026-07-30 · fired (scripts/release-tag.sh touched 2026-07-30; no drift guard present). Real gap from the v0.15.0 incident: tag target 40e5f74 while a concurrent study commit c3dcf29 landed on main, requiring operator history surgery to preserve the intended pin-to-M. Current scripts/release-tag.sh (~42KB) has NO tag-ready→tag HEAD-drift detection (grep for tag.ready/intervening/pin/pin-to-M returns empty). Files: scripts/release-tag.sh (repo root, project-owned) + templates/overlays/release/agents/releaser.md (canonical source for release-agent guidance). Validation plan: (1) fixture where HEAD advances between the tag-ready assertion and tag application → release-tag.sh detects and fails/pins rather than silently re-targeting; (2) happy path where HEAD is unchanged → proceeds normally; (3) the released tag object's tree matches the manifest-pinned commit, not whatever main pointed at mid-window; (4) `go test ./...`, `go vet`, `gofmt`; (5) dogfood render/doctor if templates touched. |  |
| P2-DOCS-002 | todo | docs | Refresh README.agent.md closeout-status vocabulary to include the S1/S2 terms (`rebased_refused`, `reviewed_tree_diverged`, `head_at_acquire`) added by 4bc821e and reconcile the remaining `cas_conflict` contrast references. |  | source:review-defer · defer-readme-agent-rebased-refused-status-vocab · studied:2026-07-30 · fired (README.agent.md grown since the card was authored; vocab section now at lines 937-955). AGENTS.md mission rule: stale README.agent.md is a bug. The section documents `committed`/`no_head_progress`/`could_not_land` but OMITS `rebased_refused`/`reviewed_tree_diverged`/`head_at_acquire` (the S1/S2 closeout terms from 4bc821e). NOTE README still references `cas_conflict` as a contrast term at lines 949/954 (cas_conflict is no longer emitted by the gate — a separate surface from defer-011's trigger set, tracked here for vocab coherence). File: README.agent.md (project-owned repo-root, single copy). Validation plan: (1) README closeout-status vocab lists ALL current gate statuses including the S1/S2 trio; (2) `cas_conflict` references reconciled/removed or correctly scoped; (3) render/doctor consistency if templates touched; README.agent.md is repo-root so go test/vet/gofmt unaffected unless code referenced. |  |
| P2-M1-001 | todo | m1 | Run the M1 natural-miss classification in-lane under floored `exec-sandbox --sandbox=strict` — `researcher` executes `tmp/commit-study/m1_classify.py` (reads the DB copy `mode=ro`, writes the neutral dump to `tmp/`), classifies the 71 sampled misses, and reproduces the (e) genuine-capability share (already established in the source packet as 27/69 = 39.1%). The in-lane dogfood payoff of the exec-sandbox Level-B grant (no python3 wildcard, no build handoff). |  | source:decision-payoff · memo:researches/decisions/2026-07-29-exec-sandbox-level-b-mode-floor.md §Payoff · source-packet:researches/sources/2026-07-28-m1-natural-miss-mechanism-study.md · studied:2026-07-30. The exec-sandbox Level-B decision promoted `researcher` to Level B (kernel-contained arbitrary read-code); its §Payoff documents that once `researcher` has the floored grant it runs `m1_classify.py` under `vh-agent-harness exec-sandbox --sandbox=strict` (DB copy read `mode=ro`, neutral dump to `./tmp/`), classifies, and computes the (e) share — no python3 wildcard grant, no build handoff. This row tracks that in-lane run as work; it is currently UNTRACKED. Files: no durable source change by default (the run produces gitignored `tmp/commit-study/` outputs — sensitive `m1_classify_input.json` stays in tmp, only neutral `m1_classify_output.json` is promotable; the script already exists gitignored). Success criteria: `researcher` runs the full Leg-2 pipeline in-lane under the strict floor (no wildcard, no build handoff) and the (e) share reproduces — the dogfood proof (a read-only agent runs arbitrary read-code safely in-lane) is demonstrated. Validation plan: (1) `vh-agent-harness exec-sandbox --sandbox=strict -- python3 tmp/commit-study/m1_classify.py` runs in-lane from the researcher profile (DB copy `mode=ro`; neutral dump to tmp/); (2) the strict floor contains every side effect (write-outside-tmp → EACCES, network denied) — re-confirm the P5 bypass is blocked under the floored config; (3) the (e) genuine-capability share + Wilson 95% CI is computed and reconciled with the source-packet headline (27/69 = 39.1%, CI [28.5%, 50.9%]); (4) persist-guard held (sensitive input stays gitignored; only neutral output promotable); (5) `go test ./...`/`go vet`/`gofmt` unaffected (no code change); doctor HEALTHY. |  |

## Later

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-DOCS-001 | todo | docs | Author real docs/ai/* operational primitives for this repo (dogfood) so its own AGENTS.md refs resolve — or formally document them as project-authored. |  | dogfood self-consistency |  |
| P2-INSTALL-001 | todo | install | Make `AGENTS.md` composition reachable for a cold consumer agent after `install`. Today `install` writes only `.vh-agent-harness/AGENTS.core.md`; the composed top-level `AGENTS.md` (core+mission) appears only after `.vh-agent-harness/AGENTS.mission.md` exists AND `update` runs. Investigate: seed a minimal mission stub on install, have install run an initial compose, and/or strengthen guide/install next-step messaging. |  | 2026-06-28 surfaced by Slice-1 validation in a throwaway consumer repo. Pre-existing, documented in install next-step #1, but a cold agent reading AGENTS.md right after install finds it missing. See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
| P2-LINEAGE-001 | todo | lineage | Decide whether `lineage.yml` should record per-file ownership labels (overlay_extension/platform_managed/project_owned). Today ownership is enforced at render time only; there is no ownership tag in lineage, so 'verify overlay_extension' is provable only behaviorally (overwrite-resilience), not by reading a label. Design question — scope before implementing. |  | 2026-06-28 surfaced by Slice-1 validation. Behavioral proof works today; this is about agent-inspectability |  |
| P2-DOCTOR-001 | todo | doctor | Reconcile `doctor` 'managed in sync' count with `update` 'managed-overwrite' count — doctor's in-sync baseline appears to exclude overlay_extension files while update includes them, so the counts differ by the number of overlay-rendered files. Either align the baselines or document the difference explicitly in doctor output. |  | 2026-06-28 surfaced by Slice-1 validation. Harmless (0 problems reported) but could make a careful agent second-guess whether an overlay file is tracked |  |
| P2-PERMCFG-001 | todo | permconfig | `internal/permconfig/emit.go` `validate()` does not validate `EditOverride.Decision` values via `validDecision()` (currently only checks `rule.Edit`). |  | v0.2.1 follow-up #3 (DEFER). Safe today: the only consumer is the committer, hardcoded `Allow`. Trigger to fix: an overlay pack introduces `editOverrides`, OR `emit.go validate()` is next touched. Priority: low. |  |
| P2-PERMCFG-002 | todo | permconfig | Remove the retained `stage-message *` allowlist entry (in `internal/permconfig/tables.go` gate group) and its test assertion, now that the corpus BANS the heredoc `stage-message` form. Cross-cuts shell-guard. |  | v0.2.1 follow-up #4. Non-functional today (shell-guard chain-guard refuses any newline-containing command regardless). Schedule for the follow-up that drops the `stage-message` subcommand from `.opencode/scripts/commit-gate.sh`. Priority: low. |  |
| P1-DRIFT-003 | todo | drift | F4 — overlay_extension override path fails loudly in `checkManagedDrift` Resolve; optional fix = mirror seamInventory overlay-path extension so doctor and seamInventory agree on the ownership-defaults map. |  | Surfaced during the F3 fix (P1-DRIFT-002). checkManagedDrift builds its ownership defaults from `corpus.CoreOwnershipDefaults()` (CORE only) then `ownership.Resolve(defaults, overrides)`, while seamInventory/computeSeamDrift extends defaults with OVERLAY paths (overlay_extension files). So a project that raises an overlay_extension path via harness-ownership.yml hits an UnknownPathError in doctor's Resolve (the overlay path is absent from the core-only map) => loud FAIL, while seamInventory accepts the same override. This is fail-safe-loud today (no silent corruption) and no consumer is known to override an overlay_extension path, so deferred. Fix shape: have checkManagedDrift extend defaults with active-overlay paths the way seamInventory does, so both surfaces agree. Priority: low. | F4 |
| P2-PERMCFG-004 | todo | permconfig | D-F1 — stage-message stderr/stdout regression test: assert the deprecation notice goes to STDERR only and the staged-payload JSON to STDOUT (RC=0), so a future refactor cannot regress the stream split. |  | Surfaced during P1-DOCS-002 (the stage-message deprecation slice). Its functional test confirmed the split manually but added no durable regression guard. Complements P2-PERMCFG-002 (removing the retained stage-message allowlist entry once the subcommand is dropped). Priority: low. | D-F1 |
| P2-COMMIT-001 | todo | commit | Committer auto-split for mixed backlog+code path sets — committer detects `docs/planning/backlog.md` alongside code paths and splits internally into a code-only acquire + a backlog-only acquire, retrying the backlog one on CAS conflict. |  | Trigger to ship: dogfood shows real friction from manually pre-splitting code-only vs backlog-only handoffs at task boundaries. O1 gate preflight (commit 77dc6f46) already structurally protects code from backlog blob conflicts — the auto-split is convenience (one delegation instead of two), NOT correctness. No G1/lock needed (backlog conflict bubbles to agent for re-read+reapply). Do NOT bundle with P2-GATE-001. Priority: low. |  |
| P2-GATE-001 | todo | gate | G1 — line-level 3-way merge (`git merge-file`) for declared shared paths (initially `docs/planning/backlog.md`) in `commit-gate.sh cmd_commit`, superseding blob-level `read-tree -m -i` for those paths. |  | Deferred behind a validation packet (decision memo `researches/decisions/2026-07-05-commit-gate-shared-file-coupling.md` §G1/Deferred-Work). Packet must cover: duplicate-ID detection, conflict-marker rejection, `normalize-backlog.js --check` coverage, false-conflict rate. `docs/planning/G1-validation-scope.md` (referenced by an old planner brief) was never authored — packet authorship is the first sub-step. Core-wide blast radius (gate hot path on every acquire); do NOT bundle into another slice. Priority: low-medium. |  |

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P2-REL-001 | done | release,release-gate | Widen release DEFER manifest scope to all firing cards (option b) + runbook complement. | build | done:2026-08-02 (build). source:decision-memo · memo:researches/decisions/2026-08-02-defer-liveness-provenance-scope-divergence.md · resolves option (b) widen manifest + (c) runbook complement · external-study=release-relevant · confidence:MEDIUM · studied:2026-08-02. Authorized-but-not-landed slice: update releaser/release-readiness agent policy so EVERY firing card the release-prep enumerator surfaces gets an explicit, committed disposition (today the manifest commits dispositions only for `source:review-defer`-tagged cards; other firing cards are surfaced-but-undisposed). Go gate UNTOUCHED. Files likely affected: .vh-agent-harness/overlays/harness-dogfood/agents/harness-release-readiness.md (policy: manifest ALL firing cards, not just source:review-defer); templates/overlays/release/agents/releaser.md (releaser ceremony policy); internal/cli/harness_dogfood_render_test.go:411 (replace the `"source:p2-followup exclusion"` assertion with the widened-scope assertion); ceremony runbook docs (document `VH_HARNESS_DEFER_OVERRIDE_IDS` escape hatch + "do NOT `rm` transport cards to clear the gate"). NON-GOALS (explicit): do NOT touch the Go defer-liveness gate (`checkDeferLiveness`/`loadLivenessCards`); do NOT add a source filter to `loadLivenessCards`; do NOT narrow silencer-immunity; the `.js → .mjs` rename of `check-defer-triggers.js` is a SEPARATE lower-priority slice (track independently, not here). Validation plan: (1) `go test ./...` (esp. `TestHarnessDogfoodRender*` + the render assertion at harness_dogfood_render_test.go:411 now asserting widened scope); (2) `gofmt -l .`; (3) `go vet ./...`; (4) `vh-agent-harness doctor` HEALTHY; (5) `make update --dry-run` confirms no platform_managed drift. Gated on the memo commit landing (happening concurrently). DONE 2026-08-02 (build): option (b) widen-manifest + (c) runbook complement LANDED. Changed: .vh-agent-harness/overlays/harness-dogfood/agents/harness-release-readiness.md (provenance-scope policy block widened to ALL sources incl source:external-study=release-relevant-frequently-disclose; evidence-cmd candidate phrase widened; new "Go defer-liveness gate recovery (VH_HARNESS_DEFER_OVERRIDE_IDS)" subsection: non-destructive override + do-not-rm rule + two-surface distinction); templates/overlays/release/agents/releaser.md (matching "Go defer-liveness gate recovery" section); internal/cli/harness_dogfood_render_test.go:411 (assertion replaced: "source:p2-followup exclusion"→"external-study release-relevant (widened scope)" pinning source:external-study, a token the pre-widening doc lacked); README.agent.md (concise two-gate + all-sources + override/do-not-rm cross-reference); rendered mirrors .opencode/agents/{harness-release-readiness,releaser}.md + .vh-agent-harness/lineage.yml + rendered-outputs.json regenerated via make update. Go gate UNTOUCHED (checkDeferLiveness/loadLivenessCards/release_gate.go/claim.go — non-goal preserved). Verification: go test ./... GREEN (TestHarnessDogfood_ReleaseReadinessCarriesG7Gate PASS source+mirror incl new assertion; TestDeferRecurrence_* incl _LiveRepoIsClean + _ClearsViaOperatorOverride PASS; TestDeferLivenessGate_* incl _LiveRepoIsClean PASS); gofmt -l . empty; go vet ./... clean; go build ./... exit 0; vh-agent-harness doctor HEALTHY (0 problems/0 warnings; dev-stale-embed PASS); make update (2 managed-overwrite, no platform_managed drift) + update --dry-run idempotent (204 managed byte-identical). NOT committed — routed to coordinator for committer (split-commit: backlog row separate from code/docs). |  |
| P1-MEMORY-001 | done | memory,claims,release | Recurrence signature + dedup enforcement (resolves defer-002 S3). | build | done:2026-07-31 (build) — Recurrence-signature + dedup-enforcement feature COMPLETE (6 staged slices, spec memo efa53fb). Two-level identity: explicit `recurrence_id` (exact-defect key) + versioned `symptom_class_id` (symptom class). Synchronous producer dedup (N reports w/ one recurrence_id → 1 canonical defer entry carrying N observations; no spawned cards). Doctor advisory diagnostics added (#21 `recurrence-state`). Release-gate fail-closed enforcement via doctor #12 on unacknowledged recurrence (`count > last_acknowledged_count`); manifest ack-pair forward-compat. Commits: 7c7c295 (Slice 1 contract) / 86ca29b (Slice 2 derivation) / 1eca045 (Slice 3 producer-dedup) / cacce766 (Slice 4 doctor) / 5056596 (Slice 5 release-gate) / 50c10e3 (Slice 6a migration tests) / 95c51f2 (Slice 6b docs). Dedup crux PROVEN at every layer (write path Slice 3, release-gate Slice 5). Backward-compat preserved throughout (legacy cards → effective identity=task_id; v1 manifest entries without ack fields pass; no auto-hash). Verification: go test/gofmt/go vet green; normalize-backlog.js --check exit 0; verify-task-card-schema.py pass. Open follow-ups (tracked, not done): defer-018 (gate-wire Python validator into CI — jsonschema dep wrinkle), defer-recurrence-toctou-race (concurrency integrity, bounded by release ceremony), defer-recurrence-ack-min-multimatch-test, defer-recurrence-doctor-rundoctor-integration-test. | efa53fb |
| P2-SUBSTRATE-002 | done | substrate | Wire the existing `verify-task-card-schema.py` validator into a gate (`make test` / CI) so task-card schema violations are caught automatically, not only on manual invocation. | build | done:2026-07-31 (build) — Resolved by ARCHITECTURAL CORRECTION, not the original wire-in framing: the Python `verify-task-card-schema.py` (jsonschema Draft7Validator + `pip install jsonschema` dep) was a standalone-Python outlier in a Go repo's gate. PORTED to a Go binary subcommand `vh-agent-harness task-card validate [<file>]` (noun-verb, mirrors `recurrence dedup`): pure logic in new `internal/taskcard/` (hand-rolled bounded draft-07 subset engine — type/enum/const/required/properties/additionalProperties/pattern/minimum/minLength/minItems/format(date-time)/items/allOf/anyOf/if-then-else — NO new Go dep) + cross-field ack-pair guard `recurrence_count >= last_acknowledged_count`; CLI bridge `internal/cli/taskcard.go`. Schema sourced from embedded `corpus.CoreFS` (drift-free). Gate = a Go test (`internal/taskcard/validator_test.go`) that ports all 20 Python fixtures (6 ok / 13 schema-reject / 1 ack-reject) + 7 extras (additionalProperties at 3 levels, nested evidence/aliases shape) → runs in `go test ./...` with NO Python, NO pip. Python files DELETED (source + rendered mirror); live refs updated (diagnostics.go comment, this row); `internal/memory/recurrence.MalformedBlock` RETAINED (doctor's runtime advisory subset, complementary). Verification: go test/gofmt/go vet green; normalize-backlog.js --check exit 0; `make update` (templates/core Python deletion) re-rendered clean. Resolution path chosen over the wire-in framing because a pip dep must not live in a Go repo's gate. |  |
| P1-DEFER-LIVENESS-001 | done | release,safety-gate,defer-liveness | Release-boundary defer-liveness gate (F4-C): doctor #12 release-diff-recurrence predicate + release-prep manifest enumerator; refuses the tag while any open defer with a fired `path_touched` target in `PRIOR_TAG..HEAD` lacks a disposition. | build | source:case-study-§4.3 · memo:researches/decisions/2026-07-30-defer-liveness-release-gate.md · crosswalk:69e0104→09fe9eb · studied:2026-07-30. DONE 2026-07-30 (commit 09fe9eb, recorded by memo 5dd2c3c5). F1-D1: 16 `draft` DEFER cards had fired `path_touched` targets in `v0.18.0..HEAD` and would ship without a forced verdict. Two contradiction classes MERGE'd into doctor #12 (released-claim `69e0104` + release-diff-recurrence `09fe9eb`); architecture (iii) BOTH — doctor-live = load-bearing tag refusal via G0c; manifest = committed CI-verifiable disposition record, now mechanically populated via the `--mode=release-prep` enumerator. Changed files: internal/cli/release_gate.go, internal/memory/claims/claim.go, .opencode/scripts/check-defer-triggers.js (+ template source templates/core/.opencode/scripts/check-defer-triggers.js), scripts/release-tag.sh, internal/cli/release_gate_test.go. Verification: `go test ./internal/cli/ -run TestDeferRecurrence` (9 tests, refuse→clear PROVEN across 3 disposition surfaces [closed status / manifest entry / operator override] + silencer immunity [.md card blocks]); `go test ./...`, `go vet`, `gofmt`, `vh-agent-harness doctor` HEALTHY; commit 09fe9eb. **DORMANCY-NOTE:** Implemented; recurrence refusal becomes active when an about-to-release note exists. Ordinary `doctor` runs remain advisory by design. Open v1 boundaries (fog cards bypass; glob/dir triggers COLD) captured by spawned DEFER cards. | 09fe9eb |
| P1-GATE-003 | done | gate | S1 (review-time) commit-scope integrity — forward `head_at_acquire` through the committer→commit-reviewer handoff and repoint the leaf review diff off bare `HEAD` onto the acquire-time anchor; a concurrent committer moving HEAD can no longer pull an unrequested dirty/concurrent file into reviewed scope. | build | source:decision-memo · memo:researches/decisions/2026-07-27-commit-scope-integrity.md (d87e6f7d) · studied:2026-07-28. DONE 2026-07-28 (ses commit-scope-integrity-reframe, commit 4bc821e, co-landed with S2=P1-GATE-004). Touch sites: committer.md step 3 forwards head_at_acquire verbatim from acquire output JSON; commit-review.md passes the anchor; commit-reviewer.md + commit-reviewer-{a,b,c,d}.md consume it and repoint the leaf diff to `git diff <head_at_acquire> <tree_hash>`. Invariant: unrequested dirty/concurrent file never enters reviewed scope. Test: TestCommitGate_ReviewDiffBase PASS (crux subtests: acquire_emits_head_at_acquire_anchor, reviewed_tree_excludes_unrequested_dirty_file, acquire_anchor_excludes_concurrent_phantom_files). go test ./... fully green. Builds on P1-GATE-002 (which added head_at_acquire RECORDING). | 4bc821e |
| P1-GATE-004 | done | gate | S2 (commit-time, fail-closed) commit-scope integrity — when the CAS commit path would commit a merged tree ≠ the reviewed tree (e.g. a disjoint concurrent commit lands mid-review), REFUSE with status `rebased_refused` (reason `reviewed_tree_diverged`) and require re-acquire, instead of silently substituting the merged tree. | build | source:decision-memo · memo:researches/decisions/2026-07-27-commit-scope-integrity.md (d87e6f7d) · studied:2026-07-28. DONE 2026-07-28 (ses commit-scope-integrity-reframe, commit 4bc821e, co-landed with S1=P1-GATE-003). Touch sites: commit-gate.sh adds the rebased_refused refuse path (after write-tree, before commit-tree) + closeout-ledger logging of the terminal refusal; committer.md step 4 routes rebased_refused/could_not_land to re-acquire. Invariant: committed_tree==reviewed_tree OR refusal. Test: TestCommitGate_CloseoutLedger PASS (crux subtest: rebased_refused_on_disjoint_concurrent_commit + ledger-persistence subtests). Dogfooded live: this commit was itself refused 3× by S2 (disjoint concurrent commits 03ca41d/ec34c6b/5f3fb17 each landed mid-review) before landing on the 4th acquire — committed_tree==reviewed_tree held end-to-end. go test ./... fully green. Builds on P1-GATE-002 closeout ledger. | 4bc821e |
| P1-GATE-002 | done | gate,doctor | P0-B (HEAD-progress / `no_head_progress` / `could_not_land` commit-failure surfacing) — adopted-gate-shaped in disposition §4.2 but NEVER STARTED (F1-class untracked adoption). Three sub-items, gate/doctor ACT + coordinator reads (§5). |  | source:adopted-disposition · disposition:researches/decisions/2026-07-23-vh-solara-orchestration-field-report-disposition.md §4.2 (the 2026-07-26 addendum item 1) · rc:RC3 · studied:2026-07-26. The disposition is the ONLY place P0-B's corrective machinery is named: the NEW signal vocab (`no_head_progress`/`could_not_land`/`could_not_progress`) is 0 matches in code; the per-session `head_at_acquire` (commit-gate.sh:698-952) already exists as the prerequisite sub-item 1 extends. Three sub-items: (1) committer closeout records post-commit HEAD alongside the existing per-session `head_at_acquire`; (2) `doctor` synchronous HEAD-staleness check WARNing when N successful committer closeouts precede an unchanged HEAD; (3) extend commit-gate status vocabulary with `no_head_progress` / `could_not_land`. Files: `.opencode/scripts/commit-gate.sh`, `internal/cli/doctor.go`, templates/core closeout/contract templates. Validation: new status emitted on a no-progress acquire→commit; doctor WARN on synthetic N-closeouts-unchanged-HEAD; go test + commit-reviewer on the slice. Near-term mitigation for the ~6h commit-freeze symptom (Pattern 4 same-file tangle); does NOT address root cause (P1-A worktree/G1/lease, long-term). NOT the unrelated visual-parity "P0-B" in researches/sources/2026-07-24-tree2-rewrite-visual-parity-addendum.md (different namespace). DONE 2026-07-26 (ses p1-gate-002-p0b): 3 slices landed via gated-commit — A=`a4afe6e8` (durable closeout ledger `.git/commit-gate/closeouts.log` JSON-lines, flock-serialized append+trim, count-cap GC; post-commit HEAD recorded at both commit-success sites), B=`6f524bdb` (`no_head_progress`/`could_not_land` status vocab; merge-fail sites emit `could_not_land`, gate now emits ZERO `cas_conflict`; README status-vocab bullet), C=`5495a9ee` (doctor check #19 `head-progress`: WARN on N=3 `post_commit_head` flatline across successful closeouts, env `COMMIT_GATE_STALE_HEAD_THRESHOLD`; SKIP greenfield; never FAIL). Files: templates/core/.opencode/scripts/commit-gate.sh (+rendered .opencode pair), internal/cli/commit_gate_closeout_ledger_test.go (new), internal/cli/doctor_head_progress.go (new), internal/cli/doctor_head_progress_test.go (new), internal/cli/doctor.go, README.agent.md. Verified: `go test ./...` + `gofmt` + `go vet` clean; behavioral-closure PROVEN (crux #1 `TestCommitGate_CloseoutLedger/could_not_land_on_merge_conflict` — gate emits `could_not_land` + ledger record carries `post_commit_head`==unchanged HEAD; crux #2 `TestDoctorHeadProgress_Integration` — flatline→WARN section, greenfield→SKIP section); doctor HEALTHY with managed-drift+dev-stale-embed PASS (rendered byte-equals templates/core via transitivity). WARN-only authority per §5 (doctor INFORMs, coordinator reads; never exit-code change). Cheap surfacing mitigation — does NOT fix root cause (P1-A worktree/lease; follow-up card filed). 3 review DEFERs filed as follow-up cards (cas_conflict doc-drift; no_head_progress status-logic; crux could_not_land post_commit_head assertion). |  |
| P1-LINEAGE-002 | done | lineage,render,seam,overlay | Detect & report/prune rendered `.opencode/skills/<name>/` dirs whose overlay source was removed. | build | 2026-07-15 REOPENED-then-reverified (v1.1, build/lineage-orphans): v1 commit 136c79f was prematurely marked done; /ship-review returned needs-changes with 3 blockers (#1 manifest provenance untruthful under live-write failure — `Apply` has no write-error signal, so the rendered-outputs manifest persisted even when a tracked overlay-skill dest failed its live write; #2 directory-level cleanup guidance risked removing active sibling files in a mixed dir; #3 boolean source checker false-positive'd a transient/unreadable source as a definite orphan). FIXED in v1.1 commit 0047142 (same report-only scope, NO prune): #1 additive typed `substrate.FileOutcome.WriteState` (not_attempted/succeeded/failed) + `seamApply` manifest-skip gate on failed tracked overlay-skill dests (prior manifest byte-intact, stderr warning; non-skill failures do NOT gate; `substrate.Apply` return semantics UNCHANGED — broader truthfulness deferred to P1-SUBSTRATE-001); #2 file-level reporting (`DestinationPath`) + file-level removal advice in README/migration; #3 tri-state `CheckSource` present/missing/indeterminate (only `fs.ErrNotExist`=missing; transient/permission=indeterminate→warn+skip; incl. a direct project-local-pack probe so `OpenPackFor`'s stat-error swallow cannot false-positive as missing). `internal/lineage/lineage.go` untouched (S1-only invariant). Self-verified `go test ./...`/gofmt/go vet green; commit-reviewer APPROVED 4/4; awaiting coordinator `/ship-review` re-review. --- ORIGINAL premature-done v1 note (superseded by v1.1, kept for audit): report-only orphan detection shipped — NEW `internal/renderstate/` pkg + `.vh-agent-harness/rendered-outputs.json` manifest (separate from lineage.yml); seam loads prior manifest + Compare (dry-run+live) attaching "Preserved orphan skill(s)" notices to `update`/`update --dry-run`; definite orphan = source MISSING (not deselected); pack-deselection + project-added dirs NOT flagged; NO deletion/prune in v1. Tests: renderstate unit suite (incl. non-normalized-prior key hardening) + 8 seam lifecycle tests. README.agent.md + `templates/migrations/v0.10.0.md` (credits TrueAI; supersedes misleading v0.9.0 rollback line on re-render removing skills). Code commit 136c79f. go test/gofmt/go vet green. Originally 2026-07-14 reported by TrueAI (vh-agent-harness v0.9.0). Bug: removing an overlay skill source (`.vh-agent-harness/overlays/<pack>/skills/<name>/`) then re-running `vh-agent-harness update` leaves the previously-rendered `.opencode/skills/<name>/` live, unreported, and invisible to `update --dry-run`. Orphaned rendered skills stay auto-discoverable (SKILL.md auto-loaded), drift from canonical sources, and can contradict newly-adopted core skills (reporter's case: removed overlay `tdd`/`diagnosing-bugs` left rendered copies contradicting new core `tdd-loop`/`debugging-loop`); required manual `rm -rf` + commit to clear. ROOT CAUSE (confirmed): `.vh-agent-harness/lineage.yml` records only template/copier/render metadata (no per-render file/dir manifest); seam-apply (`internal/cli/seam.go` seamApply@126, renderSeamStaging@~384, seamClassifierWithOverlays@642) builds `overlayFiles` from LIVE overlay paths only, so a removed source simply drops out of `overlayFiles` → never classified → left untouched (renderer's deliberate conservative non-deleting posture). DESIGN QUESTION (blocks build): (a) WHERE to persist a rendered-outputs manifest — `lineage.yml` as bare paths (COLLIDES with the S1-only invariant: `internal/lineage/lineage.go` `AssertLineageOnly` + `forbiddenTopLevel` reject S2/S3 keys) vs a separate manifest file vs `substrate.ApplyReport`; (b) report-only orphan list under `--dry-run` vs explicit `--prune`/`prune` command vs auto-prune; (c) provenance mechanism to distinguish a true orphan (rendered from a now-gone overlay/core source) from an operator-added project skill dir, since `.opencode/skills/<name>/` is also a legitimate project-owned location — required to avoid deleting operator content. NEXT STEP: `/solution-brief` on the design question. Precedent: P1-SEC-001 (TrueAI commit-gate bypass) — same reporter-credit + migration-note pattern applies (note credited in `templates/migrations/v0.4.2.md`). NON-NEGOTIABLES for the eventual fix: README.agent.md MUST update in the SAME change for any new flag/orphan-reporting surface; ownership is the safety contract (preview with `--dry-run`); `templates/core` must stay domain-free; `go test ./...`, `gofmt`, `go vet` must pass; the renderer's conservative non-deleting posture is deliberate; a migration note is needed under `templates/migrations/` for the release carrying the fix (reporter on v0.9.0). Adjacent open rows: P2-LINEAGE-001, P2-DOCTOR-001. |  |
| P2-SKILLS-004 | done | skills | Make harness `AGENTS.md` testing section localizable (dogfood finding). | skill-craft-import | 2026-07-15 done. Committed f1fcaa7. Stripped prescriptive pytest/tests{unit,integration,e2e}/ testing boilerplate from AGENTS.core.md; replaced with universal testing discipline + S1 seam-map authority + absence-contract pointer. Decision memo: researches/decisions/2026-07-14-skill-s2-promotion-gate.md. | templates/core/.vh-agent-harness/AGENTS.core.md, commit f1fcaa7 |
| P2-SKILLS-001 | done | skills | Promote tdd-loop core skill (red→green→refactor in-loop, seam localization, S1 absence-contract) | skill-craft-import | 2026-07-14 done. Committed f56b964 (SKILL.md +84, references/seam-localization.md +81). S2 gate satisfied via TrueAI overlay pilot (validated in-loop refactor + seam discipline). Design decisions: D1A in-loop refactor (canonical Beck TDD, overturns mattpocock in-review stance); D3 localization authority-honesty + AGENTS.md co-localization. Decision memo: researches/decisions/2026-07-14-skill-s2-promotion-gate.md. | commit f56b964 |
| P2-SKILLS-002 | done | skills | Promote debugging-loop core skill (build-red-first + reproduce→minimise→hypothesise→instrument→fix→regress + D2C downgrade/handoff escape) | skill-craft-import | 2026-07-14 done. Committed c52268a (SKILL.md +110, references/red-signal-recipes.md +66, references/downgrade-protocol.md +90). S2 satisfied via VH-Solara forward pilot — D2C escape validated against their Firefox/WebRender GPU/thermal heat saga (mask-image gradient defect under real GPU load); the MEDIUM-conf-on-wording hold resolved when the pilot returned positive signal on the specific uncertain design. All FIVE guardrails fire (human-observation-never-promoted-to-deterministic-red; do-not-continue-theorizing-after-downgrade; diagnostics-export packages handoff; bgshell-job only for non-GPU probes; END loop after handoff). The `human-observed \| non-deterministic \| not agent-runnable` triple is the label TAXONOMY the five guardrails operate on — NOT a sixth guardrail (use FIVE as the count). Statistical-sample serial ×50 red correctly stayed in-loop and was NOT downgraded (downgrade-protocol.md:46-49). S2 core discipline previously satisfied via TrueAI pilot (step-0 absence-contract + step-7 post-mortem). Decision memo: researches/decisions/2026-07-14-skill-s2-promotion-gate.md. Non-blocking follow-up: P2-SKILLS-003 (red-signal-recipes.md fast-property vs slow aggregate reds). | commit c52268a |
| P2-SKILLS-003 | done | skills | Clarify `red-signal-recipes.md` "fast" property vs legitimate slow predeclared-aggregate reds. | skill-craft-import | 2026-07-14 done. Committed c5e9cec. The pre-specified carve from the VH-Solara forward pilot landed: blessed the predeclared-aggregate red shape (predeclared threshold + reproducible count + no cheaper seam + bgshell-hosted) into the "fast" property + "slow" anti-pattern so a validated slow aggregate red is not falsely flagged. SKILL.md guardrails + `downgrade-protocol.md:46-49` unchanged (the statistical-sample red correctly stays in-loop today). Consistent — the carve structurally excludes the flaky-race downgrade shape. Decision memo: researches/decisions/2026-07-14-skill-s2-promotion-gate.md. | templates/core/.opencode/skills/debugging-loop/references/red-signal-recipes.md, commit c5e9cec |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-CLI-001 | cancelled | cli | Evaluate a release subagent/overlay (auto semver from changes, tag, push) — the consumer ask that surfaced the discoverability gap; possibly ship as an example overlay. |  | 2026-07-01 superseded — delivered by P2-RESOLVER-006 (the `release` overlay pack ships the tag-driven release subagent this evaluate task asked about, as the first embedded overlay pack + reference implementation of the capability-installer overlay integration). |  |
| P1-OVERLAY-002 | cancelled | overlay | Implement the chosen discoverability fixes (A/B/C) from P1-OVERLAY-001; keep templates/core domain-free. |  | 2026-06-28 superseded — decomposed into P1-OVERLAY-003 (Slice 1: A+B+D+E) and P1-OVERLAY-004 (Slice 2: fix C). See docs/checkpoints/2026-06-28-harness-agent-operability.md |  |
