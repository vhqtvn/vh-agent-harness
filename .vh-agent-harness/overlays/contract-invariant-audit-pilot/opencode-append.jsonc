// contract-invariant-audit-pilot overlay pack — skills-only pilot for the
// contract/invariant audit capability (S1 overlay, advisory / INFORMS only).
//
// This pack contributes a skill only (skills/contract-invariant-audit/) plus a
// deterministic discovery/accounting helper. It wires NO agent, NO command, NO
// permission surface, and NO gate. The helper runs under the existing broad
// `vh-agent-harness *` allow (same path the skill-creator validator uses), so
// no permission addition is required here. This file is a no-op JSONC shell so
// the pack is well-formed for discovery; a skills-only pack may legitimately
// contribute nothing to opencode.jsonc.
{}
