{
  "schema_version": 1,
  "release_base": { "kind": "tag", "value": "v0.15.1" },
  "evaluated_commit": "ec0b2e9e7d71feb357ee70dd2f67ec07df3b316d",
  "evaluated_tree": "6741c34d1ccc276ebf1aaa26c3bf1fa3970fed3e",
  "manifest_parent_commit": "ec0b2e9e7d71feb357ee70dd2f67ec07df3b316d",
  "reconciliation": {
    "status": "confirmed",
    "scope": "Release arc: v0.15.1 (advisory release_base, derive-on-read via gitLatestTag excluding --release-version) through evaluated_commit (intended release v0.16.0). The v0.15.1..evaluated_commit arc is the v0.16.0 MINOR feature arc and carries real consumer-facing behavioral change (this is NOT a docs-only arc): the F1 synthesis-envelope CLI (slices 1-5: vocabulary + validator, R1 cross-lane join producer, R3 repair-routing fork producer, P-a counter-evidence producer + coverage gate, ValidatedF1Emit + F2 consumer interface + F1/F2 doctor audit), the F2 rendering family (slices 1-10: F2 ingest gate, immutable canonical sidecar persistence, deterministic MD projection + pair-level persistence coordination, P-c headline-layer salience, P-a decision-request table, R5 operator-synthesis durable binding, P-b evidence-grade media provenance slot, R1-derived operator-synthesis streak scanner, F2 doctor structural pair-consistency audit, F2 instructions shipped into templates/core/, and the first real F2 artifact), the F3 design-readiness system (slices 1-7: pure F3 validator, BUILD-READY task-card gate + b-F1 top-level envelope digest + canon amendment, BUILD-READY approved-plan gate sharing the F3 validator, dispatch freshness backstops for ready->working + plan dispatch, authoring + reviewer surfaces, and dogfood regeneration of the F3 rendered corpus), the behavioral-closure pilot (verdict/crux gate + motivation-check), the dev stale-embed guard (refuses `vh-agent-harness update` on divergent source checkouts carrying both corpus.go and templates/core/), coordinator defer-liveness hardening (defer-003: coordinator-adoption marker at the updateCoordinationTask chokepoint closing the §4.3 fail-open), memory identity hardening (collision-safe claims task_id), and bounded help/migration forward-path + make-update caveat. Go green was verified at evaluated_commit by the build agent: `go test ./...` (all ok), `go vet ./...` (clean), `go build ./...` (clean), and `gofmt -l .` (empty) all pass. The authoritative release-mode evaluator (`node .opencode/scripts/check-defer-triggers.js --mode=release --since v0.15.1 --release-version v0.16.0`) disclosed NO records and blocked on NO records for this arc (disclosures=[], disclose_ids=[], blocking_ids=[], accepted_overrides=[], refusals=[], advisories=[]); the single records[] entry below is the canonical non-release-relevant disclosure carried forward unchanged from prior arcs. Release ceremony topology: this manifest commit M is a manifest-only immediate child of evaluated_commit (the staged working-tree diff is ONLY .vh-agent-harness/release-defer-dispositions.json). At tag time (HEAD=M) the freshness handshake invariants hold: HEAD^ (relative to M) == evaluated_commit == manifest_parent_commit == ec0b2e9e7d71feb357ee70dd2f67ec07df3b316d AND tree(HEAD^) == evaluated_tree == 6741c34d1ccc276ebf1aaa26c3bf1fa3970fed3e AND `git diff --name-only HEAD^..HEAD` == [.vh-agent-harness/release-defer-dispositions.json] only. The release-readiness specialist's artifact .vh-agent-harness/release-readiness-pass.json is a pre-existing working-tree change intentionally left uncommitted by this manifest step (owned by the forward releaser ceremony; out of scope for the manifest-only diff). Release mode is manifest-authority-only; the authorized release wrapper and CI recheck always read this committed manifest. release_base.value is advisory (derive-on-read via gitLatestTag excluding --release-version); kind stays operator-attested. records[] dispositions are operator-attested and unchanged by the releaser's SHA recompute.",
    "zero_records_confirmed": false
  },
  "records": [
    {
      "defer_id": "defer-p1-lineage-002-001",
      "release_relevance": "no",
      "disposition": "disclose",
      "metadata_state": "invalid",
      "summary": "P1-LINEAGE-002 deferred coverage (F6/F7) — conditional on a future prune phase.",
      "reason": "Not part of the current release. Retained for audit with unsupported legacy trigger metadata (the source card's trigger grammar uses a '||' separator, a directory operand, and a non-predicate feature: term, which the strict release-mode predicate parser rejects). Re-evaluate only when a prune phase is pursued or renderstate/seam is touched for deletion logic. The canonical no+disclose+invalid disposition lets the release proceed while disclosing the conditional follow-up and its invalid metadata state.",
      "source_ref": ".local/coordinator/tasks/defer-p1-lineage-002-001.json",
      "studied_at": "2026-07-15T00:00:00Z",
      "reviewed_at": "2026-07-24T00:00:00Z",
      "override": null
    }
  ]
}
