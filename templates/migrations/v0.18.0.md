# Migration: v0.18.0

## Summary
- **Release class:** MINOR (semver compatible). The `v0.17.0..HEAD` arc (9
  commits) is a **commit-gate observability + release-ceremony contract**
  release. It carries **4 `feat`** commits (`a4afe6e`, `6f524bd`, `5495a9e`,
  `b852408`), **1 `fix`** commit (`6a99269`), **zero `!:` subjects**, and **no
  `BREAKING CHANGE:` footers** → MINOR bump; not patch (real consumer-facing
  behavioral additions: a durable closeout ledger, new closeout-status
  vocabulary, a new doctor check, and an encoded operator-interaction
  contract), not major (no breaking change — every edit is additive). No
  profile, ownership-class, capability, or runtime change is required; the
  default consumer's `opencode.jsonc` is byte-identical unless an active
  overlay alters it. Last tag = `v0.17.0`; intended version = `v0.18.0`.
- **Upgrade path:** binary self-update, then re-render the corpus. Fully
  automatic for the default consumer. The durable closeout ledger, the new
  status vocabulary, the doctor head-progress check, and the
  operator-interaction contract surfaces ship in the embedded Go binary and
  the embedded corpus (re-rendered by `update`). `templates/core/` remains
  domain-free (token-only; no adopter/overlay identifier in any source or
  rendered file). Source-builders rebuilding from this tag are unaffected (no
  Go toolchain bump).
- **Risk:** low. The durable closeout ledger and status vocabulary are
  additive observability on the existing gated-commit path (no gate decision
  changes; `committed` remains the success status every normal acquire→commit
  path emits). The doctor head-progress check (#19) is a WARN-only INFORM that
  never returns FAIL and never changes the exit code (greenfield SKIP on
  fresh checkouts, exactly like dev-stale-embed #15). The operator-interaction
  contract compiles a correction into enforceable invariants/prompts at every
  release-flow surface; it removes the "shall I cut now?" confirmation-round
  anti-pattern after a green gate but does not weaken any STOP-AND-ASK
  condition. The new standalone verify script
  (`verify-release-interaction-contract.js`) is a structural text-contract
  guard, not a runtime behavioral proof.

## What changed (consumer-visible only)
| area | change | ships-via | class |
|------|--------|-----------|-------|
| **commit-gate durable closeout ledger** (`a4afe6e`) | The gated-commit closeout now records a durable, append-only JSON-lines ledger at `.git/commit-gate/closeouts.log` alongside the existing per-session `head_at_acquire`. The transient session meta is reclaimed on commit-success, so the new doctor head-progress check (#19, `5495a9e`) needs a record that survives that cleanup. Each entry records `uuid` / `acquired_at` / `head_at_acquire` / `post_commit_head` / `status` / `branch` / `ts`. A count-cap GC (`COMMIT_GATE_CLOSEOUT_LOG_MAX`, default 200; tail-trim) runs under a dedicated flock on `closeouts.lock`; on flock-absent hosts the append is unlocked (PIPE_BUF-atomic) and trim is skipped entirely (no race; ledger correct but unbounded). This slice records `committed` closeouts only; the new status vocab (`6f524bd`) and the doctor check (`5495a9e`) land in slices B/C. | Go binary (the closeout helper lives in `internal/`) + `update` (the rendered `commit-gate.sh` is regenerated from `templates/core/`) | non-breaking (additive observability; `committed` remains the success status on every normal acquire→commit path; no gate decision changes) |
| **commit-gate closeout-status vocabulary** (`6f524bd`) | The commit-gate status vocabulary gains two first-class closeout statuses, distinct from `cas_conflict`, both recorded into the durable ledger: **`could_not_land`** (emitted on the 3-way-merge content-tangle path — the same-file conflict the CAS merge cannot reconcile; replaces `cas_conflict` at the `merge_failed` + `write_tree_failed` sites, recorded with `post_commit_head=current_head`) and **`no_head_progress`** (emitted on the commit-SUCCESS path when the branch HEAD after `update-ref` equals the reference the commit was built on; structurally unreachable via normal acquire→commit because `commit-tree` always mints a new OID and `no_changes` guards no-op trees — reserved forward-compatible vocab for the genuine post==pre fault edge). `cas_conflict` now specifically means concurrent HEAD MOVEMENT the retry loop handles (and never terminates there — it retries to success or error). The parented success site computes `closeout_status` (`committed` | `no_head_progress`) and drives BOTH the ledger record and the JSON output; callers still see `committed` on every normal path. | Go binary + `update` (rendered `commit-gate.sh` from `templates/core/`) | non-breaking (additive vocabulary; `committed` is unchanged on every normal path; `no_head_progress` is structurally unreachable; `could_not_land` is a clearer name for an existing failure site) |
| **doctor head-progress HEAD-staleness WARN check #19** (`5495a9e`) | New synchronous doctor check (#19) that reads the durable closeout ledger (`.git/commit-gate/closeouts.log`, `a4afe6e`) and WARNs when the last N (default 3, env `COMMIT_GATE_STALE_HEAD_THRESHOLD`) successful closeouts all recorded the SAME `post_commit_head` — a flatline signalling commits may be recording as successful without advancing HEAD. Authority is INFORM-via-WARN only: it NEVER returns FAIL, never changes the exit code, never gives the coordinator authority. Greenfield — not a git work tree, ledger missing/unreadable, or fewer than N successful closeouts — is SKIP, fail-open-safe (consumers / fresh checkouts see nothing), exactly like dev-stale-embed #15. Malformed ledger lines are skipped (append-only best-effort). The gitdir is resolved via `git rev-parse --git-dir` to handle worktrees. README.agent.md is updated to list #19 and "all 18 checks" → "all 19 checks". | Go binary (the check lives in `internal/`) | non-breaking (WARN-only INFORM; never FAIL; greenfield SKIP; no exit-code change) |
| **operator-interaction release contract** (`b852408`) | Compiles the operator's correction — "once the operator INITIATES a release, the ceremony must run end-to-end WITHOUT confirmation rounds; a green gate = proceed" — into enforceable invariants/prompts at every release-flow surface. Adds **Invariant 8** to the releaser agent prompt ("Operator initiation authorizes the standard ceremony; do not run confirmation rounds") under the absolute-invariants spine. Adds the canonical **"Operator-interaction contract (release ceremony)"** subsection to `README.agent.md` as the process rule: STOP-AND-ASK only on the six named conditions (RED gate / wrapper refuse; DEFER `refuse`/`override-required`; ambiguous version; note content decision the arc doesn't answer; irreversible AND non-standard action; pending blocker); NEVER ask to confirm the standard next ceremony step, post-release cleanup named in the initiating instruction, anything a green gate already authorized, or bundled secondary tasks (pre-authorized in order). Core rule: operator initiation of a release IS the authorization for the standard end-to-end ceremony. Aligns the readiness reporter `handoff_to_releaser` discipline so a clean `ready:yes` is itself the handoff authorization (operator initiation IS the authorization; the reporter still stops on the six STOP-AND-ASK conditions). | `update` (the release overlay's `releaser.md` is re-rendered from `templates/overlays/release/agents/`) for consumers selecting the release overlay; `README.agent.md` is hand-maintained (not rendered) | non-breaking (encodes a correction into enforceable surfaces; removes a confirmation-round anti-pattern after a green gate; does not weaken any STOP-AND-ASK condition) |
| **release-interaction-contract structural verify script** (`6a99269`) | New domain-neutral standalone verify script `verify-release-interaction-contract.js` (sibling to `verify-f3-authoring-surfaces.js` / `verify-no-unrendered-paths.js` / `verify-no-source-tree-only-paths.js`). Asserts the releaser contract (STOP-AND-ASK, NEVER-ASK, confirmation-round, operator-initiation) directly, applies a structural handoff-binding rule (any agent carrying `handoff_to_releaser` must bind it to `authorized_by:"operator-initiation"` and name the confirmation-round anti-pattern) without naming any project-specific reporter, and scans every rendered agent for the superseded `approved_by_human` / "human explicitly approves" phrasing. Header states honestly it is a STRUCTURAL text-contract guard, not a runtime behavioral proof. Not wired into Makefile/doctor/release-prep (standalone sibling-verifier convention). | `update` (re-rendered from `templates/core/`) | non-breaking (additive standalone verifier; not wired into any gate) |
| **v0.17.0 follow-up batch** (`6a99269`) | Three independent follow-up fixes from the v0.17.0 release-correction arc: (1) the callable-graph snippet's readiness→releaser handoff edge label is rewritten to reflect the corrected operator-interaction contract (`authorized_by:"operator-initiation"`; no confirmation round) instead of the stale "a human explicitly approves" label; (2) the v0.13.0 erratum gains a third section covering the false "update re-renders README.agent.md" claim (README.agent.md is hand-maintained, not rendered); (3) the release-interaction-contract verify script above. Render bookkeeping (lineage.yml render ID + rendered-outputs.json) is mechanical output of `make update`. | `update` (callable-graph snippet + verify script re-rendered from `templates/core/`); the v0.13.0 erratum doc is dev-local | non-breaking (descriptive-label correction + additive verifier + dev-local erratum doc) |

## How to migrate (automated)
```bash
vh-agent-harness self-update            # pull the new binary (v0.18.0)
vh-agent-harness version                # expect: 0.18.0 (<label>)
vh-agent-harness update --dry-run       # ownership-safe preview
vh-agent-harness update                 # applies platform_managed + active overlay_extension
vh-agent-harness doctor                 # lint the result
```

## What `update` handles for you
- Re-renders the updated `commit-gate.sh` (from `templates/core/`) whose Go-binary side now records the durable closeout ledger with `post_commit_head`, emits the `no_head_progress` / `could_not_land` status vocabulary, and computes `closeout_status` on the parented success path.
- Ships the new doctor head-progress WARN check #19 in the Go binary (no template to re-render — the check reads `.git/commit-gate/closeouts.log` at doctor time; greenfield SKIP on fresh checkouts).
- Re-renders the release overlay's `releaser.md` (from `templates/overlays/release/agents/`) carrying Invariant 8 (operator initiation authorizes the standard ceremony) — for consumers selecting the release overlay.
- Re-renders the new `verify-release-interaction-contract.js` standalone verify script and the corrected callable-graph snippet (from `templates/core/`).
- No profile, ownership-class, capability, or runtime change; the default consumer's `opencode.jsonc` is byte-identical unless an active overlay alters it.

## Watch-outs
1. **The closeout ledger is a `.git` internal.** It lives at `.git/commit-gate/closeouts.log` and is created on the first gated commit after this release. It is never committed (`.git` is not tracked) and is per-clone. A fresh checkout / consumer render starts with no ledger and the doctor head-progress check SKIPs greenfield — this is correct, not a regression.
2. **Source-checkout repos (this dev/dogfood repo).** Source-checkout consumers rebuilding locally MUST use `make update` (the `Makefile` target rebuilds the binary first), NEVER a bare `vh-agent-harness update` — a bare update would silently overwrite in-progress `.opencode/` work with a stale embedded corpus (the stale-embed guard refuses a live `update` when the embedded corpus differs from `templates/core/`). GoReleaser rebuilds for the published artifact regardless.
3. **Four non-blocking DEFER disclosures** (operator visibility; no consumer action required). `defer-013` (release_relevance=yes + disclose + valid): the P1-GATE-002 Slice B crux test verifies the `could_not_land` JSON + ledger status but does not assert the ledger record's `post_commit_head == unchanged branch HEAD` — an outcome-assertion gap, not a behavioral defect (the status is correctly emitted; fail-closed-safe). `defer-014` (release_relevance=yes + disclose + valid): the closeout-ledger flock-absent non-Linux fallback is structurally sound but not exercised under CI (Linux has flock; defense-in-depth). `defer-release-readiness-attestation-separation` (release_relevance=yes + disclose + valid): the releaser permission edge structurally allows self-attestation of G1–G5; non-blocking THIS release because the pass artifact is authored by the separate `harness-release-readiness` reporter (not the releaser) — the v0.15.0 self-attestation failure mode does not recur. `normalizer-archive-companion-tension` (release_relevance=yes + disclose + valid): pre-existing backlog-split design tension re-surfaced by the `commit-gate.sh` touch; the two-commit landing is a working workaround. Two informational disclosures (`defer-readme-defer-liveness-check-doc`, `defer-release-override-sha-doc-drift`) re-fire on file traffic but were resolved pre-v0.17.0. One retained invalid card (`defer-p1-lineage-002-001`, release_relevance=no + disclose + invalid) has a legacy `||`-chain trigger grammar that was not met for this arc.

## Verification commands
```bash
vh-agent-harness version                # expect: 0.18.0 (<label>)
vh-agent-harness doctor                 # expect: HEALTHY, 0 problems
```

## Rollback
Reversibility: binary downgrade + re-render. Install a prior `vh-agent-harness` binary (e.g. `v0.17.0`), then `vh-agent-harness update` to re-render the corpus from that binary's embedded corpus. No data migration; no irreversible state. The `.git/commit-gate/closeouts.log` ledger is a best-effort append-only diagnostic — leaving it in place after a rollback is harmless (the prior binary does not read it); removing it is also harmless (greenfield SKIP). Source-checkout consumers rolling back MUST use `make update` against the prior tag (the `Makefile` target rebuilds first), not a bare `update`.

## Non-consumer changes
The `v0.17.0..HEAD` arc also carries: `145546b` docs(research) recording the P0-B post-implementation vocab asymmetry (could_not_land live + tested vs no_head_progress structurally unreachable); `16d3a88` docs(backlog) P1-GATE-002 done; `100680a` docs(backlog) archive companions for the P1-GATE-002 normalization; `f36e189` chore(release) the v0.18.0 DEFER manifest rebind ceremony (the manifest-only commit bound to `145546b`, re-bound to the artifact commit in this release ceremony). None are consumer-visible behavior. The defect class and the operator-interaction contract are documented in `docs/case-study/2026-07-22-unverified-claims-at-lossy-boundaries.md` (§4.7 operator corrections are specification).
## Errata for v0.13.0

## What was wrong

The v0.13.0 migration note (`templates/migrations/v0.13.0.md`) made three
false claims: two about the ownership and seeding of the
`.vh-agent-harness/release-defer-dispositions.json` manifest (sections 1–2),
and one about `README.agent.md` being a rendered artifact (section 3).

### 1. Ownership class — "seeded `platform_managed`" (line 12)

**Claimed (line 12, table row for commit `38c5c47`):** the manifest is
"seeded `platform_managed`" — i.e. the renderer seeds it from a template
and reconciles it on every `update`, the same lifecycle as the evaluator
script.

**Actual behavior:** no template exists for
`.vh-agent-harness/release-defer-dispositions.json` (confirmed: zero matches
under `templates/`). The file is fully UNMANAGED by the renderer — never
seeded on install, never reconciled by `update`, never in `lineage.yml` /
`rendered-outputs.json`. It is runtime-committed by the releaser agent as
the single manifest-only commit M in the release ceremony. This is
structurally identical to the coordinator-adoption marker
(`.vh-agent-harness/coordinator-adoption.json`) introduced in v0.16.0:
git-tracked, unmanaged, runtime-created, never renderer-seeded.

### 2. Placeholder handshake SHAs (line 30)

**Claimed (line 30, "What `update` handles for you"):** "Seeds the
`release-defer-dispositions.json` manifest template for fresh installs of
the release overlay (schema v1; the seed carries placeholder handshake SHAs
that the releaser recomputes at each release-prep)."

**Actual behavior:** there is no seed/template at all. The live file
carries REAL handshake SHAs (e.g. `evaluated_commit` /
`manifest_parent_commit` = the actual commit the releaser evaluated;
`evaluated_tree` = the actual tree SHA). These are never placeholders —
they are recomputed by the releaser at each release-prep from the real
commit graph, then committed. A fresh install has NO manifest until the
releaser creates it during the first release ceremony.

### 3. README.agent.md "re-rendered by update" claim (lines 5, 21, 27, 60)

**Claimed (lines 5, 21, 27, and 60 of `templates/migrations/v0.13.0.md`):**
`vh-agent-harness update` re-renders `README.agent.md` (specifically its
release-ceremony section). Line 21 states it directly ("re-renders
README.agent.md release ceremony"); line 27 restates it under "What `update`
handles for you" ("Re-renders `README.agent.md` with the single
release-authority model"); lines 5 and 60 each state or imply the same — that
`README.agent.md` is a rendered artifact whose release-ceremony section is
reconciled by `update`.

**Actual behavior:** `README.agent.md` is HAND-MAINTAINED, not a rendered
artifact. There is no `README.agent.md.tmpl` under `templates/core/`, and the
file is absent from the corpus ownership map (it carries no render ownership
class — not `platform_managed`, not `overlay_extension`, not any other). A
plain render may only overwrite `platform_managed` (and active
`overlay_extension`); everything else is preserved. `update` therefore
PRESERVES `README.agent.md` — it does not re-render it. Changes to
`README.agent.md` land via direct `docs(readme):` commits, exactly as this
repo's non-negotiable rule ("`README.agent.md` must always be up-to-date ...
any change ... MUST be reflected there in the same change") requires: the
maintainer edits it directly, the renderer never touches it.

**F3 class (§4.3):** released doc claiming unexecuted behavior. The note
asserts `update` performs a re-render the renderer structurally cannot
perform (no template exists to render from). An operator who trusts the note
would expect `update` to refresh `README.agent.md`'s release-ceremony section
after a binary self-update; it does not, and the section stays as the last
direct edit left it. This is the same defect class as sections 1–2 (a released
note describing a non-existent render/seeding path), now extended to a third
artifact.

## Impact

Low for release correctness. The release gate reads the committed manifest
via `HEAD:<path>` (committed blob) and validates the handshake SHAs against
the real commit graph regardless of how the file was created. The false
"seeded `platform_managed`" / "placeholder SHAs" claims describe a
non-existent template path; they do not affect the fail-closed gate
behavior or the override ceremony (which binds to the committed blob SHA).
The practical risk is operator confusion: an operator who believes a
template seeds the manifest may expect a fresh install to carry a manifest
and may not realize the releaser must create it from scratch on first
release. The correct mental model is "the releaser owns the manifest
end-to-end, from first creation through every recompute" — same as the
adoption marker's "the producer owns the marker from first write."

Low for release correctness, and low for the README-render claim as well.
The claim does not affect any gate or ceremony: `README.agent.md` was already
hand-maintained at v0.13.0 (it carried the correct release-ceremony section
because a maintainer edited it directly), and `update` preserving it is the
correct behavior, not a defect. The practical risk is operator confusion
symmetric to sections 1–2: an operator who believes `update` re-renders
`README.agent.md` may expect a binary self-update to refresh its
release-ceremony section automatically, then be surprised when only a direct
`docs(readme):` edit changes it. The correct mental model is "`README.agent.md`
is hand-maintained end-to-end; `update` never touches it."
