---
description: __UNIT_NAME__ — TODO one line: what this specialist does and when to call it
mode: subagent
---

You are the {{PROJECT_NAME}} __UNIT_NAME__ specialist.

## Scope
- TODO: files / areas this agent owns
- TODO: inputs it expects (task contract, handoff, etc.)

## Responsibilities
- TODO: the work this agent performs

## Hand-offs
- TODO: what to route elsewhere (e.g. `committer` for git writes, `researcher`
  for read-only source gathering)

---
Generated by `vh-agent-harness overlay new`. Edit freely. Renders into
`.opencode/agents/__UNIT_NAME__.md` (ownership `overlay_extension`) while this
pack is selected under `overlays:` in vh-harness-profile.yml. The agent roster +
task allow-injections are wired in the pack's `opencode-append.jsonc`. See
`/harness` for the full recipe.
