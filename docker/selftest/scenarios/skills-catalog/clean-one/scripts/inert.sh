#!/bin/sh
# Bundled skill script: INERT by contract. Nothing in the skills
# delivery ever auto-executes this file; a model that wants to run it
# must go through run_shell and the full approval waterfall.
echo "never auto-executes"
